{{ header }}{{ column_left }}
<div id="content">
  <div class="page-header">
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-12 col-lg-offset-2 col-lg-8 col-xl-offset-2 col-xl-8">
          <div class="pull-right">
            <button type="submit" form="form-option-set" class="btn btn-primary"><i data-lucide="save" width="16" height="16"></i> {{ button_save }}</button>
            <a href="{{ cancel }}" class="btn btn-default"><i data-lucide="x" width="16" height="16"></i> {{ button_cancel }}</a>
          </div>
          <a href="{{ cancel }}" class="page-header__link">
            <span class="page-header__back">
              <i data-lucide="arrow-left" width="20" height="20"></i>
            </span>
            <span class="page-header__title">
              {% if entity_name %}
                {{ entity_name }}
                <span class="page-header__badge {{ status ? 'page-header__badge--active' : 'page-header__badge--inactive' }}">
                  {{ status ? text_active : text_inactive }}
                </span>
              {% else %}
                {{ text_form }}
              {% endif %}
            </span>
          </a>
        </div>
      </div>
      {% if error_warning %}
      <div class="row">
        <div class="col-sm-12 col-lg-offset-2 col-lg-8 col-xl-offset-2 col-xl-8">
          <div class="alert alert-danger alert-dismissible"><i data-lucide="circle-alert" width="16" height="16"></i> {{ error_warning }}
            <button type="button" class="close" data-dismiss="alert">&times;</button>
          </div>
        </div>
      </div>
      {% endif %}
    </div>
  </div>
  <div class="container-fluid">
    <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-option-set">
      <div class="row">
        <div class="col-sm-12 col-lg-offset-2 col-lg-8 col-xl-offset-2 col-xl-8">
          <div class="dcx-shell">
            <div class="dcx-panel">
              <div class="dcx-panel-head">
                <div class="dcx-panel-head__icon"><i data-lucide="layers-3" width="18" height="18"></i></div>
                <div>
                  <div class="dcx-panel-title">{{ text_panel_option_set_title }}</div>
                  <div class="dcx-panel-subtitle">{{ text_panel_option_set_subtitle }}</div>
                </div>
              </div>
              <div class="dcx-panel-body">
                <div class="dcx-fields">
                  <div class="dcx-field required">
                    <label class="dcx-label">{{ entry_name }}</label>
                    {% for language in languages %}
                    <div class="dc-attr-row__value">
                      <span class="lang-code">{{ language.code|split('-')|first|upper }}</span> <span class="lang-name">{{ language.name }}</span>
                      <input type="text" name="option_set_description[{{ language.language_id }}][name]" value="{{ (option_set_description[language.language_id] ? option_set_description[language.language_id].name)|e('html_attr') }}" placeholder="{{ entry_name }}" class="form-control" />
                    </div>
                    {% if error_name[language.language_id] %}
                    <div class="text-danger">{{ error_name[language.language_id] }}</div>
                    {% endif %}
                    {% endfor %}
                  </div>
                  <div class="dcx-field">
                    <label class="dcx-label" for="input-sort-order">{{ entry_sort_order }}</label>
                    <input type="text" name="sort_order" value="{{ sort_order }}" placeholder="{{ entry_sort_order }}" id="input-sort-order" class="form-control" />
                  </div>
                  <div class="dcx-field required">
                    <label class="dcx-label" for="input-status">{{ entry_status }}</label>
                    <select name="status" id="input-status" class="form-control">
                      {% if status %}
                      <option value="1" selected="selected">{{ text_enabled }}</option>
                      <option value="0">{{ text_disabled }}</option>
                      {% else %}
                      <option value="1">{{ text_enabled }}</option>
                      <option value="0" selected="selected">{{ text_disabled }}</option>
                      {% endif %}
                    </select>
                  </div>
                </div>
              </div>
            </div>

            <div class="dcx-panel" style="margin-top: 16px;">
              <div class="dcx-panel-head">
                <div class="dcx-panel-head__icon"><i data-lucide="sliders-horizontal" width="18" height="18"></i></div>
                <div>
                  <div class="dcx-panel-title">{{ text_panel_options_title }} <span data-toggle="tooltip" title="{{ text_help_options }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span></div>
                  <div class="dcx-panel-subtitle">{{ text_panel_options_subtitle }}</div>
                </div>
              </div>
              <div class="dcx-panel-body">
                <div id="set-options-container">
                  {% set option_row = 0 %}
                  {% for set_option in option_set_options %}
                  <div class="dc-attr-row" id="set-option-row{{ option_row }}">
                    <div class="dc-attr-row__name" style="flex:1 1 auto;">
                      <input type="text" name="option_set_option[{{ option_row }}][name]" value="{{ set_option.name|e('html_attr') }}" placeholder="{{ entry_option }}" class="form-control" style="width:100%;"/>
                      <input type="hidden" name="option_set_option[{{ option_row }}][option_id]" value="{{ set_option.option_id }}"/>
                      <input type="hidden" name="option_set_option[{{ option_row }}][type]" value="{{ set_option.type }}"/>
                      <div class="dc-set-opt-type text-muted" style="margin-top:3px;font-size:11px;min-height:13px;">{{ set_option.type }}</div>
                    </div>
                    <button type="button" onclick="$('#set-option-row{{ option_row }}').remove();" class="btn btn-link btn-remove-attr" data-toggle="tooltip" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                  </div>
                  {% set option_row = option_row + 1 %}
                  {% endfor %}
                </div>
                <button type="button" onclick="addSetOption();" class="btn btn-primary btn-sm" style="margin-top:8px;">
                  <i data-lucide="circle-plus" width="14" height="14"></i> {{ button_option_add }}
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </form>
  </div>
</div>

<script type="text/javascript"><!--
  var option_row = {{ option_row }};

  function addSetOption() {
	  var html = '<div class="dc-attr-row" id="set-option-row' + option_row + '">';
	  html += '  <div class="dc-attr-row__name" style="flex:1 1 auto;">';
	  html += '      <input type="text" name="option_set_option[' + option_row + '][name]" value="" placeholder="{{ entry_option }}" class="form-control" style="width:100%;"/>';
	  html += '      <input type="hidden" name="option_set_option[' + option_row + '][option_id]" value=""/>';
	  html += '      <input type="hidden" name="option_set_option[' + option_row + '][type]" value=""/>';
	  html += '      <div class="dc-set-opt-type text-muted" style="margin-top:3px;font-size:11px;min-height:13px;"></div>';
	  html += '  </div>';
	  html += '  <button type="button" onclick="$(\'#set-option-row' + option_row + '\').remove();" class="btn btn-link btn-remove-attr" data-toggle="tooltip" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>';
	  html += '</div>';

	  $('#set-options-container').append(html);
	  lucide.createIcons();

	  setOptionAutocomplete(option_row);

	  option_row++;
  }

  function setOptionAutocomplete(option_row) {
	  $('input[name=\'option_set_option[' + option_row + '][name]\']').autocomplete({
		  'source': function(request, response) {
			  $.ajax({
				  url: 'index.php?route=catalog/option_set/autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
				  dataType: 'json',
				  success: function(json) {
					  response($.map(json, function(item) {
						  return {
							  category: item.type,
							  label: item.name,
							  value: item.option_id
						  }
					  }));
				  }
			  });
		  },
		  'select': function(item) {
			  var $row = $('input[name=\'option_set_option[' + option_row + '][name]\']').closest('.dc-attr-row');
			  $('input[name=\'option_set_option[' + option_row + '][name]\']').val(item['label']);
			  $('input[name=\'option_set_option[' + option_row + '][option_id]\']').val(item['value']);
			  $('input[name=\'option_set_option[' + option_row + '][type]\']').val(item['category'] || '');
			  $row.find('.dc-set-opt-type').text(item['category'] || '');
		  }
	  });
  }

  $('#set-options-container .dc-attr-row').each(function(index, element) {
	  setOptionAutocomplete(index);
  });
//--></script>

{{ footer }}
