{{ header }}{{ column_left }}
<div id="content">
  <div class="page-header">
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-12 col-lg-12 col-xl-offset-1 col-xl-10">
          <a href="{{ cancel }}" class="page-header__link">
            <span class="page-header__back">
              <i data-lucide="arrow-left" width="20" height="20"></i>
            </span>
            <span class="page-header__title">
              {% if product_name %}
                {{ product_name }}
                <span class="page-header__badge {{ status ? 'page-header__badge--active' : 'page-header__badge--inactive' }}">
                  {{ status ? text_active : text_inactive }}
                </span>
              {% else %}
                {{ text_form }}
              {% endif %}
            </span>
          </a>
          <div class="pull-right">
            <button type="submit" form="form-product" class="btn btn-primary"><i data-lucide="save" width="16" height="16"></i> {{ button_save }}</button>
            <a href="{{ cancel }}" class="btn btn-default"><i data-lucide="x" width="16" height="16"></i> {{ button_cancel }}</a>
          </div>
        </div>
      </div>
      {% if error_warning %}
      <div class="row">
        <div class="col-sm-12 col-lg-12 col-xl-offset-1 col-xl-10">
          <div class="alert alert-danger alert-dismissible"><i data-lucide="circle-alert" width="16" height="16"></i> {{ error_warning }}
            <button type="button" class="close" data-dismiss="alert">&times;</button>
          </div>
        </div>
      </div>
      {% endif %}
    </div>
  </div>
  <div class="container-fluid">
    <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-product">
    <div class="row">
      <div class="col-md-12 col-sm-12 col-lg-8 col-xl-offset-1 col-xl-7">
        <div class="dcx-shell">
              <div class="dcx-panel">
                <div class="dcx-panel-head">
                  <div class="dcx-panel-head__icon"><i data-lucide="file-text" width="18" height="18"></i></div>
                  <div>
                    <div class="dcx-panel-title">{{ text_panel_description_title }}</div>

                  </div>
                </div>
                <div class="dcx-panel-body">
                  <ul class="nav nav-tabs" id="language">
                    {% for language in languages %}
                      <li><a href="#language{{ language.language_id }}" data-toggle="tab"><span class="lang-code">{{ language.code|split('-')|first|upper }}</span> {{ language.name }}</a></li>
                    {% endfor %}
                  </ul>
                  <div class="tab-content">{% for language in languages %}
                      <div class="tab-pane" id="language{{ language.language_id }}">
                        <div class="dcx-fields">
                          <div class="dcx-field required">
                            <label class="dcx-label" for="input-name{{ language.language_id }}">{{ entry_name }}</label>
                            <input type="text" name="product_description[{{ language.language_id }}][name]" value="{{ (product_description[language.language_id] ? product_description[language.language_id].name)|e('html_attr') }}" placeholder="{{ entry_name }}" id="input-name{{ language.language_id }}" class="form-control"/>
                            {% if error_name[language.language_id] %}
                              <div class="text-danger">{{ error_name[language.language_id] }}</div>
                            {% endif %}
                          </div>
                          <div class="dcx-field">
                            <label class="dcx-label" for="input-description{{ language.language_id }}">{{ entry_description }}</label>
                            <textarea name="product_description[{{ language.language_id }}][description]" placeholder="{{ entry_description }}" id="input-description{{ language.language_id }}" data-toggle="tinymce" class="form-control">{{ product_description[language.language_id] ? product_description[language.language_id].description }}</textarea>
                          </div>
                          <div class="dcx-field required">
                            <label class="dcx-label" for="input-meta-title{{ language.language_id }}">{{ entry_meta_title }}</label>
                            <input type="text" name="product_description[{{ language.language_id }}][meta_title]" value="{{ (product_description[language.language_id] ? product_description[language.language_id].meta_title)|e('html_attr') }}" placeholder="{{ entry_meta_title }}" id="input-meta-title{{ language.language_id }}" class="form-control"/>
                            {% if error_meta_title[language.language_id] %}
                              <div class="text-danger">{{ error_meta_title[language.language_id] }}</div>
                            {% endif %}
                          </div>
                          <div class="dcx-field">
                            <label class="dcx-label" for="input-meta-description{{ language.language_id }}">{{ entry_meta_description }}</label>
                            <textarea name="product_description[{{ language.language_id }}][meta_description]" rows="5" placeholder="{{ entry_meta_description }}" id="input-meta-description{{ language.language_id }}" class="form-control">{{ product_description[language.language_id] ? product_description[language.language_id].meta_description }}</textarea>
                          </div>
                          <div class="dcx-field">
                            <label class="dcx-label" for="input-meta-keyword{{ language.language_id }}">{{ entry_meta_keyword }}</label>
                            <textarea name="product_description[{{ language.language_id }}][meta_keyword]" rows="5" placeholder="{{ entry_meta_keyword }}" id="input-meta-keyword{{ language.language_id }}" class="form-control">{{ product_description[language.language_id] ? product_description[language.language_id].meta_keyword }}</textarea>
                          </div>
                          <div class="dcx-field">
                            <label class="dcx-label" for="input-tag{{ language.language_id }}">{{ entry_tag }}</label>
                            <div class="dc-tag-input">
                              <input type="text" id="input-tag{{ language.language_id }}" class="form-control dc-tag-input__field" placeholder="{{ entry_tag }}"/>
                              <div class="dc-tag-input__tags" id="tag-chips{{ language.language_id }}"></div>
                              <input type="hidden" name="product_description[{{ language.language_id }}][tag]" value="{{ product_description[language.language_id] ? product_description[language.language_id].tag }}"/>
                              <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_tag }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                            </div>
                          </div>
                        </div>
                      </div>
                    {% endfor %}</div>
                </div>
              </div>
              <div class="dcx-panel" style="margin-top: 16px;">
                <div class="dcx-panel-head">
                  <div class="dcx-panel-head__icon"><i data-lucide="sliders-vertical" width="18" height="18"></i></div>
                  <div>
                    <div class="dcx-panel-title">{{ text_panel_identifiers_title }}</div>

                  </div>
                </div>
                <div class="dcx-panel-body">
                  <div class="dc-axis-info-banner" id="dc-note-configurable-identifiers" style="display:none"><i data-lucide="info" width="14" height="14"></i> {{ help_configurable_identifiers }}</div>
                  <div class="row">
                    <div class="col-sm-3">
                      <div class="dcx-field required">
                        <label class="dcx-label" for="input-model">{{ entry_model }}</label>
                        <div class="dcx-field-icon-wrap">
                          <input type="text" name="model" value="{{ model }}" placeholder="{{ entry_model }}" id="input-model" class="form-control"/>
                          <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_model }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                        </div>
                        {% if error_model %}
                          <div class="text-danger">{{ error_model }}</div>
                        {% endif %}
                      </div>
                    </div>
                    <div class="col-sm-3">
                      <div class="dcx-field">
                        <label class="dcx-label" for="input-sku">{{ entry_sku }}</label>
                        <div class="dcx-field-icon-wrap">
                          <input type="text" name="sku" value="{{ sku }}" placeholder="{{ entry_sku }}" id="input-sku" class="form-control"/>
                          <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_sku }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                        </div>
                      </div>
                    </div>
                  </div>
                  <div class="dc-article-extra" style="display:{% if upc or ean or jan or isbn or mpn %}block{% else %}none{% endif %};">
                    <div class="row">
                      <div class="col-sm-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-upc">{{ entry_upc }}</label>
                          <div class="dcx-field-icon-wrap">
                            <input type="text" name="upc" value="{{ upc }}" placeholder="{{ entry_upc }}" id="input-upc" class="form-control"/>
                            <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_upc }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                          </div>
                        </div>
                      </div>
                      <div class="col-sm-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-ean">{{ entry_ean }}</label>
                          <div class="dcx-field-icon-wrap">
                            <input type="text" name="ean" value="{{ ean }}" placeholder="{{ entry_ean }}" id="input-ean" class="form-control"/>
                            <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_ean }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                          </div>
                        </div>
                      </div>
                    </div>
                    <div class="row">
                      <div class="col-sm-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-jan">{{ entry_jan }}</label>
                          <div class="dcx-field-icon-wrap">
                            <input type="text" name="jan" value="{{ jan }}" placeholder="{{ entry_jan }}" id="input-jan" class="form-control"/>
                            <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_jan }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                          </div>
                        </div>
                      </div>
                      <div class="col-sm-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-isbn">{{ entry_isbn }}</label>
                          <div class="dcx-field-icon-wrap">
                            <input type="text" name="isbn" value="{{ isbn }}" placeholder="{{ entry_isbn }}" id="input-isbn" class="form-control"/>
                            <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_isbn }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                          </div>
                        </div>
                      </div>
                    </div>
                    <div class="row">
                      <div class="col-sm-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-mpn">{{ entry_mpn }}</label>
                          <div class="dcx-field-icon-wrap">
                            <input type="text" name="mpn" value="{{ mpn }}" placeholder="{{ entry_mpn }}" id="input-mpn" class="form-control"/>
                            <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_mpn }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                          </div>
                        </div>
                      </div>
                      <div class="col-sm-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-location">{{ entry_location }}</label>
                          <input type="text" name="location" value="{{ location }}" placeholder="{{ entry_location }}" id="input-location" class="form-control"/>
                        </div>
                      </div>
                    </div>
                  </div>
                  <div class="dcx-field">
                    <div>
                      <button type="button" id="toggle-articles" class="btn btn-default btn-sm"><i data-lucide="toggle-left" width="14" height="14"></i> <span id="toggle-articles-text">{{ button_show_articles }}</span></button>
                    </div>
                  </div>
                </div>
              </div>
              <div class="dcx-panel" style="margin-top: 16px;">
                <div class="dcx-panel-head">
                  <div class="dcx-panel-head__icon"><i data-lucide="credit-card" width="18" height="18"></i></div>
                  <div>
                    <div class="dcx-panel-title">{{ text_panel_pricing_title }}</div>

                  </div>
                </div>
                <div class="dcx-panel-body">
                  <div class="dc-axis-info-banner" id="dc-note-configurable-pricing" style="display:none"><i data-lucide="info" width="14" height="14"></i> {{ help_configurable_pricing }}</div>
                  <div class="dcx-fields">
                    <div class="row">
                      <div class="col-sm-12 col-lg-6 col-xl-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-price">{{ entry_price }}</label>
                          <input type="text" name="price" value="{{ price }}" placeholder="{{ entry_price }}" id="input-price" class="form-control"/>
                          <div class="dcx-checkbox-group">
                            <label class="dcx-checkbox-label">
                              <input type="checkbox" name="call_for_price" value="1" id="input-call-for-price"{% if call_for_price %} checked{% endif %} /> {{ entry_call_for_price }}
                              <span data-toggle="tooltip" title="{{ help_call_for_price }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span>
                            </label>
                          </div>
                        </div>
                      </div>
                      <div class="col-sm-12 col-lg-6 col-xl-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-currency">{{ entry_currency }}</label>
                          <select name="currency_id" id="input-currency" class="form-control">
                            <option value="0">{{ text_store_default }}</option>
                            {% for currency in currencies %}
                              {% if currency.currency_id == currency_id %}
                                <option value="{{ currency.currency_id }}" selected="selected">{{ currency.title }} ({{ currency.code }})</option>
                              {% else %}
                                <option value="{{ currency.currency_id }}">{{ currency.title }} ({{ currency.code }})</option>
                              {% endif %}
                            {% endfor %}
                          </select>
                          <span data-toggle="tooltip" title="{{ help_currency }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span>
                        </div>
                      </div>
                    </div>
                    <div class="dcx-field">
                      <label class="dcx-label">{{ entry_customer_group_price }}</label>
                      <div id="customer-group-prices">
                        {% set customer_group_price_row = 0 %}
                        {% for product_customer_group_price in product_customer_group_prices %}
                        <div class="dc-cgp-row" id="customer-group-price-row{{ customer_group_price_row }}">
                          <select name="product_customer_group_price[{{ customer_group_price_row }}][customer_group_id]" class="form-control dc-cgp-select">
                            {% for customer_group in customer_groups %}
                              {% if customer_group.customer_group_id == product_customer_group_price.customer_group_id %}
                                <option value="{{ customer_group.customer_group_id }}" selected="selected">{{ customer_group.name }}</option>
                              {% else %}
                                <option value="{{ customer_group.customer_group_id }}">{{ customer_group.name }}</option>
                              {% endif %}
                            {% endfor %}
                          </select>
                          <input type="text" name="product_customer_group_price[{{ customer_group_price_row }}][price]" value="{{ product_customer_group_price.price }}" placeholder="{{ entry_price }}" class="form-control dc-cgp-price"/>
                          <button type="button" onclick="$('#customer-group-price-row{{ customer_group_price_row }}').remove();" class="btn btn-link dc-cgp-remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                        </div>
                        {% set customer_group_price_row = customer_group_price_row + 1 %}
                        {% endfor %}
                      </div>
                      <button type="button" onclick="addCustomerGroupPrice();" class="btn btn-default btn-sm"><i data-lucide="plus" width="14" height="14"></i> {{ button_customer_group_price_add }}</button>
                    </div>
                    {% if tax_classes %}
                    <div class="row">
                      <div class="col-sm-4">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-tax-class">{{ entry_tax_class }}</label>
                          <select name="tax_class_id" id="input-tax-class" class="form-control">
                            <option value="0">{{ text_none }}</option>
                            {% for tax_class in tax_classes %}
                              {% if tax_class.tax_class_id == tax_class_id %}
                                <option value="{{ tax_class.tax_class_id }}" selected="selected">{{ tax_class.title }}</option>
                              {% else %}
                                <option value="{{ tax_class.tax_class_id }}">{{ tax_class.title }}</option>
                              {% endif %}
                            {% endfor %}
                          </select>
                        </div>
                      </div>
                    </div>
                    {% endif %}
                  </div>
                </div>
              </div>
              <div class="dcx-panel" style="margin-top: 16px;">
                <div class="dcx-panel-head">
                  <div class="dcx-panel-head__icon"><i data-lucide="package" width="18" height="18"></i></div>
                  <div>
                    <div class="dcx-panel-title">{{ text_panel_inventory_title }}</div>

                  </div>
                </div>
                <div class="dcx-panel-body">
                  <div class="dc-axis-info-banner" id="dc-note-configurable-inventory" style="display:none"><i data-lucide="info" width="14" height="14"></i> {{ help_configurable_inventory }}</div>
                  <div class="dcx-fields">
                    <div class="row">
                      <div class="col-sm-12 col-lg-6 col-xl-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-quantity">{{ entry_quantity }}</label>
                          <input type="text" name="quantity" value="{{ quantity }}" placeholder="{{ entry_quantity }}" id="input-quantity" class="form-control"/>
                          <div class="dcx-checkbox-group">
                            <label class="dcx-checkbox-label">
                              <input type="checkbox" name="subtract" value="1"{% if subtract %} checked{% endif %} /> {{ entry_subtract }}
                              <span data-toggle="tooltip" title="{{ help_subtract }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span>
                            </label>
                            <label class="dcx-checkbox-label">
                              <input type="checkbox" name="preorder" value="1"{% if preorder %} checked{% endif %} /> {{ entry_preorder }}
                              <span data-toggle="tooltip" title="{{ help_preorder }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span>
                            </label>
                            <label class="dcx-checkbox-label">
                              <input type="checkbox" name="shipping" value="1"{% if shipping %} checked{% endif %} /> {{ entry_shipping }}
                              <span data-toggle="tooltip" title="{{ help_shipping }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span>
                            </label>
                          </div>
                        </div>
                      </div>
                    </div>
                    <div class="row">
                      <div class="col-sm-6 col-lg-4 col-xl-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-minimum">{{ entry_minimum }}</label>
                          <div class="dcx-field-icon-wrap">
                            <input type="text" name="minimum" value="{{ minimum }}" placeholder="{{ entry_minimum }}" id="input-minimum" class="form-control"/>
                            <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_minimum }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                          </div>
                        </div>
                      </div>
                      <div class="col-sm-6 col-lg-4 col-xl-3">
                        <div class="dcx-field">
                          <label class="dcx-label" for="input-quantity-step">{{ entry_quantity_step }}</label>
                          <div class="dcx-field-icon-wrap">
                            <input type="text" name="quantity_step" value="{{ quantity_step }}" placeholder="{{ entry_quantity_step }}" id="input-quantity-step" class="form-control"/>
                            <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_quantity_step }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="dcx-panel" style="margin-top: 16px;">
                <div class="dcx-panel-head">
                  <div class="dcx-panel-head__icon"><i data-lucide="ruler" width="18" height="18"></i></div>
                  <div>
                    <div class="dcx-panel-title">{{ text_panel_dimensions_title }}</div>

                  </div>
                </div>
                <div class="dcx-panel-body">
                  <div class="dc-axis-info-banner" id="dc-note-configurable-dimensions" style="display:none"><i data-lucide="info" width="14" height="14"></i> {{ help_configurable_dimensions }}</div>
                  <div class="dcx-fields">
                    <div class="dcx-field">
                      <label class="dcx-label" for="input-length">{{ entry_dimension }}</label>
                      <div class="row{% if is_configurable %} dc-opts-axis-hidden{% endif %}">
                        <div class="col-sm-6 col-lg-3">
                          <input type="text" name="length" value="{{ length }}" placeholder="{{ entry_length }}" id="input-length" class="form-control"/>
                        </div>
                        <div class="col-sm-6 col-lg-3">
                          <input type="text" name="width" value="{{ width }}" placeholder="{{ entry_width }}" id="input-width" class="form-control"/>
                        </div>
                        <div class="col-sm-6 col-lg-3">
                          <input type="text" name="height" value="{{ height }}" placeholder="{{ entry_height }}" id="input-height" class="form-control"/>
                        </div>
                        <div class="col-sm-6 col-lg-3">
                          <select name="length_class_id" id="input-length-class" class="form-control">
                            {% for length_class in length_classes %}
                              {% if length_class.length_class_id == length_class_id %}
                                <option value="{{ length_class.length_class_id }}" selected="selected">{{ length_class.title }}</option>
                              {% else %}
                                <option value="{{ length_class.length_class_id }}">{{ length_class.title }}</option>
                              {% endif %}
                            {% endfor %}
                          </select>
                        </div>
                      </div>
                    </div>
                    <div class="dcx-field">
                      <label class="dcx-label" for="input-weight">{{ entry_weight }}</label>
                      <div class="row">
                        <div class="col-sm-6 col-lg-3">
                          <input type="text" name="weight" value="{{ weight }}" placeholder="{{ entry_weight }}" id="input-weight" class="form-control"/>
                        </div>
                        <div class="col-sm-6 col-lg-3">
                          <select name="weight_class_id" id="input-weight-class" class="form-control">
                            {% for weight_class in weight_classes %}
                              {% if weight_class.weight_class_id == weight_class_id %}
                                <option value="{{ weight_class.weight_class_id }}" selected="selected">{{ weight_class.title }}</option>
                              {% else %}
                                <option value="{{ weight_class.weight_class_id }}">{{ weight_class.title }}</option>
                              {% endif %}
                            {% endfor %}
                          </select>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="dcx-panel" style="margin-top: 16px;">
                <div class="dcx-panel-head">
                  <div class="dcx-panel-head__icon"><i data-lucide="image" width="18" height="18"></i></div>
                  <div>
                    <div class="dcx-panel-title">{{ text_panel_media_title }}</div>

                  </div>
                </div>
                <div class="dcx-panel-body">
                  <div class="dcx-fields">
                    <div class="dcx-field">
                      <label class="dcx-label">{{ entry_images }}</label>
                      <div class="product-media-gallery">
                        <div class="dc-img-card dc-img-card--main">
                          <a href="" id="thumb-image" data-toggle="image" class="dc-img-link" style="margin:0;"><img src="{{ thumb }}" alt="" title="" data-placeholder="{{ placeholder }}" /></a>
                          <input type="hidden" name="image" value="{{ image }}" id="input-image"/>
                        </div>
                        <div class="product-media-thumbs">
                          <ul class="nav nav-tabs" id="language-images">
                            <li class="active"><a href="#tab-image-all" data-toggle="tab"><i data-lucide="globe" width="14" height="14"></i> {{ text_all_languages }}</a></li>
                            {% for language in languages %}
                            <li><a href="#tab-image-{{ language.language_id }}" data-toggle="tab"><span class="lang-code">{{ language.code|split('-')|first|upper }}</span> {{ language.name }}</a></li>
                            {% endfor %}
                          </ul>
                          <div class="tab-content" style="padding-top:12px;">
                            {% set image_row = 0 %}
                            <div class="tab-pane active" id="tab-image-all">
                              <div class="product-images" id="product-images-all">
                                {% for product_image in product_images_global %}
                                <div class="dc-img-card" data-row="{{ image_row }}" title="{{ text_drag_to_reorder }}">
                                  <span class="dc-img-card__handle" title="{{ text_drag_to_reorder }}" aria-hidden="true"><i data-lucide="grip-vertical" width="12" height="12" class="slide-drag-handle"></i></span>
                                  <input type="hidden" name="product_image[{{ image_row }}][image]" value="{{ product_image.image }}" id="input-image{{ image_row }}"/>
                                  <input type="hidden" name="product_image[{{ image_row }}][sort_order]" value="{{ product_image.sort_order }}" class="dc-img-sort-order"/>
                                  <input type="hidden" name="product_image[{{ image_row }}][language_id]" value="{{ product_image.language_id }}"/>
                                  <a href="" id="thumb-image{{ image_row }}" data-toggle="image" class="dc-img-link"><img src="{{ product_image.thumb }}" alt="" title="" data-placeholder="{{ placeholder }}" draggable="false"/></a>
                                  <button type="button" onclick="removeImage({{ image_row }});" class="dc-img-remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                                </div>
                                {% set image_row = image_row + 1 %}
                                {% endfor %}
                              </div>
                              {% if product_images_global|length > 0 %}
                              <small class="form-text text-muted" style="display:block; margin-bottom:12px;">{{ text_drag_to_reorder }}</small>
                              {% endif %}
                              <button type="button" onclick="addImage('');" class="btn btn-default" style="margin-top:12px;"><i data-lucide="plus" width="14" height="14"></i> {{ button_image_add }}</button>
                            </div>
                            {% for language in languages %}
                            <div class="tab-pane" id="tab-image-{{ language.language_id }}">
                              <div class="product-images" id="product-images-{{ language.language_id }}">
                                {% set lang_images = product_images_by_lang[language.language_id] is defined ? product_images_by_lang[language.language_id] : [] %}
                                {% for product_image in lang_images %}
                                <div class="dc-img-card" data-row="{{ image_row }}" title="{{ text_drag_to_reorder }}">
                                  <span class="dc-img-card__handle" title="{{ text_drag_to_reorder }}" aria-hidden="true"><i data-lucide="grip-vertical" width="12" height="12" class="slide-drag-handle"></i></span>
                                  <input type="hidden" name="product_image[{{ image_row }}][image]" value="{{ product_image.image }}" id="input-image{{ image_row }}"/>
                                  <input type="hidden" name="product_image[{{ image_row }}][sort_order]" value="{{ product_image.sort_order }}" class="dc-img-sort-order"/>
                                  <input type="hidden" name="product_image[{{ image_row }}][language_id]" value="{{ product_image.language_id }}"/>
                                  <a href="" id="thumb-image{{ image_row }}" data-toggle="image" class="dc-img-link"><img src="{{ product_image.thumb }}" alt="" title="" data-placeholder="{{ placeholder }}" draggable="false"/></a>
                                  <button type="button" onclick="removeImage({{ image_row }});" class="dc-img-remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                                </div>
                                {% set image_row = image_row + 1 %}
                                {% endfor %}
                              </div>
                              {% if lang_images|length > 0 %}
                              <small class="form-text text-muted" style="display:block; margin-bottom:12px;">{{ text_drag_to_reorder }}</small>
                              {% endif %}
                              <button type="button" onclick="addImage('{{ language.language_id }}');" class="btn btn-default" style="margin-top:12px;"><i data-lucide="plus" width="14" height="14"></i> {{ button_image_add }}</button>
                            </div>
                            {% endfor %}
                          </div>
                        </div>
                      </div>
                    </div>
                    <div class="dcx-field">
                      <label class="dcx-label">{{ entry_video }}</label>
                      <div class="dc-video-wrapper" style="max-width:500px;">
                        <div class="input-group" style="margin-bottom:8px;">
                          <span class="input-group-addon"><i data-lucide="video" width="14" height="14"></i></span>
                          <input type="text" name="video_youtube" value="{{ video_type == 'youtube' ? video : '' }}" id="input-video-youtube" class="form-control" placeholder="{{ entry_video_youtube_placeholder }}" />
                          <span class="input-group-btn">
                            <button type="button" onclick="clearProductVideo()" class="btn btn-default" title="{{ button_remove }}"><i data-lucide="trash-2" width="14" height="14"></i></button>
                          </span>
                        </div>
                        <div id="video-youtube-preview" style="margin-bottom:8px;{{ video_type == 'youtube' and video ? '' : 'display:none;' }}">
                          <img src="{{ (video_type == 'youtube' and video_youtube_id) ? ('https://img.youtube.com/vi/' ~ video_youtube_id ~ '/hqdefault.jpg') : video_placeholder }}" id="video-youtube-thumb-img" style="max-height:90px;border-radius:4px;" data-placeholder="{{ video_placeholder }}" onerror="this.onerror=null;this.src='{{ video_placeholder }}';" />
                          <div style="font-size:11px;color:#6b7280;margin-top:4px;">{{ text_youtube_video }}</div>
                        </div>
                        <div id="video-mp4-block" style="margin-bottom:8px;{{ video_type == 'mp4' and video ? '' : 'display:none;' }}">
                          <div class="dc-video-card">
                            <a href="" id="thumb-product-video" data-toggle="image" data-type="video" class="dc-video-card__link">
                              <img src="{{ video_thumb }}" data-placeholder="{{ video_placeholder }}" class="dc-video-card__img" />
                              <div class="dc-video-card__overlay"{% if not (video_type == 'mp4' and video) %} style="display:none;"{% endif %}>
                                <i data-lucide="film" class="dc-video-card__icon"></i>
                                <span class="dc-video-card__name">{{ video }}</span>
                              </div>
                            </a>
                            <button type="button" onclick="clearProductVideo()" class="dc-video-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                            <input type="hidden" name="video_mp4" value="{{ video }}" id="input-product-video" />
                          </div>
                        </div>
                        <button type="button" onclick="openVideoFileManager();" class="btn btn-default" id="button-browse-mp4"><i data-lucide="folder-open" width="14" height="14"></i> {{ button_browse_mp4 }}</button>
                        <span class="help-block text-muted">{{ help_video_combined }}</span>
                      </div>
                    </div>
                    <div class="dcx-field">
                      <label class="dcx-label">{{ entry_model_3d }}</label>
                      <div class="dc-video-card">
                        <a href="" id="thumb-model-3d" data-toggle="image" data-type="model_3d" class="dc-video-card__link">
                          <img src="{{ model_3d_thumb }}" data-placeholder="{{ model_3d_placeholder }}" class="dc-video-card__img" />
                          <div class="dc-video-card__overlay"{% if not model_3d %} style="display:none;"{% endif %}>
                            <i data-lucide="box" class="dc-video-card__icon"></i>
                            <span class="dc-video-card__name">{{ model_3d }}</span>
                          </div>
                        </a>
                        <button type="button" onclick="clearModel3d()" class="dc-video-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                        <input type="hidden" name="model_3d" value="{{ model_3d }}" id="input-model-3d" />
                      </div>
                      <div class="help-block text-muted" style="margin-top:8px;">{{ help_model_3d }}</div>
                    </div>
                    <div class="dcx-field">
                      <label class="dcx-label">{{ entry_image_360 }}</label>
                      <div class="dc-video-card">
                        <a href="" id="thumb-image-360" data-toggle="image" data-type="image" class="dc-video-card__link">
                          <img src="{{ image_360_thumb }}" data-placeholder="{{ image_360_placeholder }}" class="dc-video-card__img" />
                          <div class="dc-video-card__overlay"{% if not image_360 %} style="display:none;"{% endif %}>
                            <i data-lucide="rotate-3d" class="dc-video-card__icon"></i>
                            <span class="dc-video-card__name">{{ image_360 }}</span>
                          </div>
                        </a>
                        <button type="button" onclick="clearImage360()" class="dc-video-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                        <input type="hidden" name="image_360" value="{{ image_360 }}" id="input-image-360" />
                      </div>
                      <div class="help-block text-muted" style="margin-top:8px;">{{ help_image_360 }}</div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="dcx-panel" style="margin-top: 16px;">
                <div class="dcx-panel-head">
                  <div class="dcx-panel-head__icon"><i data-lucide="list-checks" width="18" height="18"></i></div>
                  <div>
                    <div class="dcx-panel-title">{{ text_panel_attributes_title }} <span data-toggle="tooltip" title="{{ text_help_attributes }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span></div>
                  </div>
                </div>
                <div class="dcx-panel-body">
                  <div class="dc-attr-set-toolbar" style="display:flex;align-items:center;gap:8px;margin-bottom:10px;flex-wrap:wrap;">
                    <label for="dc-attribute-set-select" style="margin:0;white-space:nowrap;">{{ text_attribute_set_selector }} <span data-toggle="tooltip" title="{{ text_help_attribute_set }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span></label>
                    <select id="dc-attribute-set-select" class="form-control" style="width:auto;display:inline-block;min-width:220px;">
                      <option value=""></option>
                      {% for attribute_set in attribute_sets %}
                      <option value="{{ attribute_set.attribute_set_id }}">{{ attribute_set.name }} ({{ attribute_set.attribute_count }})</option>
                      {% else %}
                      <option value="" disabled="disabled">{{ text_no_attribute_sets }}</option>
                      {% endfor %}
                    </select>
                    <button type="button" class="btn btn-default btn-sm" onclick="loadAttributeSet();">
                      <i data-lucide="download" width="14" height="14"></i> {{ button_attribute_set_load }}
                    </button>
                  </div>
                  <div id="attributes-container">
                    {% set attribute_row = 0 %}
                    {% for product_attribute in product_attributes %}
                    <div class="dc-attr-row" id="attribute-row{{ attribute_row }}">
                      <div class="dc-attr-row__name">
                        <input type="text" name="product_attribute[{{ attribute_row }}][name]" value="{{ product_attribute.name|e('html_attr') }}" placeholder="{{ entry_attribute }}" class="form-control"/>
                        <input type="hidden" name="product_attribute[{{ attribute_row }}][attribute_id]" value="{{ product_attribute.attribute_id }}"/>
                      </div>
                      <div class="dc-attr-row__values">
                        {% for language in languages %}
                        <div class="dc-attr-row__value">
                          <span class="lang-code">{{ language.code|split('-')|first|upper }}</span>
                          <input type="text" name="product_attribute[{{ attribute_row }}][product_attribute_description][{{ language.language_id }}][text]" value="{{ (product_attribute.product_attribute_description[language.language_id] ? product_attribute.product_attribute_description[language.language_id].text)|e('html_attr') }}" placeholder="{{ entry_text }}" class="form-control"/>
                        </div>
                        {% endfor %}
                      </div>
                      <button type="button" onclick="$('#attribute-row{{ attribute_row }}').remove();" class="btn btn-link btn-remove-attr" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                    </div>
                    {% set attribute_row = attribute_row + 1 %}
                    {% endfor %}
                  </div>
                  <button type="button" onclick="addAttribute();" class="btn btn-primary btn-sm" style="margin-top:8px;">
                    <i data-lucide="circle-plus" width="14" height="14"></i> {{ button_attribute_add }}
                  </button>
                </div>
              </div>
              <div class="dcx-panel" style="margin-top: 16px;">
                <div class="dcx-panel-head">
                  <div class="dcx-panel-head__icon"><i data-lucide="list" width="18" height="18"></i></div>
                  <div>
                    <div class="dcx-panel-title">{{ text_panel_options_title }} <span data-toggle="tooltip" title="{{ text_help_options_mode }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span></div>
                  </div>
                </div>
                <div class="dcx-panel-body">
                  <div class="dc-option-mode-selector" style="margin-bottom:15px;padding:10px 15px;background:#f7f7f7;border-radius:4px">
                    <label style="margin:0;font-weight:600">{{ entry_option_mode }}:</label>
                    <span id="dc-option-mode-label" class="label {% if is_configurable %}label-primary{% else %}label-default{% endif %}" style="font-size:13px">
                      <i data-lucide="circle" width="10" height="10"></i> {% if is_configurable %}{{ text_mode_combined }}{% else %}{{ text_mode_simple }}{% endif %}
                    </span>
                    <button type="button" id="dc-toggle-mode-btn" class="btn btn-sm btn-default">
                      {% if is_configurable %}
                        <i data-lucide="arrow-left" width="14" height="14"></i> {{ text_switch_to_simple }}
                      {% else %}
                        {{ text_switch_to_combined }} <i data-lucide="arrow-right" width="14" height="14"></i>
                      {% endif %}
                    </button>
                    <span id="dc-mode-saved" style="color:green;display:none;font-size:12px"><i data-lucide="check" width="14" height="14"></i> {{ text_success_mode }}</span>
                  </div>

                  <div id="dc-variants-section" {% if not is_configurable %}style="display:none"{% endif %}>
                    {% if error_default_variant %}
                    <div class="alert alert-danger" id="dc-error-default-variant"><i data-lucide="circle-alert" width="14" height="14"></i> {{ error_default_variant }}</div>
                    {% endif %}
                    {% include 'catalog/product_variants.twig' %}
                  </div>

                  <div style="margin-top: 30px">
                    <h4>{{ tab_options }} <small id="dc-option-count">({{ product_options|length }})</small></h4>
                    <div class="dc-opt-set-toolbar" style="display:flex;align-items:center;gap:8px;margin-bottom:10px;flex-wrap:wrap;">
                      <label for="dc-option-set-select" style="margin:0;white-space:nowrap;">{{ text_option_set_selector }} <span title="{{ text_help_option_set }}" data-toggle="tooltip"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span></label>
                      <select id="dc-option-set-select" class="form-control" style="width:auto;display:inline-block;min-width:220px;">
                        <option value=""></option>
                        {% for option_set in option_sets %}
                        <option value="{{ option_set.option_set_id }}">{{ option_set.name }} ({{ option_set.option_count }})</option>
                        {% else %}
                        <option value="" disabled="disabled">{{ text_no_option_sets }}</option>
                        {% endfor %}
                      </select>
                      <button type="button" class="btn btn-default btn-sm" onclick="loadOptionSet();">
                        <i data-lucide="download" width="14" height="14"></i> {{ button_option_set_load }}
                      </button>
                    </div>
                    <div id="dc-options-container">
                      {% set option_row = 0 %}
                      {% set option_value_row = 0 %}
                      {% for product_option in product_options %}
                        <div class="dc-option-section{% if product_option.is_axis %} dc-option-axis{% endif %}" id="option-section{{ option_row }}">
                          <div class="dc-option-section__header">
                            <span class="dc-option-section__title">{{ product_option.name }}</span>
                            <span class="dc-option-section__type">({{ product_option.type }})</span>
                            {% if product_option.is_axis %}<span class="dc-axis-badge" title="{{ entry_axis_lock_warning|e('html_attr') }}"><i data-lucide="lock" width="10" height="10"></i> {{ text_variant|e('html') }}</span>{% endif %}
                            <div class="dc-option-section__controls">
                              <label class="dc-option-section__required">
                                <span>{{ entry_required }}:</span>
                                <select name="product_option[{{ option_row }}][required]" class="form-control input-sm"{% if product_option.is_axis %} disabled="disabled"{% endif %}>
                                  {% if product_option.required %}
                                    <option value="1" selected="selected">{{ text_yes }}</option>
                                    <option value="0">{{ text_no }}</option>
                                  {% else %}
                                    <option value="1">{{ text_yes }}</option>
                                    <option value="0" selected="selected">{{ text_no }}</option>
                                  {% endif %}
                                </select>
                              </label>
                              {% if not product_option.is_axis %}
                              <button type="button" class="btn btn-danger btn-sm" onclick="dcRemoveOption('{{ option_row }}')">
                                <i data-lucide="trash-2" width="14" height="14"></i> {{ button_remove }}
                              </button>
                              {% endif %}
                            </div>
                          </div>
                          {% if product_option.is_axis %}
                          <div class="dc-axis-info-banner"><i data-lucide="info" width="14" height="14"></i> {{ entry_axis_lock_warning }}</div>
                          {% endif %}
                          {% if error_option[product_option.option_id] is defined %}
                          <div class="alert alert-danger" style="margin:5px 15px"><i data-lucide="circle-alert" width="14" height="14"></i> {{ error_option[product_option.option_id] }}</div>
                          {% endif %}
                          <div class="dc-option-section__body">
                            <input type="hidden" name="product_option[{{ option_row }}][product_option_id]" value="{{ product_option.product_option_id }}"/>
                            <input type="hidden" name="product_option[{{ option_row }}][name]" value="{{ product_option.name|e('html_attr') }}"/>
                            <input type="hidden" name="product_option[{{ option_row }}][option_id]" value="{{ product_option.option_id }}"/>
                            <input type="hidden" name="product_option[{{ option_row }}][type]" value="{{ product_option.type }}"/>
                          {% if product_option.type == 'text' %}
                            <div class="form-group">
                              <label class="col-sm-2 control-label" for="input-value{{ option_row }}">{{ entry_option_value }}</label>
                              <div class="col-sm-10">
                                <input type="text" name="product_option[{{ option_row }}][value]" value="{{ product_option.value }}" placeholder="{{ entry_option_value }}" id="input-value{{ option_row }}" class="form-control"/>
                              </div>
                            </div>
                          {% endif %}
                          {% if product_option.type == 'textarea' %}
                            <div class="form-group">
                              <label class="col-sm-2 control-label" for="input-value{{ option_row }}">{{ entry_option_value }}</label>
                              <div class="col-sm-10">
                                <textarea name="product_option[{{ option_row }}][value]" rows="5" placeholder="{{ entry_option_value }}" id="input-value{{ option_row }}" class="form-control">{{ product_option.value }}</textarea>
                              </div>
                            </div>
                          {% endif %}
                          {% if product_option.type == 'file' %}
                            <div class="form-group" style="display: none;">
                              <label class="col-sm-2 control-label" for="input-value{{ option_row }}">{{ entry_option_value }}</label>
                              <div class="col-sm-10">
                                <input type="text" name="product_option[{{ option_row }}][value]" value="{{ product_option.value }}" placeholder="{{ entry_option_value }}" id="input-value{{ option_row }}" class="form-control"/>
                              </div>
                            </div>
                          {% endif %}
                          {% if product_option.type == 'date' %}
                            <div class="form-group">
                              <label class="col-sm-2 control-label" for="input-value{{ option_row }}">{{ entry_option_value }}</label>
                              <div class="col-sm-3">
                                <div class="input-group date">
                                  <input type="text" name="product_option[{{ option_row }}][value]" value="{{ product_option.value }}" placeholder="{{ entry_option_value }}" data-date-format="YYYY-MM-DD" id="input-value{{ option_row }}" class="form-control"/> <span class="input-group-btn">
                <button class="btn btn-default" type="button"><i data-lucide="calendar" width="14" height="14"></i></button>
                              </span></div>
                              </div>
                            </div>
                          {% endif %}
                          {% if product_option.type == 'time' %}
                            <div class="form-group">
                              <label class="col-sm-2 control-label" for="input-value{{ option_row }}">{{ entry_option_value }}</label>
                              <div class="col-sm-10">
                                <div class="input-group time">
                                  <input type="text" name="product_option[{{ option_row }}][value]" value="{{ product_option.value }}" placeholder="{{ entry_option_value }}" data-date-format="HH:mm" id="input-value{{ option_row }}" class="form-control"/> <span class="input-group-btn">
                              <button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button>
                              </span></div>
                              </div>
                            </div>
                          {% endif %}
                          {% if product_option.type == 'datetime' %}
                            <div class="form-group">
                              <label class="col-sm-2 control-label" for="input-value{{ option_row }}">{{ entry_option_value }}</label>
                              <div class="col-sm-10">
                                <div class="input-group datetime">
                                  <input type="text" name="product_option[{{ option_row }}][value]" value="{{ product_option.value }}" placeholder="{{ entry_option_value }}" data-date-format="YYYY-MM-DD HH:mm" id="input-value{{ option_row }}" class="form-control"/> <span class="input-group-btn">
                              <button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button>
                              </span></div>
                              </div>
                            </div>
                          {% endif %}
                          {% if product_option.type == 'select' or product_option.type == 'radio' or product_option.type == 'checkbox' or product_option.type == 'image' or product_option.type == 'color' %}
                            <div class="dc-opt-cards" id="option-value{{ option_row }}">
                              {% for product_option_value in product_option.product_option_value %}
                                <div class="dc-opt-card" id="option-value-row{{ option_value_row }}">
                                  <div class="dc-opt-card__header">
                                    <span class="dc-opt-card__handle" title="{{ text_drag_to_reorder }}" aria-hidden="true"><i data-lucide="grip-vertical" width="14" height="14" class="slide-drag-handle"></i></span>
                                    <span class="dc-opt-card__title">{% for ov in option_values[product_option.option_id]|default([]) %}{% if ov.option_value_id == product_option_value.option_value_id %}{{ ov.name }}{% endif %}{% endfor %}</span>
<span class="dc-opt-card__badges">
                                       {% if product_option_value.is_hit %}<span class="dc-opt-badge dc-opt-badge--hit">{{ entry_hit }}</span>{% endif %}
                                       {% if product_option_value.price != 0 %}
                                        <span class="dc-opt-badge dc-opt-badge--price" data-badge="price">{{ product_option_value.price_prefix }}{{ product_option_value.price }}</span>
                                      {% else %}
                                        <span class="dc-opt-badge dc-opt-badge--price" data-badge="price" style="display:none"></span>
                                      {% endif %}
                                    </span>
                                     <button type="button" class="btn btn-danger btn-xs dc-opt-card__remove"{% if product_option.is_axis %} style="display:none"{% endif %} onclick="$(this).closest('.dc-opt-card').remove();" data-toggle="tooltip" title="{{ button_remove }}"><i data-lucide="circle-minus" width="14" height="14"></i></button>
                                  </div>
                                  <div class="dc-opt-card__body">
                                    <input type="hidden" name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][product_option_value_id]" value="{{ product_option_value.product_option_value_id }}"/>
                                    <input type="hidden" name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][is_hit]" value="0"/>
                                    <input type="hidden" name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][sort_order]" value="{{ option_value_row }}" class="dc-opt-sort-order"/>
<div class="dc-opt-card__grid">
                                        <div class="dc-opt-card__field">
                                          <label>{{ entry_option_value }}</label>
                                          <select name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][option_value_id]" class="form-control"{% if product_option.is_axis %} disabled="disabled"{% endif %} onchange="dcUpdateCardTitle(this)">
                                           {% if option_values[product_option.option_id] %}
                                             {% for option_value in option_values[product_option.option_id] %}
                                               <option value="{{ option_value.option_value_id }}"{% if option_value.option_value_id == product_option_value.option_value_id %} selected="selected"{% endif %}>{{ option_value.name }}</option>
                                             {% endfor %}
                                           {% endif %}
                                         </select>
                                       </div>
                                        <div class="dc-opt-card__field{% if product_option.is_axis %} dc-opt-card__field--locked{% endif %}">
                                          <label>{{ entry_price }}</label>
                                         <div class="option-value-prefix-group">
                                           <select name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][price_prefix]" class="form-control option-value-prefix-select"{% if product_option.is_axis %} disabled="disabled"{% endif %}>
                                             <option value="+"{% if product_option_value.price_prefix == '+' %} selected="selected"{% endif %}>+</option>
                                             <option value="-"{% if product_option_value.price_prefix == '-' %} selected="selected"{% endif %}>-</option>
                                           </select>
                                           <input type="text" name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][price]" value="{{ product_option_value.price }}" placeholder="{{ entry_price }}" class="form-control option-value-prefix-input"{% if product_option.is_axis %} readonly="readonly" data-axis-price="1"{% endif %} oninput="dcUpdateBadges(this)" />
                                         </div>
                                       </div>
                                       <div class="dc-opt-card__field{% if product_option.is_axis %} dc-opt-card__field--locked{% endif %}">
                                         <label>{{ entry_option_points }}</label>
                                         <div class="option-value-prefix-group">
                                           <select name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][points_prefix]" class="form-control option-value-prefix-select"{% if product_option.is_axis %} disabled="disabled"{% endif %}>
                                             <option value="+"{% if product_option_value.points_prefix == '+' %} selected="selected"{% endif %}>+</option>
                                             <option value="-"{% if product_option_value.points_prefix == '-' %} selected="selected"{% endif %}>-</option>
                                           </select>
                                           <input type="text" name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][points]" value="{{ product_option_value.points }}" placeholder="{{ entry_points }}" class="form-control option-value-prefix-input"{% if product_option.is_axis %} readonly="readonly"{% endif %} />
                                         </div>
                                       </div>
                                       <div class="dc-opt-card__field{% if product_option.is_axis %} dc-opt-card__field--locked{% endif %}">
                                         <label>{{ entry_weight }}</label>
                                         <div class="option-value-prefix-group">
                                           <select name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][weight_prefix]" class="form-control option-value-prefix-select"{% if product_option.is_axis %} disabled="disabled"{% endif %}>
                                             <option value="+"{% if product_option_value.weight_prefix == '+' %} selected="selected"{% endif %}>+</option>
                                             <option value="-"{% if product_option_value.weight_prefix == '-' %} selected="selected"{% endif %}>-</option>
                                           </select>
                                           <input type="text" name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][weight]" value="{{ product_option_value.weight }}" placeholder="{{ entry_weight }}" class="form-control option-value-prefix-input"{% if product_option.is_axis %} readonly="readonly"{% endif %} />
                                        </div>
                                      </div>
                                    </div>
                                    {% if product_option.type == 'color' %}
                                    <div class="dc-opt-card__hit">
                                      <label class="dc-opt-card__hit-label">
                                        <input type="checkbox" name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][is_hit]" value="1"{% if product_option_value.is_hit %} checked{% endif %}{% if product_option.is_axis %} disabled="disabled"{% endif %} onchange="$(this).closest('.dc-opt-card').find('input[name$=\\'[is_hit\\']]').val(this.checked ? 1 : 0);dcUpdateHitBadge(this)"/>
                                        <i data-lucide="star" width="14" height="14"></i> {{ entry_hit }}
                                      </label>
                                    </div>
                                    {% endif %}
                                    {% if not product_option.is_axis %}
                                    <div class="dc-opt-card__cg">
                                      <button type="button" class="btn btn-default btn-xs dc-opt-card__cg-toggle" onclick="event.stopPropagation();$(this).closest('.dc-opt-card__cg').find('.dc-opt-card__cg-body').slideToggle(150);">
                                        <i data-lucide="users" width="14" height="14"></i> {{ entry_option_value_cg_price }}
                                      </button>
                                      <div class="dc-opt-card__cg-body" style="display:{% if product_option_value.customer_group_prices|length > 0 %}block{% else %}none{% endif %}">
                                        <div class="dc-cg-prices" id="option-value-cg{{ option_row }}-{{ option_value_row }}">
                                          {% set cg_row = 0 %}
                                          {% for cg_price in product_option_value.customer_group_prices %}
                                            <div class="dc-opt-card__cg-row">
                                              <select name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][customer_group_price][{{ cg_row }}][customer_group_id]" class="form-control">
                                                {% for customer_group in customer_groups %}
                                                  <option value="{{ customer_group.customer_group_id }}"{% if customer_group.customer_group_id == cg_price.customer_group_id %} selected="selected"{% endif %}>{{ customer_group.name }}</option>
                                                {% endfor %}
                                              </select>
                                              <div class="option-value-prefix-group">
                                                <select name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][customer_group_price][{{ cg_row }}][price_prefix]" class="form-control option-value-prefix-select">
                                                  <option value="+"{% if cg_price.price_prefix == '+' %} selected="selected"{% endif %}>+</option>
                                                  <option value="-"{% if cg_price.price_prefix == '-' %} selected="selected"{% endif %}>-</option>
                                                </select>
                                                <input type="text" name="product_option[{{ option_row }}][product_option_value][{{ option_value_row }}][customer_group_price][{{ cg_row }}][price]" value="{{ cg_price.price }}" placeholder="{{ entry_price }}" class="form-control option-value-prefix-input" />
                                              </div>
                                              <button type="button" onclick="$(this).closest('.dc-opt-card__cg-row').remove();" class="btn btn-danger btn-xs"><i data-lucide="circle-minus" width="14" height="14"></i></button>
                                            </div>
                                            {% set cg_row = cg_row + 1 %}
                                          {% endfor %}
                                        </div>
                                        <button type="button" class="btn btn-primary btn-xs" style="margin-top:4px;" onclick="addOptionValueCgPrice(this, '{{ option_row }}', '{{ option_value_row }}');"><i data-lucide="circle-plus" width="14" height="14"></i> {{ button_option_value_cg_price_add }}</button>
                                      </div>
                                    </div>
                                    {% endif %}
                                  </div>
                                </div>
                                {% set option_value_row = option_value_row + 1 %}
                              {% endfor %}
                            </div>
                            {% if not product_option.is_axis %}
                            <small class="form-text text-muted" style="display:block; margin-top:6px;">{{ text_drag_to_reorder }}</small>
                            <div class="dc-opt-actions">
                              <button type="button" class="btn btn-primary btn-sm" onclick="addOptionValue('{{ option_row }}');"><i data-lucide="circle-plus" width="14" height="14"></i> {{ button_option_value_add }}</button>
                              <button type="button" class="btn btn-default btn-sm" onclick="addAllOptionValues('{{ option_row }}');"><i data-lucide="square-plus" width="14" height="14"></i> {{ button_add_all_values }}</button>
                            </div>
                            {% endif %}
                            <select id="option-values{{ option_row }}" style="display: none;">
                              {% if option_values[product_option.option_id] %}
                                {% for option_value in option_values[product_option.option_id] %}
                                  <option value="{{ option_value.option_value_id }}">{{ option_value.name }}</option>
                                {% endfor %}
                              {% endif %}
                            </select>
                          {% endif %}
                        </div>
                      </div>
                      {% set option_row = option_row + 1 %}
                    {% endfor %}
                    </div>
                    <div class="dc-opt-add" style="margin-top: 15px; padding: 15px; background: var(--dc-gray-50); border-radius: var(--dc-radius-lg); border: 1px dashed var(--dc-primary-active);">
                      <h4 style="margin: 0 0 10px 0; font-size: 14px; color: var(--dc-primary-active);"><i data-lucide="circle-plus" width="16" height="16"></i> {{ button_option_add }}</h4>
                      <div class="dcx-tree-select"
                           data-url="index.php?route=catalog/option/tree&user_token={{ user_token }}"
                           data-id-key="option_id"
                           data-none-text="{{ text_none }}"
                           data-placeholder="{{ entry_option }}">
                        <div class="dcx-tree-select__input-wrap">
                          <input type="text" class="dcx-tree-select__input form-control" placeholder="{{ entry_option }}" tabindex="-1" />
                          <input type="hidden" name="option_id" value="" />
                          <span class="dcx-tree-select__toggle"><i data-lucide="chevron-down" width="16" height="16"></i></span>
                        </div>
                        <div class="dcx-tree-select__dropdown">
                          <div class="dcx-tree-select__search">
                            <input type="text" class="form-control" placeholder="{{ entry_option }}" />
                            <i data-lucide="search" width="14" height="14"></i>
                          </div>
                          <div class="dcx-tree-select__list"></div>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="dcx-panel" style="margin-top: 16px;">
                <div class="dcx-panel-head">
                  <div class="dcx-panel-head__icon"><i data-lucide="percent" width="18" height="18"></i></div>
                  <div>
                    <div class="dcx-panel-title">{{ text_panel_promo_title }} <span data-toggle="tooltip" title="{{ text_panel_promo_subtitle }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span></div>
                  </div>
                </div>
                <div class="dcx-panel-body">
                  <div class="dc-axis-info-banner" id="dc-special-note-configurable" style="display:none"><i data-lucide="info" width="14" height="14"></i> {{ help_configurable_specials }}</div>
                  <div id="promo-container">
                    {% for promo in all_promotions %}
                      {% if promo.promo_type == 'discount' %}
                    <div class="dc-promo-card" id="discount-row{{ promo.row_index }}">
                      <div class="dc-promo-card__actions">
                        <label class="dc-promo-card__toggle">
                          <input type="checkbox" name="product_discount[{{ promo.row_index }}][auto_renew]" value="1" class="auto-renew-cb" {% if promo.auto_renew %}checked{% endif %} />
                          <span>{{ entry_auto_renew }}</span>
                        </label>
                        <button type="button" onclick="removePromoCard('discount-row{{ promo.row_index }}')" class="dc-promo-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                      </div>
                      <input type="hidden" name="product_discount[{{ promo.row_index }}][date_added]" value="{{ promo.date_added }}" />
                      <span class="dc-promo-badge dc-promo-badge--discount">{{ text_promo_discount }}</span>
                      <div class="dc-promo-card__grid">
                        <div class="dc-promo-card__field">
                          <label>{{ entry_customer_group }}</label>
                          <select name="product_discount[{{ promo.row_index }}][customer_group_id]" class="form-control">
                            {% for customer_group in customer_groups %}
                              {% if customer_group.customer_group_id == promo.customer_group_id %}
                                <option value="{{ customer_group.customer_group_id }}" selected="selected">{{ customer_group.name }}</option>
                              {% else %}
                                <option value="{{ customer_group.customer_group_id }}">{{ customer_group.name }}</option>
                              {% endif %}
                            {% endfor %}
                          </select>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_quantity }}</label>
                          <input type="text" name="product_discount[{{ promo.row_index }}][quantity]" value="{{ promo.quantity }}" placeholder="{{ entry_quantity }}" class="form-control"/>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_priority }}</label>
                          <input type="text" name="product_discount[{{ promo.row_index }}][priority]" value="{{ promo.priority }}" placeholder="{{ entry_priority }}" class="form-control"/>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_price }}</label>
                          <input type="text" name="product_discount[{{ promo.row_index }}][price]" value="{{ promo.price }}" placeholder="{{ entry_price }}" class="form-control"/>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_date_start }}</label>
                          <div class="input-group date">
                            <input type="text" name="product_discount[{{ promo.row_index }}][date_start]" value="{{ promo.date_start }}" placeholder="{{ entry_date_start }}" data-date-format="YYYY-MM-DD" class="form-control"/>
                            <span class="input-group-btn"><button class="btn btn-default" type="button"><i data-lucide="calendar" width="14" height="14"></i></button></span>
                          </div>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_date_end }}</label>
                          <div class="input-group date">
                            <input type="text" name="product_discount[{{ promo.row_index }}][date_end]" value="{{ promo.date_end }}" placeholder="{{ entry_date_end }}" data-date-format="YYYY-MM-DD" class="form-control"/>
                            <span class="input-group-btn"><button class="btn btn-default" type="button"><i data-lucide="calendar" width="14" height="14"></i></button></span>
                          </div>
                        </div>
                      </div>
                    </div>
                      {% elseif promo.promo_type == 'special' %}
                    <div class="dc-promo-card" id="special-row{{ promo.row_index }}">
                      <div class="dc-promo-card__actions">
                        <label class="dc-promo-card__toggle">
                          <input type="checkbox" name="product_special[{{ promo.row_index }}][auto_renew]" value="1" class="auto-renew-cb" {% if promo.auto_renew %}checked{% endif %} />
                          <span>{{ entry_auto_renew }}</span>
                        </label>
                        <button type="button" onclick="removePromoCard('special-row{{ promo.row_index }}')" class="dc-promo-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                      </div>
                      <input type="hidden" name="product_special[{{ promo.row_index }}][date_added]" value="{{ promo.date_added }}" />
                      <span class="dc-promo-badge dc-promo-badge--special">{{ text_promo_special }}</span>
                      <div class="dc-promo-card__grid">
                        <div class="dc-promo-card__field">
                          <label>{{ entry_customer_group }}</label>
                          <select name="product_special[{{ promo.row_index }}][customer_group_id]" class="form-control">
                            {% for customer_group in customer_groups %}
                              {% if customer_group.customer_group_id == promo.customer_group_id %}
                                <option value="{{ customer_group.customer_group_id }}" selected="selected">{{ customer_group.name }}</option>
                              {% else %}
                                <option value="{{ customer_group.customer_group_id }}">{{ customer_group.name }}</option>
                              {% endif %}
                            {% endfor %}
                          </select>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_priority }}</label>
                          <input type="text" name="product_special[{{ promo.row_index }}][priority]" value="{{ promo.priority }}" placeholder="{{ entry_priority }}" class="form-control"/>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_price }}</label>
                          <input type="text" name="product_special[{{ promo.row_index }}][price]" value="{{ promo.price }}" placeholder="{{ entry_price }}" class="form-control"/>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_date_start }}</label>
                          <div class="input-group date">
                            <input type="text" name="product_special[{{ promo.row_index }}][date_start]" value="{{ promo.date_start }}" placeholder="{{ entry_date_start }}" data-date-format="YYYY-MM-DD" class="form-control"/>
                            <span class="input-group-btn"><button class="btn btn-default" type="button"><i data-lucide="calendar" width="14" height="14"></i></button></span>
                          </div>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_date_end }}</label>
                          <div class="input-group date">
                            <input type="text" name="product_special[{{ promo.row_index }}][date_end]" value="{{ promo.date_end }}" placeholder="{{ entry_date_end }}" data-date-format="YYYY-MM-DD" class="form-control"/>
                            <span class="input-group-btn"><button class="btn btn-default" type="button"><i data-lucide="calendar" width="14" height="14"></i></button></span>
                          </div>
                        </div>
                      </div>
                    </div>
                      {% elseif promo.promo_type == 'gift' %}
                    <div class="dc-promo-card" id="gift-row{{ promo.row_index }}">
                      <div class="dc-promo-card__actions">
                        <label class="dc-promo-card__toggle">
                          <input type="checkbox" name="product_gift[{{ promo.row_index }}][auto_renew]" value="1" class="auto-renew-cb" {% if promo.auto_renew %}checked{% endif %} />
                          <span>{{ entry_auto_renew }}</span>
                        </label>
                        <button type="button" onclick="removePromoCard('gift-row{{ promo.row_index }}')" class="dc-promo-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                      </div>
                      <input type="hidden" name="product_gift[{{ promo.row_index }}][date_added]" value="{{ promo.date_added }}" />
                      <span class="dc-promo-badge dc-promo-badge--gift">{{ text_promo_gift }}</span>
                      <div class="dc-promo-card__grid">
                        <div class="dc-promo-card__field dc-promo-card__field--wide">
                          <label>{{ entry_gift_product }}</label>
                          <div style="position:relative;">
                            <input type="text" name="product_gift[{{ promo.row_index }}][gift_product_name]" value="{{ promo.gift_product_name }}" placeholder="{{ entry_gift_product }}" class="form-control" autocomplete="off"/>
                            <input type="hidden" name="product_gift[{{ promo.row_index }}][gift_product_id]" value="{{ promo.gift_product_id }}"/>
                          </div>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_minimum }}</label>
                          <input type="text" name="product_gift[{{ promo.row_index }}][minimum_quantity]" value="{{ promo.minimum_quantity }}" placeholder="{{ entry_minimum }}" class="form-control"/>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_date_start }}</label>
                          <div class="input-group date">
                            <input type="text" name="product_gift[{{ promo.row_index }}][date_start]" value="{{ promo.date_start }}" placeholder="{{ entry_date_start }}" data-date-format="YYYY-MM-DD" class="form-control"/>
                            <span class="input-group-btn"><button class="btn btn-default" type="button"><i data-lucide="calendar" width="14" height="14"></i></button></span>
                          </div>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_date_end }}</label>
                          <div class="input-group date">
                            <input type="text" name="product_gift[{{ promo.row_index }}][date_end]" value="{{ promo.date_end }}" placeholder="{{ entry_date_end }}" data-date-format="YYYY-MM-DD" class="form-control"/>
                            <span class="input-group-btn"><button class="btn btn-default" type="button"><i data-lucide="calendar" width="14" height="14"></i></button></span>
                          </div>
                        </div>
                      </div>
                    </div>
                      {% elseif promo.promo_type == 'bxgy' %}
                    <div class="dc-promo-card" id="bxgy-row{{ promo.row_index }}">
                      <div class="dc-promo-card__actions">
                        <label class="dc-promo-card__toggle">
                          <input type="checkbox" name="product_bxgy[{{ promo.row_index }}][auto_renew]" value="1" class="auto-renew-cb" {% if promo.auto_renew %}checked{% endif %} />
                          <span>{{ entry_auto_renew }}</span>
                        </label>
                        <button type="button" onclick="removePromoCard('bxgy-row{{ promo.row_index }}')" class="dc-promo-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                      </div>
                      <input type="hidden" name="product_bxgy[{{ promo.row_index }}][date_added]" value="{{ promo.date_added }}" />
                      <span class="dc-promo-badge dc-promo-badge--bxgy">{{ text_promo_bxgy }}</span>
                      <div class="dc-promo-card__grid">
                        <div class="dc-promo-card__field dc-promo-card__field--wide">
                          <label>{{ entry_bxgy_reward_product }}</label>
                          <div style="position:relative;">
                            <input type="text" name="product_bxgy[{{ promo.row_index }}][reward_product_name]" value="{{ promo.reward_product_name }}" placeholder="{{ entry_bxgy_reward_product }}" class="form-control" autocomplete="off"/>
                            <input type="hidden" name="product_bxgy[{{ promo.row_index }}][reward_product_id]" value="{{ promo.reward_product_id }}"/>
                          </div>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_bxgy_trigger_quantity }}</label>
                          <input type="text" name="product_bxgy[{{ promo.row_index }}][trigger_quantity]" value="{{ promo.trigger_quantity }}" placeholder="{{ entry_bxgy_trigger_quantity }}" class="form-control"/>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_bxgy_discount_value }}</label>
                          <input type="text" name="product_bxgy[{{ promo.row_index }}][discount_value]" value="{{ promo.discount_value }}" placeholder="{{ entry_bxgy_discount_value }}" class="form-control"/>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_date_start }}</label>
                          <div class="input-group date">
                            <input type="text" name="product_bxgy[{{ promo.row_index }}][date_start]" value="{{ promo.date_start }}" placeholder="{{ entry_date_start }}" data-date-format="YYYY-MM-DD" class="form-control"/>
                            <span class="input-group-btn"><button class="btn btn-default" type="button"><i data-lucide="calendar" width="14" height="14"></i></button></span>
                          </div>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_date_end }}</label>
                          <div class="input-group date">
                            <input type="text" name="product_bxgy[{{ promo.row_index }}][date_end]" value="{{ promo.date_end }}" placeholder="{{ entry_date_end }}" data-date-format="YYYY-MM-DD" class="form-control"/>
                            <span class="input-group-btn"><button class="btn btn-default" type="button"><i data-lucide="calendar" width="14" height="14"></i></button></span>
                          </div>
                        </div>
                      </div>
                    </div>
                      {% elseif promo.promo_type == 'bundle' %}
                    <div class="dc-promo-card dc-promo-card--bundle" id="bundle-row{{ promo.row_index }}">
                      <div class="dc-promo-card__actions">
                        <label class="dc-promo-card__toggle">
                          <input type="checkbox" name="product_bundle[{{ promo.row_index }}][auto_renew]" value="1" class="auto-renew-cb" {% if promo.auto_renew %}checked{% endif %} />
                          <span>{{ entry_auto_renew }}</span>
                        </label>
                        <button type="button" onclick="removeBundleCard('bundle-row{{ promo.row_index }}', {{ promo.bundle_id }})" class="dc-promo-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>
                      </div>
                      <input type="hidden" name="product_bundle[{{ promo.row_index }}][date_added]" value="{{ promo.date_added }}" />
                      <span class="dc-promo-badge dc-promo-badge--bundle">{{ text_promo_bundle }}</span>
                      <div class="dc-promo-card__grid">
                        <div class="dc-promo-card__field" style="display:none;">
                          <input type="hidden" name="product_bundle[{{ promo.row_index }}][bundle_id]" value="{{ promo.bundle_id }}" />
                        </div>
                        <div class="dc-promo-card__field dc-promo-card__field--wide">
                          <label>{{ entry_bundle_products }}</label>
                          <div style="position:relative;">
                            <input type="text" name="product_bundle[{{ promo.row_index }}][bundle_products_text]" value="" placeholder="{{ entry_bundle_products }}" class="form-control bundle-product-input" autocomplete="off" />
                          </div>
                          <input type="hidden" name="product_bundle[{{ promo.row_index }}][bundle_product_ids]" value="{{ promo.product_ids_csv }}" />
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_discount_type }}</label>
                          <select name="product_bundle[{{ promo.row_index }}][discount_type]" class="form-control">
                            <option value="percentage" {% if promo.discount_type == 'percentage' %}selected{% endif %}>{{ text_percentage }}</option>
                            <option value="fixed" {% if promo.discount_type == 'fixed' %}selected{% endif %}>{{ text_fixed }}</option>
                          </select>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_discount_value }}</label>
                          <input type="text" name="product_bundle[{{ promo.row_index }}][discount_value]" value="{{ promo.discount_value }}" placeholder="{{ entry_discount_value }}" class="form-control" />
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_date_start }}</label>
                          <div class="input-group date"><input type="text" name="product_bundle[{{ promo.row_index }}][date_start]" value="{{ promo.date_start }}" placeholder="{{ entry_date_start }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>
                        </div>
                        <div class="dc-promo-card__field">
                          <label>{{ entry_date_end }}</label>
                          <div class="input-group date"><input type="text" name="product_bundle[{{ promo.row_index }}][date_end]" value="{{ promo.date_end }}" placeholder="{{ entry_date_end }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>
                        </div>
                      </div>
                      <label style="font-size:12px;font-weight:500;color:var(--dc-text-muted);">{{ text_bundle_selected_products }}</label>
                      <div class="bundle-selected-products" id="bundle-selected-{{ promo.row_index }}">
                        {% for bp in promo.products %}
                        <span class="dc-chip" data-product-id="{{ bp.product_id }}">{{ bp.name|escape }} ({{ bp.price }}) <button type="button" onclick="removeBundleProduct(this, {{ bp.product_id }})" class="dc-chip__remove"><i data-lucide="x" width="12" height="12"></i></button></span>
                        {% endfor %}
                      </div>
                    </div>
                      {% endif %}
                    {% endfor %}

                    {% if all_promotions is empty %}
                    <div class="dc-promo-empty" id="promo-empty"><i data-lucide="tag" width="32" height="32" style="opacity:0.25;margin-bottom:6px;"></i><div>{{ text_no_promo }}</div></div>
                    {% endif %}
                  </div>
                  <div style="position:relative; display:inline-block;">
                    <button type="button" class="dc-promo-add-btn" id="promo-add-btn">
                      <i data-lucide="plus" width="14" height="14"></i> {{ button_promo_add }} <i data-lucide="chevron-down" width="12" height="12"></i>
                    </button>
                    <div id="promo-add-dropdown" style="display:none;position:absolute;top:100%;left:0;margin-top:4px;background:#fff;border:1px solid var(--dc-border);border-radius:var(--dc-radius-md);box-shadow:var(--dc-shadow-lg);z-index:1050;min-width:220px;">
                      <div style="padding:6px 0;">
                        <a href="javascript:void(0)" onclick="addPromoCard('discount');$('#promo-add-dropdown').hide();" style="display:block;padding:8px 14px;font-size:13px;color:var(--dc-text-dark);text-decoration:none;"><span class="dc-promo-badge dc-promo-badge--discount" style="margin-right:6px;">{{ text_promo_discount }}</span></a>
                        <a href="javascript:void(0)" onclick="addPromoCard('special');$('#promo-add-dropdown').hide();" style="display:block;padding:8px 14px;font-size:13px;color:var(--dc-text-dark);text-decoration:none;"><span class="dc-promo-badge dc-promo-badge--special" style="margin-right:6px;">{{ text_promo_special }}</span></a>
                        <a href="javascript:void(0)" onclick="addPromoCard('gift');$('#promo-add-dropdown').hide();" style="display:block;padding:8px 14px;font-size:13px;color:var(--dc-text-dark);text-decoration:none;"><span class="dc-promo-badge dc-promo-badge--gift" style="margin-right:6px;">{{ text_promo_gift }}</span></a>
                        <a href="javascript:void(0)" onclick="addPromoCard('bxgy');$('#promo-add-dropdown').hide();" style="display:block;padding:8px 14px;font-size:13px;color:var(--dc-text-dark);text-decoration:none;"><span class="dc-promo-badge dc-promo-badge--bxgy" style="margin-right:6px;">{{ text_promo_bxgy }}</span></a>
                        <a href="javascript:void(0)" onclick="addPromoCard('bundle');$('#promo-add-dropdown').hide();" style="display:block;padding:8px 14px;font-size:13px;color:var(--dc-text-dark);text-decoration:none;"><span class="dc-promo-badge dc-promo-badge--bundle" style="margin-right:6px;">{{ text_promo_bundle }}</span></a>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
              <div class="dcx-panel" style="margin-top: 16px;">
                <div class="dcx-panel-head">
                  <div class="dcx-panel-head__icon"><i data-lucide="trophy" width="18" height="18"></i></div>
                  <div>
                    <div class="dcx-panel-title">{{ text_panel_rewards_title }} <span data-toggle="tooltip" title="{{ text_panel_rewards_subtitle }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span></div>
                  </div>
                </div>
                <div class="dcx-panel-body">
                  <div class="dcx-fields">
                    <div class="dcx-field">
                      <label class="dcx-label" for="input-points">{{ entry_points }}</label>
                      <div class="dcx-field-icon-wrap" style="max-width:100px;">
                        <input type="text" name="points" value="{{ points }}" placeholder="{{ entry_points }}" id="input-points" class="form-control"/>
                        <span class="dcx-field-icon" data-toggle="tooltip" title="{{ help_points }}"><i data-lucide="circle-question-mark" width="16" height="16" class="text-muted"></i></span>
                      </div>
                    </div>
                  </div>
                  <div id="rewards-container" class="table-responsive">
                    <table class="table table-bordered dcx-table dcx-table--reward">
                      <thead>
                        <tr>
                          <td class="text-left">{{ entry_customer_group }}</td>
                          <td class="text-left" style="width: 200px;">{{ entry_reward }}</td>
                        </tr>
                      </thead>
                      <tbody>
                        {% for customer_group in customer_groups %}
                        <tr>
                          <td class="text-left">{{ customer_group.name }}</td>
                          <td class="text-left">
                            <input type="text" name="product_reward[{{ customer_group.customer_group_id }}][points]" value="{{ product_reward[customer_group.customer_group_id] ? product_reward[customer_group.customer_group_id].points }}" placeholder="{{ entry_reward }}" class="form-control"/>
                          </td>
                        </tr>
                        {% endfor %}
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
        </div>
      </div>

      <div class="col-md-12 col-sm-12 col-lg-4 col-xl-3">
        <div class="dcx-sidebar-card">
          <div class="dcx-sidebar-card__header">
            <div class="dcx-sidebar-card__header-icon"><i data-lucide="toggle-right" width="16" height="16"></i></div>
            <h3 class="dcx-sidebar-card__title">{{ text_status_card }}</h3>
          </div>
          <div class="dcx-sidebar-card__body">
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label" for="input-status">{{ entry_status }}</label>
              <select name="status" id="input-status" class="form-control">
                {% if status %}
                <option value="1" selected="selected">{{ text_enabled }}</option>
                <option value="0">{{ text_disabled }}</option>
                {% else %}
                <option value="1">{{ text_enabled }}</option>
                <option value="0" selected="selected">{{ text_disabled }}</option>
                {% endif %}
              </select>
            </div>
            <div class="dcx-sidebar-card__field">
              <div class="dcx-checkbox-group">
                <label class="dcx-checkbox-label">
                  <input type="checkbox" name="discontinued" value="1" id="input-discontinued"{% if discontinued %} checked{% endif %} /> {{ entry_discontinued }}
                  <span data-toggle="tooltip" title="{{ help_discontinued }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span>
                </label>
              </div>
            </div>
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label" for="input-sort-order">{{ entry_sort_order }}</label>
              <input type="text" name="sort_order" value="{{ sort_order }}" placeholder="{{ entry_sort_order }}" id="input-sort-order" class="form-control"/>
            </div>
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label" for="input-date-available">{{ entry_date_available }}</label>
              <div class="input-group date">
                <input type="text" name="date_available" value="{{ date_available }}" placeholder="{{ entry_date_available }}" data-date-format="YYYY-MM-DD" id="input-date-available" class="form-control"/>
                <span class="input-group-btn">
                  <button class="btn btn-default" type="button"><i data-lucide="calendar" width="14" height="14"></i></button>
                </span>
              </div>
            </div>
          </div>
        </div>
        <div class="dcx-sidebar-card">
          <div class="dcx-sidebar-card__header">
            <div class="dcx-sidebar-card__header-icon"><i data-lucide="folder-tree" width="16" height="16"></i></div>
            <h3 class="dcx-sidebar-card__title">{{ text_organization_card }}</h3>
          </div>
          <div class="dcx-sidebar-card__body">
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label" for="input-manufacturer">{{ entry_manufacturer }} <span data-toggle="tooltip" title="{{ help_manufacturer }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span></label>
              <div class="dcx-field-icon-wrap">
                <span class="dcx-field-icon"><i data-lucide="search" width="14" height="14" class="text-muted"></i></span>
                <input type="text" name="manufacturer" value="{{ manufacturer|e('html_attr') }}" placeholder="{{ entry_manufacturer }}" id="input-manufacturer" class="form-control"/>
                <input type="hidden" name="manufacturer_id" value="{{ manufacturer_id }}"/>
              </div>
            </div>
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label">{{ entry_store }}</label>
              <div class="dcx-sidebar-card__stores">
                {% for store in stores %}
                <label class="dcx-sidebar-card__store-item">
                  <input type="checkbox" name="product_store[]" value="{{ store.store_id }}" {% if store.store_id in product_store %}checked="checked"{% endif %} />
                  <span>{{ store.name }}</span>
                </label>
                {% endfor %}
              </div>
            </div>
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label">{{ entry_main_category }} <span data-toggle="tooltip" title="{{ help_main_category }}"><i data-lucide="circle-question-mark" width="14" height="14" style="cursor:help;color:var(--dc-text-muted);"></i></span></label>
              <div class="dcx-tree-select"
                   data-url="index.php?route=catalog/category/tree&user_token={{ user_token }}"
                   data-none-text="{{ text_none }}"
                   data-placeholder="{{ text_quick_find }}">
                <div class="dcx-tree-select__input-wrap">
                  <input type="text" name="main_category_path" value="{{ main_category_name }}" placeholder="{{ entry_main_category }}" class="dcx-tree-select__input form-control" autocomplete="off" />
                  <input type="hidden" name="main_category_id" value="{{ main_category_id }}" />
                  <span class="dcx-tree-select__toggle"><i data-lucide="chevron-down" width="16" height="16"></i></span>
                </div>
                <div class="dcx-tree-select__dropdown">
                  <div class="dcx-tree-select__search">
                    <input type="text" class="form-control" placeholder="{{ text_quick_find }}" />
                    <i data-lucide="search" width="14" height="14"></i>
                  </div>
                  <div class="dcx-tree-select__list">
                    <div class="dcx-tree-select__item dcx-tree-select__item--none" data-id="0" data-name="{{ text_none }}">
                      <span class="dcx-tree-select__label">{{ text_none }}</span>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label">{{ entry_categories_display }} <span data-toggle="tooltip" title="{{ help_category }}"><i data-lucide="circle-question-mark" width="14" height="14" style="cursor:help;color:var(--dc-text-muted);"></i></span></label>
              <button type="button" class="btn btn-default btn-sm btn-block" id="btn-open-category-modal" style="text-align:left;">
                <i data-lucide="folder-tree" width="14" height="14" style="margin-right:6px;"></i>
                <span id="category-modal-count">{% if product_categories|length > 0 %}{{ product_categories|length }} {{ button_select_category }}{% else %}{{ button_select_category }}{% endif %}</span>
              </button>
              <div id="product-category" class="dcx-tree-select__well" style="height: 150px; overflow: auto; margin-top: 6px;"> {% for product_category in product_categories %}
                  <div class="dcx-tree-select__well-item" data-id="{{ product_category.category_id }}"><i data-lucide="circle-minus" width="14" height="14" class="dcx-tree-select__well-remove"></i> <span class="dcx-tree-select__well-label">{{ product_category.name }}</span>
                    <input type="hidden" name="product_category[]" value="{{ product_category.category_id }}"/>
                  </div>
                {% endfor %}</div>
            </div>
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label" for="input-related">{{ entry_related }} <span data-toggle="tooltip" title="{{ help_related }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span></label>
              <div class="dcx-picker"
                   data-url="index.php?route=catalog/product/autocomplete&user_token={{ user_token }}"
                   data-well="#product-related"
                   data-input-name="product_related[]"
                   data-placeholder="{{ entry_related }}"
                   data-id-key="product_id"
                   data-name-key="name"
                   data-sub-key="model"
                   data-min-chars="0"
                   data-text-type-to-search="{{ text_picker_type_to_search }}"
                   data-text-no-results="{{ text_picker_no_results }}"
                   data-text-error="{{ text_picker_error }}">
                <input type="text" class="dcx-picker__input form-control" placeholder="{{ entry_related }}" />
              </div>
              <div id="product-related" class="dcx-picker__well" style="height: 150px; overflow: auto;"> {% for product_related in product_relateds %}
                  <div class="dcx-picker__well-item" data-id="{{ product_related.product_id }}"><i data-lucide="circle-minus" width="14" height="14" class="dcx-picker__well-remove"></i> <span class="dcx-picker__well-label">{{ product_related.name }}</span>
                    <input type="hidden" name="product_related[]" value="{{ product_related.product_id }}"/>
                  </div>
                {% endfor %}</div>
            </div>
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label" for="input-upsell">{{ entry_upsell }} <span data-toggle="tooltip" title="{{ help_upsell }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span></label>
              <div class="dcx-picker"
                   data-url="index.php?route=catalog/product/autocomplete&user_token={{ user_token }}"
                   data-well="#product-upsell"
                   data-input-name="product_upsell[]"
                   data-placeholder="{{ entry_upsell }}"
                   data-id-key="product_id"
                   data-name-key="name"
                   data-sub-key="model"
                   data-min-chars="0"
                   data-text-type-to-search="{{ text_picker_type_to_search }}"
                   data-text-no-results="{{ text_picker_no_results }}"
                   data-text-error="{{ text_picker_error }}">
                <input type="text" class="dcx-picker__input form-control" placeholder="{{ entry_upsell }}" />
              </div>
              <div id="product-upsell" class="dcx-picker__well" style="height: 150px; overflow: auto;"> {% for product_upsell in product_upsells %}
                  <div class="dcx-picker__well-item" data-id="{{ product_upsell.product_id }}"><i data-lucide="circle-minus" width="14" height="14" class="dcx-picker__well-remove"></i> <span class="dcx-picker__well-label">{{ product_upsell.name }}</span>
                    <input type="hidden" name="product_upsell[]" value="{{ product_upsell.product_id }}"/>
                  </div>
                {% endfor %}</div>
            </div>
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label" for="input-accessory">{{ entry_accessory }} <span data-toggle="tooltip" title="{{ help_accessory }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span></label>
              <div class="dcx-picker"
                   data-url="index.php?route=catalog/product/autocomplete&user_token={{ user_token }}"
                   data-well="#product-accessory"
                   data-input-name="product_accessory[]"
                   data-placeholder="{{ entry_accessory }}"
                   data-id-key="product_id"
                   data-name-key="name"
                   data-sub-key="model"
                   data-min-chars="0"
                   data-text-type-to-search="{{ text_picker_type_to_search }}"
                   data-text-no-results="{{ text_picker_no_results }}"
                   data-text-error="{{ text_picker_error }}">
                <input type="text" class="dcx-picker__input form-control" placeholder="{{ entry_accessory }}" />
              </div>
              <div id="product-accessory" class="dcx-picker__well" style="height: 150px; overflow: auto;"> {% for product_accessory in product_accessories %}
                  <div class="dcx-picker__well-item" data-id="{{ product_accessory.product_id }}"><i data-lucide="circle-minus" width="14" height="14" class="dcx-picker__well-remove"></i> <span class="dcx-picker__well-label">{{ product_accessory.name }}</span>
                    <input type="hidden" name="product_accessory[]" value="{{ product_accessory.product_id }}"/>
                  </div>
                {% endfor %}</div>
            </div>
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label" for="input-fbt">{{ entry_fbt }} <span data-toggle="tooltip" title="{{ help_fbt }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span></label>
              <div class="dcx-picker"
                   data-url="index.php?route=catalog/product/autocomplete&user_token={{ user_token }}"
                   data-well="#product-fbt"
                   data-input-name="product_fbt[]"
                   data-placeholder="{{ entry_fbt }}"
                   data-id-key="product_id"
                   data-name-key="name"
                   data-sub-key="model"
                   data-min-chars="0"
                   data-text-type-to-search="{{ text_picker_type_to_search }}"
                   data-text-no-results="{{ text_picker_no_results }}"
                   data-text-error="{{ text_picker_error }}">
                <input type="text" class="dcx-picker__input form-control" placeholder="{{ entry_fbt }}" />
              </div>
              <div id="product-fbt" class="dcx-picker__well" style="height: 150px; overflow: auto;"> {% for product_fbt_item in product_fbt %}
                  <div class="dcx-picker__well-item" data-id="{{ product_fbt_item.product_id }}"><i data-lucide="circle-minus" width="14" height="14" class="dcx-picker__well-remove"></i> <span class="dcx-picker__well-label">{{ product_fbt_item.name }}</span>
                    <input type="hidden" name="product_fbt[]" value="{{ product_fbt_item.product_id }}"/>
                  </div>
                {% endfor %}</div>
            </div>
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label" for="input-similar">{{ entry_similar }} <span data-toggle="tooltip" title="{{ help_similar }}"><i data-lucide="circle-question-mark" width="14" height="14" class="text-muted"></i></span></label>
              <div class="dcx-picker"
                   data-url="index.php?route=catalog/product/autocomplete&user_token={{ user_token }}"
                   data-well="#product-similar"
                   data-input-name="product_similar[]"
                   data-placeholder="{{ entry_similar }}"
                   data-id-key="product_id"
                   data-name-key="name"
                   data-sub-key="model"
                   data-min-chars="0"
                   data-text-type-to-search="{{ text_picker_type_to_search }}"
                   data-text-no-results="{{ text_picker_no_results }}"
                   data-text-error="{{ text_picker_error }}">
                <input type="text" class="dcx-picker__input form-control" placeholder="{{ entry_similar }}" />
              </div>
              <div id="product-similar" class="dcx-picker__well" style="height: 150px; overflow: auto;"> {% for product_similar in product_similars %}
                  <div class="dcx-picker__well-item" data-id="{{ product_similar.product_id }}"><i data-lucide="circle-minus" width="14" height="14" class="dcx-picker__well-remove"></i> <span class="dcx-picker__well-label">{{ product_similar.name }}</span>
                    <input type="hidden" name="product_similar[]" value="{{ product_similar.product_id }}"/>
                  </div>
                {% endfor %}</div>
            </div>
            {% if product_downloads|length > 0 %}
            <div class="dcx-sidebar-card__field">
              <label class="dcx-sidebar-card__label" for="input-download"><span data-toggle="tooltip" title="{{ help_download }}">{{ entry_download }}</span></label>
              <div class="dcx-tree-select"
                   data-url="index.php?route=catalog/download/autocomplete&user_token={{ user_token }}"
                   data-mode="multi"
                   data-flat="true"
                   data-well="#product-download"
                   data-input-name="product_download[]"
                   data-id-key="download_id"
                   data-name-key="name"
                   data-placeholder="{{ entry_download }}">
                <div class="dcx-tree-select__input-wrap">
                  <input type="text" class="dcx-tree-select__input form-control" placeholder="{{ entry_download }}" />
                  <span class="dcx-tree-select__toggle"><i data-lucide="chevron-down" width="16" height="16"></i></span>
                </div>
                <div class="dcx-tree-select__dropdown">
                  <div class="dcx-tree-select__search">
                    <input type="text" class="form-control" placeholder="{{ entry_download }}" />
                    <i data-lucide="search" width="14" height="14"></i>
                  </div>
                  <div class="dcx-tree-select__list"></div>
                </div>
              </div>
              <div id="product-download" class="dcx-tree-select__well" style="height: 150px; overflow: auto;"> {% for product_download in product_downloads %}
                  <div class="dcx-tree-select__well-item" data-id="{{ product_download.download_id }}"><i data-lucide="circle-minus" width="14" height="14" class="dcx-tree-select__well-remove"></i> <span class="dcx-tree-select__well-label">{{ product_download.name }}</span>
                    <input type="hidden" name="product_download[]" value="{{ product_download.download_id }}"/>
                  </div>
                {% endfor %}</div>
            </div>
            {% endif %}
          </div>
        </div>
        <div class="dcx-sidebar-card">
          <div class="dcx-sidebar-card__header">
            <div class="dcx-sidebar-card__header-icon"><i data-lucide="link" width="16" height="16"></i></div>
            <h3 class="dcx-sidebar-card__title">{{ text_seo_card }}</h3>
          </div>
          <div class="dcx-sidebar-card__body">
            <div class="dcx-sidebar-card__help"><i data-lucide="info" width="16" height="16"></i> {{ text_keyword }}</div>
            <div class="dcx-sidebar-card__links">
              {% for store in stores %}
                {% if stores|length > 1 %}
                <div class="dcx-sidebar-card__store-label">{{ store.name }}</div>
                {% endif %}
                {% for language in languages %}
                <div class="dcx-sidebar-card__link-item">
                  <label class="dcx-sidebar-card__link-label">
                    <span class="lang-code">{{ language.code|split('-')|first|upper }}</span>
                    {{ language.name }}
                  </label>
                  <input type="text" name="product_seo_url[{{ store.store_id }}][{{ language.language_id }}]" value="{% if product_seo_url[store.store_id][language.language_id] %}{{ product_seo_url[store.store_id][language.language_id] }}{% endif %}" placeholder="{{ entry_keyword }}" class="form-control" />
                  {% if error_keyword[store.store_id][language.language_id] %}
                  <div class="text-danger">{{ error_keyword[store.store_id][language.language_id] }}</div>
                  {% endif %}
                </div>
                {% endfor %}
              {% endfor %}
            </div>
            <div class="dcx-seo-preview">
              <span class="dcx-seo-preview__label">{{ text_seo_preview }}</span>
              <span class="dcx-seo-preview__url">yoursite.com/product/</span><span id="seo-url-preview">...</span>
            </div>
          </div>
        </div>
        <div class="dcx-sidebar-card">
          <div class="dcx-sidebar-card__header">
            <div class="dcx-sidebar-card__header-icon"><i data-lucide="columns-2" width="16" height="16"></i></div>
            <h3 class="dcx-sidebar-card__title">{{ text_design_card }}</h3>
          </div>
          <div class="dcx-sidebar-card__body">
            <div class="dcx-sidebar-card__help"><i data-lucide="info" width="16" height="16"></i> {{ text_layout_info }}</div>
            <div class="dcx-sidebar-card__layouts">
              {% for store in stores %}
              <div class="dcx-sidebar-card__layout-item">
                <label class="dcx-sidebar-card__layout-label">{{ store.name }}</label>
                <select name="product_layout[{{ store.store_id }}]" class="form-control">
                  <option value=""></option>
                  {% for layout in layouts %}
                  {% if product_layout[store.store_id] and product_layout[store.store_id] == layout.layout_id %}
                  <option value="{{ layout.layout_id }}" selected="selected">{{ layout.name }}</option>
                  {% else %}
                  <option value="{{ layout.layout_id }}">{{ layout.name }}</option>
                  {% endif %}
                  {% endfor %}
                </select>
              </div>
              {% endfor %}
            </div>
          </div>
        </div>
      </div>
    </div>
    </form>
  </div>

</div>

<div id="category-tree-modal" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title"><i data-lucide="folder-tree" width="18" height="18" style="margin-right:8px;"></i>{{ text_select_category }}</h4>
      </div>
      <div class="modal-body">
        <div id="category-modal-tree" class="dcx-tree-select"
             data-url="index.php?route=catalog/category/tree&user_token={{ user_token }}"
             data-mode="multi"
             data-none-text="{{ text_none }}"
             data-placeholder="{{ text_quick_find }}">
          <div class="dcx-tree-select__search">
            <input type="text" class="form-control" placeholder="{{ text_quick_find }}" id="category-tree-search" />
            <i data-lucide="search" width="14" height="14"></i>
          </div>
          <div class="dcx-tree-select__list" style="max-height:400px;overflow-y:auto;"></div>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">{{ button_cancel }}</button>
        <button type="button" class="btn btn-primary" id="category-tree-confirm"><i data-lucide="check" width="14" height="14"></i> {{ button_continue }}</button>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript" src="view/javascript/tinymce/dc.js"></script>
<script src="view/javascript/sortablejs/Sortable.min.js"></script>
<script type="text/javascript"><!--
  // Manufacturer
  $('input[name=\'manufacturer\']').autocomplete({
	  'source': function(request, response) {
		  $.ajax({
			  url: 'index.php?route=catalog/manufacturer/autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
			  dataType: 'json',
			  success: function(json) {
				  response($.map(json, function(item) {
					  return {
						  label: item['name'],
						  value: item['manufacturer_id']
					  }
				  }));
			  }
		  });
	  },
	  'select': function(item) {
		  $('input[name=\'manufacturer\']').val(item['label']);
		  $('input[name=\'manufacturer_id\']').val(item['value']);
	  }
  });

  var attribute_row = {{ attribute_row }};

  function addAttribute() {
	  var html = '<div class="dc-attr-row" id="attribute-row' + attribute_row + '">';
	  html += '  <div class="dc-attr-row__name">';
	  html += '      <input type="text" name="product_attribute[' + attribute_row + '][name]" value="" placeholder="{{ entry_attribute }}" class="form-control"/>';
	  html += '      <input type="hidden" name="product_attribute[' + attribute_row + '][attribute_id]" value=""/>';
	  html += '  </div>';
	  html += '  <div class="dc-attr-row__values">';
    {% for language in languages %}
	  html += '    <div class="dc-attr-row__value">';
	  html += '      <span class="lang-code">{{ language.code|split('-')|first|upper }}</span>';
	  html += '      <input type="text" name="product_attribute[' + attribute_row + '][product_attribute_description][{{ language.language_id }}][text]" value="" placeholder="{{ entry_text }}" class="form-control"/>';
	  html += '    </div>';
    {% endfor %}
	  html += '  </div>';
	  html += '  <button type="button" onclick="$(\'#attribute-row' + attribute_row + '\').remove();" class="btn btn-link btn-remove-attr" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>';
	  html += '</div>';

	  $('#attributes-container').append(html);
	  lucide.createIcons();

	  attributeautocomplete(attribute_row);

	  attribute_row++;
  }

  function attributeautocomplete(attribute_row) {
	  $('input[name=\'product_attribute[' + attribute_row + '][name]\']').autocomplete({
		  'source': function(request, response) {
			  $.ajax({
				  url: 'index.php?route=catalog/attribute/autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
				  dataType: 'json',
				  success: function(json) {
					  response($.map(json, function(item) {
						  return {
							  category: item.attribute_group,
							  label: item.name,
							  value: item.attribute_id
						  }
					  }));
				  }
			  });
		  },
		  'select': function(item) {
			  $('input[name=\'product_attribute[' + attribute_row + '][name]\']').val(item['label']);
			  $('input[name=\'product_attribute[' + attribute_row + '][attribute_id]\']').val(item['value']);
		  }
	  });
  }

  $('#attributes-container .dc-attr-row').each(function(index, element) {
	  attributeautocomplete(index);
  });

  function loadAttributeSet() {
	  var set_id = $('#dc-attribute-set-select').val();
	  if (!set_id) return;

	  var $existing = $('#attributes-container .dc-attr-row');
	  if ($existing.length && !confirm('{{ text_attribute_set_replace_confirm|e("js") }}')) {
		  return;
	  }

	  $.ajax({
		  url: 'index.php?route=catalog/attribute_set/getAttributes&user_token={{ user_token }}&attribute_set_id=' + set_id,
		  dataType: 'json',
		  success: function(json) {
			  if (json['error']) {
				  if (typeof showToast === 'function') showToast(json['error'], 'error');
				  return;
			  }

			  if (!json.length) {
				  if (typeof showToast === 'function') showToast('{{ text_attribute_set_empty|e("js") }}', 'error');
				  return;
			  }

			  $('#attributes-container .dc-attr-row').remove();

			  var added = 0;

			  $.each(json, function(i, item) {
				  if (!item || !item.attribute_id) return;

				  addAttribute();
				  var row = attribute_row - 1;
				  $('input[name=\'product_attribute[' + row + '][name]\']').val(item['name']);
				  $('input[name=\'product_attribute[' + row + '][attribute_id]\']').val(item['attribute_id']);
				  added++;
			  });

			  if (typeof showToast === 'function') {
				  showToast('{{ text_attribute_set_loaded|e("js") }}'.replace('%d', added), 'success');
			  }
			  lucide.createIcons();
		  }
	  });
  }

  var option_row = {{ option_row }};
  var dcOptionMode = {{ is_configurable ? "'combined'" : "'simple'" }};
  var dcAxisOptionIds = {{ axis_option_ids|json_encode|raw }};

  function dcUpdateSpecialNote() {
    var notes = [
      '#dc-note-configurable-identifiers',
      '#dc-note-configurable-pricing',
      '#dc-note-configurable-inventory',
      '#dc-note-configurable-dimensions',
      '#dc-special-note-configurable'
    ];
    var show = (dcOptionMode === 'combined');
    $.each(notes, function(i, selector) {
      $(selector).toggle(show);
    });
  }

  $(document).ready(function() {
    if (dcOptionMode === 'combined') {
      loadVariantMatrix();
    }

    dcUpdateSpecialNote();

    $('#dc-toggle-mode-btn').on('click', function() {
      var newMode = (dcOptionMode === 'simple') ? 'combined' : 'simple';

      if (newMode === 'simple') {
        if (!confirm('{{ text_confirm_switch_to_simple|e("js") }}')) {
          return;
        }
      }

      var productId = getDcProductId();

      if (!productId || productId == '0') {
        alert('{{ text_save_product_first|e("js") }}');
        return;
      }

      $.ajax({
        url: 'index.php?route=catalog/product_configurable/setMode&user_token={{ user_token }}',
        type: 'POST',
        data: {product_id: productId, mode: newMode},
        dataType: 'json',
        success: function(json) {
          if (json.error) {
            alert(json.error);
            return;
          }

          dcOptionMode = newMode;

          var $simpleOptions = $('.dc-option-mode-selector').closest('.dcx-panel-body').find('.row:first');
          var $axes = $('.dc-option-axis', $simpleOptions);

          if (newMode === 'simple') {
            $simpleOptions.removeClass('dc-opts-axis-hidden');
            $axes.find(':input').prop('disabled', false);
            $('#dc-variants-section').hide();
          } else {
            $simpleOptions.addClass('dc-opts-axis-hidden');
            $axes.find(':input').prop('disabled', true);
            $('#dc-variants-section').show();
            loadVariantMatrix();
          }

          $('#dc-mode-saved').fadeIn(200).delay(1500).fadeOut(400);

          var newLabel = newMode === 'combined' ? '{{ text_mode_combined }}' : '{{ text_mode_simple }}';
          var newBtnHtml = newMode === 'combined'
            ? '<i data-lucide="arrow-left" width="14" height="14"></i> {{ text_switch_to_simple }}'
            : '{{ text_switch_to_combined }} <i data-lucide="arrow-right" width="14" height="14"></i>';
          var newSpanClass = newMode === 'combined' ? 'label-primary' : 'label-default';

          $('#dc-toggle-mode-btn').html(newBtnHtml);
          $('#dc-option-mode-label').removeClass('label-primary label-default').addClass(newSpanClass).html('<i data-lucide="circle" width="10" height="10"></i> ' + newLabel);
          lucide.createIcons();
          dcUpdateSpecialNote();
        }
      });
    });

    $('#form-product').on('submit', dcValidateAxisPrices);

    var $optTree = $('.dc-opt-add .dcx-tree-select');
    if ($optTree.length) {
      var setOptFilter = function() {
        var inst = $optTree.data('dcx-tree-select');
        if (!inst || inst.filterCallback) return;
        inst.filterCallback = function(data) {
          var existingIds = [];
          $('#dc-options-container input[name$="[option_id]"]').each(function() {
            var val = parseInt($(this).val());
            if (val) existingIds.push(val);
          });
          return data.filter(function(item) {
            return existingIds.indexOf(parseInt(item.category_id)) === -1;
          });
        };
      };
      setTimeout(setOptFilter, 100);
      $(document).one('click', '.dc-opt-add .dcx-tree-select', function() {
        setOptFilter();
      });
    }

    $('[data-toggle="tooltip"]').tooltip({ container: 'body', html: true });

    $('#product-category').toggle($('#product-category .dcx-tree-select__well-item').length > 0);
    $('#product-download').toggle($('#product-download .dcx-tree-select__well-item').length > 0);
    $('#product-related').toggle($('#product-related .dcx-picker__well-item').length > 0);
    $('#product-upsell').toggle($('#product-upsell .dcx-picker__well-item').length > 0);
    $('#product-accessory').toggle($('#product-accessory .dcx-picker__well-item').length > 0);
    $('#product-fbt').toggle($('#product-fbt .dcx-picker__well-item').length > 0);
    $('#product-similar').toggle($('#product-similar .dcx-picker__well-item').length > 0);
  });

  function dcValidateAxisPrices() {
    if (dcOptionMode !== 'combined') return true;

    $('.dc-option-axis [data-axis-price="1"]').each(function() {
      if (parseFloat($(this).val()) > 0) {
        $(this).val(0);
      }
    });

    return true;
  }

  function dcRemoveOption(option_row) {
	  var $section = $('#option-section' + option_row);
	  if ($section.hasClass('dc-option-axis')) {
		  showToast('{{ error_simple_option_is_axis|e("js") }}', 'error');
		  return;
	  }
	  $section.remove();
	  updateOptionCount();
  }

  function updateOptionCount() {
	  var count = $('#dc-options-container .dc-option-section').length;
	  $('#dc-option-count').text('(' + count + ')');
  }

  $(document).on('change', '.dc-opt-add .dcx-tree-select input[type="hidden"]', function() {
	  var optionId = parseInt($(this).val());
	  if (!optionId || optionId <= 0) return;

	  var $treeSelect = $(this).closest('.dcx-tree-select');

	  if (dcAxisOptionIds.indexOf(optionId) >= 0) {
		  showToast('{{ error_simple_option_is_axis|e("js") }}', 'error');
		  dcResetTreeSelect($treeSelect);
		  return;
	  }

	  var existingIds = [];
	  $('#dc-options-container input[name$="[option_id]"]').each(function() {
		  var val = parseInt($(this).val());
		  if (val) existingIds.push(val);
	  });

	  if (existingIds.indexOf(optionId) >= 0) {
		  showToast('{{ error_option_already_added|e("js") }}', 'error');
		  dcResetTreeSelect($treeSelect);
		  return;
	  }

	  var optionName = $treeSelect.find('.dcx-tree-select__input').val();

	  $.ajax({
		  url: 'index.php?route=catalog/option/autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(optionName),
		  dataType: 'json',
		  success: function(json) {
			  var matched = null;
			  for (var i = 0; i < json.length; i++) {
				  if (parseInt(json[i].option_id) === optionId) {
					  matched = json[i];
					  break;
				  }
			  }
			  if (!matched) return;

			  var item = {
				  label: matched.name,
				  value: matched.option_id,
				  type: matched.type,
				  option_value: matched.option_value
			  };

			  dcAddOption(item);
			  dcResetTreeSelect($treeSelect);
		  }
	  });
  });

  function dcResetTreeSelect($treeSelect) {
	  $treeSelect.find('input[type="hidden"]').val('');
	  $treeSelect.find('.dcx-tree-select__input').val('');
  }

  function dcAddOption(item) {
	  var html = '';
	  html += '<div class="dc-option-section" id="option-section' + option_row + '">';
	  html += '  <div class="dc-option-section__header">';
	  html += '    <span class="dc-option-section__title">' + item['label'] + '</span>';
	  html += '    <span class="dc-option-section__type">(' + item['type'] + ')</span>';
	  html += '    <div class="dc-option-section__controls">';
	  html += '      <label class="dc-option-section__required">';
	  html += '        <span>{{ entry_required|e('js') }}:</span>';
	  html += '        <select name="product_option[' + option_row + '][required]" class="form-control input-sm">';
	  html += '          <option value="1">{{ text_yes|e('js') }}</option>';
	  html += '          <option value="0">{{ text_no|e('js') }}</option>';
	  html += '        </select>';
	  html += '      </label>';
	  html += '      <button type="button" class="btn btn-danger btn-sm" onclick="dcRemoveOption(\'' + option_row + '\');">';
	  html += '        <i data-lucide="trash-2" width="14" height="14"></i> {{ button_remove|e('js') }}';
	  html += '      </button>';
	  html += '    </div>';
	  html += '  </div>';
	  html += '  <div class="dc-option-section__body">';
	  html += '    <input type="hidden" name="product_option[' + option_row + '][product_option_id]" value="" />';
	  html += '    <input type="hidden" name="product_option[' + option_row + '][name]" value="' + item['label'] + '" />';
	  html += '    <input type="hidden" name="product_option[' + option_row + '][option_id]" value="' + item['value'] + '" />';
	  html += '    <input type="hidden" name="product_option[' + option_row + '][type]" value="' + item['type'] + '" />';

	  if (item['type'] == 'text') {
		  html += '    <div class="form-group">';
		  html += '      <label class="col-sm-2 control-label" for="input-value' + option_row + '">{{ entry_option_value|e('js') }}</label>';
		  html += '      <div class="col-sm-10"><input type="text" name="product_option[' + option_row + '][value]" value="" placeholder="{{ entry_option_value|e('js') }}" id="input-value' + option_row + '" class="form-control" /></div>';
		  html += '    </div>';
	  }

	  if (item['type'] == 'textarea') {
		  html += '    <div class="form-group">';
		  html += '      <label class="col-sm-2 control-label" for="input-value' + option_row + '">{{ entry_option_value|e('js') }}</label>';
		  html += '      <div class="col-sm-10"><textarea name="product_option[' + option_row + '][value]" rows="5" placeholder="{{ entry_option_value|e('js') }}" id="input-value' + option_row + '" class="form-control"></textarea></div>';
		  html += '    </div>';
	  }

	  if (item['type'] == 'file') {
		  html += '    <div class="form-group" style="display: none;">';
		  html += '      <label class="col-sm-2 control-label" for="input-value' + option_row + '">{{ entry_option_value|e('js') }}</label>';
		  html += '      <div class="col-sm-10"><input type="text" name="product_option[' + option_row + '][value]" value="" placeholder="{{ entry_option_value|e('js') }}" id="input-value' + option_row + '" class="form-control" /></div>';
		  html += '    </div>';
	  }

	  if (item['type'] == 'date') {
		  html += '    <div class="form-group">';
		  html += '      <label class="col-sm-2 control-label" for="input-value' + option_row + '">{{ entry_option_value|e('js') }}</label>';
		  html += '      <div class="col-sm-3"><div class="input-group date"><input type="text" name="product_option[' + option_row + '][value]" value="" placeholder="{{ entry_option_value|e('js') }}" data-date-format="YYYY-MM-DD" id="input-value' + option_row + '" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div></div>';
		  html += '    </div>';
	  }

	  if (item['type'] == 'time') {
		  html += '    <div class="form-group">';
		  html += '      <label class="col-sm-2 control-label" for="input-value' + option_row + '">{{ entry_option_value|e('js') }}</label>';
		  html += '      <div class="col-sm-10"><div class="input-group time"><input type="text" name="product_option[' + option_row + '][value]" value="" placeholder="{{ entry_option_value|e('js') }}" data-date-format="HH:mm" id="input-value' + option_row + '" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div></div>';
		  html += '    </div>';
	  }

	  if (item['type'] == 'datetime') {
		  html += '    <div class="form-group">';
		  html += '      <label class="col-sm-2 control-label" for="input-value' + option_row + '">{{ entry_option_value|e('js') }}</label>';
		  html += '      <div class="col-sm-10"><div class="input-group datetime"><input type="text" name="product_option[' + option_row + '][value]" value="" placeholder="{{ entry_option_value|e('js') }}" data-date-format="YYYY-MM-DD HH:mm" id="input-value' + option_row + '" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div></div>';
		  html += '    </div>';
	  }

	  if (item['type'] == 'select' || item['type'] == 'radio' || item['type'] == 'checkbox' || item['type'] == 'image' || item['type'] == 'color') {
		  html += '    <div class="dc-opt-cards" id="option-value' + option_row + '"></div>';
		  html += '    <div class="dc-opt-actions">';
		  html += '      <button type="button" class="btn btn-primary btn-sm" onclick="addOptionValue(\'' + option_row + '\');"><i data-lucide="circle-plus" width="14" height="14"></i> {{ button_option_value_add|e('js') }}</button>';
		  html += '      <button type="button" class="btn btn-default btn-sm" onclick="addAllOptionValues(\'' + option_row + '\');"><i data-lucide="square-plus" width="14" height="14"></i> {{ button_add_all_values|e('js') }}</button>';
		  html += '    </div>';

		  html += '    <select id="option-values' + option_row + '" style="display: none;">';
		  for (var i = 0; i < item['option_value'].length; i++) {
			  html += '    <option value="' + item['option_value'][i]['option_value_id'] + '">' + item['option_value'][i]['name'] + '</option>';
		  }
		  html += '    </select>';
	  }

	  html += '  </div>';
	  html += '</div>';

	  $('#dc-options-container').append(html);
	  lucide.createIcons();
	  updateOptionCount();

	  $('[data-toggle=\'tooltip\']').tooltip({
		  container: 'body',
		  html: true
	  });

	  $('.date').datetimepicker({
		  language: '{{ datepicker }}',
		  pickTime: false
	  });

	  $('.time').datetimepicker({
		  language: '{{ datepicker }}',
		  pickDate: false
	  });

	  $('.datetime').datetimepicker({
		  language: '{{ datepicker }}',
		  pickDate: true,
		  pickTime: true
	  });

	  option_row++;
  }

  function loadOptionSet() {
	  var set_id = $('#dc-option-set-select').val();
	  if (!set_id) return;

	  var $existing = $('#dc-options-container .dc-option-section').not('.dc-option-axis');
	  if ($existing.length && !confirm('{{ text_option_set_replace_confirm|e("js") }}')) {
		  return;
	  }

	  $.ajax({
		  url: 'index.php?route=catalog/option_set/getOptions&user_token={{ user_token }}&option_set_id=' + set_id,
		  dataType: 'json',
		  success: function(json) {
			  if (json['error']) {
				  if (typeof showToast === 'function') showToast(json['error'], 'error');
				  return;
			  }

			  if (!json.length) {
				  if (typeof showToast === 'function') showToast('{{ text_option_set_empty|e("js") }}', 'error');
				  return;
			  }

			  $('#dc-options-container .dc-option-section').not('.dc-option-axis').remove();

			  var added = 0;
			  var skipped = 0;

			  $.each(json, function(i, item) {
				  if (!item || !item.option_id) return;
				  if (dcAxisOptionIds.indexOf(parseInt(item.option_id)) >= 0) {
					  skipped++;
					  return;
				  }

				  dcAddOption({
					  label: item['name'],
					  value: item['option_id'],
					  type: item['type'],
					  option_value: item['option_value']
				  });

				  if (item['type'] === 'select' || item['type'] === 'radio' || item['type'] === 'checkbox' || item['type'] === 'image' || item['type'] === 'color') {
					  addAllOptionValues(option_row - 1);
				  }

				  added++;
			  });

			  if (typeof showToast === 'function') {
				  if (added === 0 && skipped > 0) {
					  showToast('{{ text_option_set_axis_skipped|e("js") }}', 'error');
				  } else if (added > 0) {
					  var msg = '{{ text_option_set_loaded|e("js") }}'.replace('%d', added);
					  if (skipped > 0) {
						  msg += ' {{ text_option_set_skipped|e("js") }}'.replace('%d', skipped);
					  }
					  showToast(msg, 'success');
				  }
			  }
			  lucide.createIcons();
		  }
	  });
  }

  var option_value_row = {{ option_value_row }};

  function dcBuildOptionValueCard(option_row, ovRow, optionValueId, optionValueName, price, price_prefix, points, points_prefix, weight, weight_prefix, isHit) {
	  var html = '';
	  var optionType = $('#option-section' + option_row + ' input[name$="[type]"]').val();
	  html += '<div class="dc-opt-card" id="option-value-row' + ovRow + '">';
	  html += '  <div class="dc-opt-card__header">';
  html += '    <span class="dc-opt-card__handle" title="{{ text_drag_to_reorder }}" aria-hidden="true"><i data-lucide="grip-vertical" width="14" height="14" class="slide-drag-handle"></i></span>';
	  html += '    <span class="dc-opt-card__title">' + (optionValueName || '') + '</span>';
	  html += '    <span class="dc-opt-card__badges">';
	  if (isHit) {
		  html += '      <span class="dc-opt-badge dc-opt-badge--hit">{{ entry_hit|e('js') }}</span>';
	  }
	  if (parseFloat(price) != 0) {
		  html += '      <span class="dc-opt-badge dc-opt-badge--price" data-badge="price">' + price_prefix + price + '</span>';
	  } else {
		  html += '      <span class="dc-opt-badge dc-opt-badge--price" data-badge="price" style="display:none"></span>';
	  }
	  html += '    </span>';
	  html += '    <button type="button" class="btn btn-danger btn-xs dc-opt-card__remove" onclick="$(this).closest(\'.dc-opt-card\').remove();" data-toggle="tooltip" title="{{ button_remove|e('js') }}"><i data-lucide="circle-minus" width="14" height="14"></i></button>';
	  html += '  </div>';
	  html += '  <div class="dc-opt-card__body">';
	  html += '    <input type="hidden" name="product_option[' + option_row + '][product_option_value][' + ovRow + '][product_option_value_id]" value="" />';
	  html += '    <input type="hidden" name="product_option[' + option_row + '][product_option_value][' + ovRow + '][is_hit]" value="0" />';
	  html += '    <input type="hidden" name="product_option[' + option_row + '][product_option_value][' + ovRow + '][sort_order]" value="' + ovRow + '" class="dc-opt-sort-order" />';
	  html += '    <div class="dc-opt-card__grid">';
	  html += '      <div class="dc-opt-card__field">';
	  html += '        <label>{{ entry_option_value|e('js') }}</label>';
	  html += '        <select name="product_option[' + option_row + '][product_option_value][' + ovRow + '][option_value_id]" class="form-control" onchange="dcUpdateCardTitle(this)">';
	  var optsHtml = $('#option-values' + option_row).html();
	  if (optionValueId) {
		  optsHtml = optsHtml.replace(/value="([^"]*)"/g, function(match, val) {
			  return val == optionValueId ? 'value="' + val + '" selected="selected"' : match;
		  });
	  }
	  html += optsHtml;
	  html += '        </select>';
	  html += '      </div>';
	  html += '      <div class="dc-opt-card__field">';
	  html += '        <label>{{ entry_price|e('js') }}</label>';
	  html += '        <div class="option-value-prefix-group">';
	  html += '          <select name="product_option[' + option_row + '][product_option_value][' + ovRow + '][price_prefix]" class="form-control option-value-prefix-select">';
	  html += '            <option value="+"' + (price_prefix == '+' ? ' selected="selected"' : '') + '>+</option>';
	  html += '            <option value="-"' + (price_prefix == '-' ? ' selected="selected"' : '') + '>-</option>';
	  html += '          </select>';
	  html += '          <input type="text" name="product_option[' + option_row + '][product_option_value][' + ovRow + '][price]" value="' + price + '" placeholder="{{ entry_price|e('js') }}" class="form-control option-value-prefix-input" oninput="dcUpdateBadges(this)" />';
	  html += '        </div>';
	  html += '      </div>';
	  html += '      <div class="dc-opt-card__field">';
	  html += '        <label>{{ entry_option_points|e('js') }}</label>';
	  html += '        <div class="option-value-prefix-group">';
	  html += '          <select name="product_option[' + option_row + '][product_option_value][' + ovRow + '][points_prefix]" class="form-control option-value-prefix-select">';
	  html += '            <option value="+"' + (points_prefix == '+' ? ' selected="selected"' : '') + '>+</option>';
	  html += '            <option value="-"' + (points_prefix == '-' ? ' selected="selected"' : '') + '>-</option>';
	  html += '          </select>';
	  html += '          <input type="text" name="product_option[' + option_row + '][product_option_value][' + ovRow + '][points]" value="' + points + '" placeholder="{{ entry_points|e('js') }}" class="form-control option-value-prefix-input" />';
	  html += '        </div>';
	  html += '      </div>';
	  html += '      <div class="dc-opt-card__field">';
	  html += '        <label>{{ entry_weight|e('js') }}</label>';
	  html += '        <div class="option-value-prefix-group">';
	  html += '          <select name="product_option[' + option_row + '][product_option_value][' + ovRow + '][weight_prefix]" class="form-control option-value-prefix-select">';
	  html += '            <option value="+"' + (weight_prefix == '+' ? ' selected="selected"' : '') + '>+</option>';
	  html += '            <option value="-"' + (weight_prefix == '-' ? ' selected="selected"' : '') + '>-</option>';
	  html += '          </select>';
	  html += '          <input type="text" name="product_option[' + option_row + '][product_option_value][' + ovRow + '][weight]" value="' + weight + '" placeholder="{{ entry_weight|e('js') }}" class="form-control option-value-prefix-input" />';
	  html += '        </div>';
	  html += '      </div>';
	  html += '    </div>';
	  if (optionType === 'color') {
		  html += '    <div class="dc-opt-card__hit">';
		  html += '      <label class="dc-opt-card__hit-label">';
		  html += '        <input type="checkbox" name="product_option[' + option_row + '][product_option_value][' + ovRow + '][is_hit]" value="1" onchange="$(this).closest(\'.dc-opt-card\').find(\'input[name$=\\\'[is_hit\\\']\').val(this.checked ? 1 : 0);dcUpdateHitBadge(this)" />';
		  html += '        <i data-lucide="star" width="14" height="14"></i> {{ entry_hit|e('js') }}';
		  html += '      </label>';
		  html += '    </div>';
	  }
	  if (!$('#option-section' + option_row).hasClass('dc-option-axis')) {
	  html += '    <div class="dc-opt-card__cg">';
	  html += '      <button type="button" class="btn btn-default btn-xs dc-opt-card__cg-toggle" onclick="event.stopPropagation();$(this).closest(\'.dc-opt-card__cg\').find(\'.dc-opt-card__cg-body\').slideToggle(150);">';
	  html += '        <i data-lucide="users" width="14" height="14"></i> {{ entry_option_value_cg_price|e('js') }}';
	  html += '      </button>';
	  html += '      <div class="dc-opt-card__cg-body" style="display:none">';
	  html += '        <div class="dc-cg-prices" id="option-value-cg' + option_row + '-' + ovRow + '"></div>';
	  html += '        <button type="button" class="btn btn-primary btn-xs" style="margin-top:4px;" onclick="addOptionValueCgPrice(this, \'' + option_row + '\', \'' + ovRow + '\');"><i data-lucide="circle-plus" width="14" height="14"></i> {{ button_option_value_cg_price_add|e('js') }}</button>';
	  html += '      </div>';
	  html += '    </div>';
	  }
	  html += '  </div>';
	  html += '</div>';

	  return html;
  }

  function addOptionValue(option_row) {
	  var $opts = $('#option-values' + option_row);
	  var firstName = $opts.find('option:first').text() || '';
	  var card = dcBuildOptionValueCard(option_row, option_value_row, '', firstName, '', '0', '+', '0', '+', '0', '+', false);
	  $('#option-value' + option_row).append(card);
	  lucide.createIcons();
	  $('[data-toggle="tooltip"]').tooltip();
	  option_value_row++;
  }

  function addAllOptionValues(option_row) {
	  var $sel = $('#option-values' + option_row);
	  var existing = [];
	  $('#option-value' + option_row + ' .dc-opt-card').each(function() {
		  var v = $(this).find('select[name$="[option_value_id]"]').val();
		  if (v) existing.push(v);
	  });
	  $sel.find('option').each(function() {
		  var ovId = $(this).val();
		  var ovName = $(this).text();
		  if (existing.indexOf(ovId) === -1) {
			  var card = dcBuildOptionValueCard(option_row, option_value_row, ovId, ovName, '', '0', '+', '0', '+', '0', '+', false);
			  $('#option-value' + option_row).append(card);
			  lucide.createIcons();
			  option_value_row++;
		  }
	  });
	  lucide.createIcons();
	  $('[data-toggle="tooltip"]').tooltip();
  }

  function addOptionValueCgPrice(button, option_row, option_value_row) {
	  var cg_row = $(button).closest('.dc-opt-card__cg-body').find('.dc-cg-prices > div').length;

	  var html = '<div class="dc-opt-card__cg-row">';
	  html += '  <select name="product_option[' + option_row + '][product_option_value][' + option_value_row + '][customer_group_price][' + cg_row + '][customer_group_id]" class="form-control">';
    {% for customer_group in customer_groups %}
	  html += '    <option value="{{ customer_group.customer_group_id }}">{{ customer_group.name|escape('js') }}</option>';
    {% endfor %}
	  html += '  </select>';
	  html += '  <div class="option-value-prefix-group">';
	  html += '    <select name="product_option[' + option_row + '][product_option_value][' + option_value_row + '][customer_group_price][' + cg_row + '][price_prefix]" class="form-control option-value-prefix-select">';
	  html += '      <option value="+">+</option>';
	  html += '      <option value="-">-</option>';
	  html += '    </select>';
	  html += '    <input type="text" name="product_option[' + option_row + '][product_option_value][' + option_value_row + '][customer_group_price][' + cg_row + '][price]" value="" placeholder="{{ entry_price|e('js') }}" class="form-control option-value-prefix-input" />';
	  html += '  </div>';
	  html += '  <button type="button" onclick="$(this).closest(\'.dc-opt-card__cg-row\').remove();" class="btn btn-danger btn-xs"><i data-lucide="circle-minus" width="14" height="14"></i></button>';
	  html += '</div>';

	  $(button).closest('.dc-opt-card__cg-body').find('.dc-cg-prices').append(html);
	  lucide.createIcons();
  }

  function dcUpdateBadges(el) {
	  var $card = $(el).closest('.dc-opt-card');
	  var qty = $card.find('input[name$="[quantity]"]').val();
	  var price = $card.find('input[name$="[price]"]').not('[name*="customer_group"]').val();
	  var price_prefix = $card.find('select[name$="[price_prefix]"]').not('[name*="customer_group"]').val();

	  var $qtyBadge = $card.find('[data-badge="qty"]');
	  $qtyBadge.text(qty + ' {{ text_qty_short|e('js') }}');
	  $qtyBadge.removeClass('dc-opt-badge--qty-ok dc-opt-badge--qty-zero');
	  $qtyBadge.addClass(parseFloat(qty) > 0 ? 'dc-opt-badge--qty-ok' : 'dc-opt-badge--qty-zero');

	  var $priceBadge = $card.find('[data-badge="price"]');
	  if (parseFloat(price) != 0) {
		  $priceBadge.text(price_prefix + price).show();
	  } else {
		  $priceBadge.hide();
	  }
  }

  function dcUpdateCardTitle(sel) {
	  var name = $(sel).find('option:selected').text();
	  $(sel).closest('.dc-opt-card').find('.dc-opt-card__title').text(name);
  }

  function dcUpdateHitBadge(cb) {
	  var $card = $(cb).closest('.dc-opt-card');
	  var $badges = $card.find('.dc-opt-card__badges');
	  if (cb.checked) {
		  if (!$badges.find('.dc-opt-badge--hit').length) {
			  $badges.prepend('<span class="dc-opt-badge dc-opt-badge--hit">{{ entry_hit|e("js") }}</span>');
		  }
	  } else {
		  $badges.find('.dc-opt-badge--hit').remove();
	  }
  }

  function removePromoCard(cardId) {
	  var $card = $('#' + cardId);
	  $card.remove();
	  var $container = $('#promo-container');
	  if ($container.find('.dc-promo-card').length === 0) {
		  if ($('#promo-empty').length === 0) {
			  $container.html('<div class="dc-promo-empty" id="promo-empty"><i data-lucide="tag" width="32" height="32" style="opacity:0.25;margin-bottom:6px;"></i><div>{{ text_no_promo }}</div></div>');
			  lucide.createIcons();
		  } else {
			  $('#promo-empty').show();
		  }
	  }
  }

  function addPromoCard(type) {
	  if (type === 'discount') addDiscount();
	  else if (type === 'special') addSpecial();
	  else if (type === 'gift') addGift();
	  else if (type === 'bxgy') addBxgy();
	  else if (type === 'bundle') addBundle();
	  $('#promo-add-dropdown').hide();
  }

  $('#promo-add-btn').on('click', function(e) {
	  e.stopPropagation();
	  $('#promo-add-dropdown').toggle();
  });
  $(document).on('click', function() { $('#promo-add-dropdown').hide(); });

  var discount_row = {{ discount_row }};

  function addDiscount() {
	  $('#promo-empty').hide();
	  var html = '<div class="dc-promo-card" id="discount-row' + discount_row + '">';
	  html += '  <div class="dc-promo-card__actions">';
	  html += '    <label class="dc-promo-card__toggle"><input type="checkbox" name="product_discount[' + discount_row + '][auto_renew]" value="1" class="auto-renew-cb" /><span>{{ entry_auto_renew }}</span></label>';
	  html += '    <button type="button" onclick="removePromoCard(\'discount-row' + discount_row + '\')" class="dc-promo-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>';
	  html += '  </div>';
	  html += '  <input type="hidden" name="product_discount[' + discount_row + '][date_added]" value="' + new Date().toISOString().slice(0, 19).replace('T', ' ') + '" />';
	  html += '  <span class="dc-promo-badge dc-promo-badge--discount">{{ text_promo_discount }}</span>';
	  html += '  <div class="dc-promo-card__grid">';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_customer_group }}</label>';
	  html += '      <select name="product_discount[' + discount_row + '][customer_group_id]" class="form-control">';
    {% for customer_group in customer_groups %}
	  html += '        <option value="{{ customer_group.customer_group_id }}">{{ customer_group.name|escape('js') }}</option>';
    {% endfor %}
	  html += '      </select>';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_quantity }}</label>';
	  html += '      <input type="text" name="product_discount[' + discount_row + '][quantity]" value="" placeholder="{{ entry_quantity }}" class="form-control" />';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_priority }}</label>';
	  html += '      <input type="text" name="product_discount[' + discount_row + '][priority]" value="" placeholder="{{ entry_priority }}" class="form-control" />';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_price }}</label>';
	  html += '      <input type="text" name="product_discount[' + discount_row + '][price]" value="" placeholder="{{ entry_price }}" class="form-control" />';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_date_start }}</label>';
	  html += '      <div class="input-group date"><input type="text" name="product_discount[' + discount_row + '][date_start]" value="" placeholder="{{ entry_date_start }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_date_end }}</label>';
	  html += '      <div class="input-group date"><input type="text" name="product_discount[' + discount_row + '][date_end]" value="" placeholder="{{ entry_date_end }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>';
	  html += '    </div>';
	  html += '  </div>';
	  html += '</div>';

	  $('#promo-container').append(html);
	  lucide.createIcons();
	  $('.date').datetimepicker({ pickTime: false });
	  discount_row++;
  }

  var special_row = {{ special_row }};

  function addSpecial() {
	  $('#promo-empty').hide();
	  var html = '<div class="dc-promo-card" id="special-row' + special_row + '">';
	  html += '  <div class="dc-promo-card__actions">';
	  html += '    <label class="dc-promo-card__toggle"><input type="checkbox" name="product_special[' + special_row + '][auto_renew]" value="1" class="auto-renew-cb" /><span>{{ entry_auto_renew }}</span></label>';
	  html += '    <button type="button" onclick="removePromoCard(\'special-row' + special_row + '\')" class="dc-promo-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>';
	  html += '  </div>';
	  html += '  <input type="hidden" name="product_special[' + special_row + '][date_added]" value="' + new Date().toISOString().slice(0, 19).replace('T', ' ') + '" />';
	  html += '  <span class="dc-promo-badge dc-promo-badge--special">{{ text_promo_special }}</span>';
	  html += '  <div class="dc-promo-card__grid">';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_customer_group }}</label>';
	  html += '      <select name="product_special[' + special_row + '][customer_group_id]" class="form-control">';
    {% for customer_group in customer_groups %}
	  html += '        <option value="{{ customer_group.customer_group_id }}">{{ customer_group.name|escape('js') }}</option>';
    {% endfor %}
	  html += '      </select>';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_priority }}</label>';
	  html += '      <input type="text" name="product_special[' + special_row + '][priority]" value="" placeholder="{{ entry_priority }}" class="form-control" />';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_price }}</label>';
	  html += '      <input type="text" name="product_special[' + special_row + '][price]" value="" placeholder="{{ entry_price }}" class="form-control" />';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_date_start }}</label>';
	  html += '      <div class="input-group date"><input type="text" name="product_special[' + special_row + '][date_start]" value="" placeholder="{{ entry_date_start }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_date_end }}</label>';
	  html += '      <div class="input-group date"><input type="text" name="product_special[' + special_row + '][date_end]" value="" placeholder="{{ entry_date_end }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>';
	  html += '    </div>';
	  html += '  </div>';
	  html += '</div>';

	  $('#promo-container').append(html);
	  lucide.createIcons();
	  $('.date').datetimepicker({ language: '{{ datepicker }}', pickTime: false });
	  special_row++;
  }

  var gift_row = {{ gift_row }};

  function addGift() {
	  $('#promo-empty').hide();
	  var html = '<div class="dc-promo-card" id="gift-row' + gift_row + '">';
	  html += '  <div class="dc-promo-card__actions">';
	  html += '    <label class="dc-promo-card__toggle"><input type="checkbox" name="product_gift[' + gift_row + '][auto_renew]" value="1" class="auto-renew-cb" /><span>{{ entry_auto_renew }}</span></label>';
	  html += '    <button type="button" onclick="removePromoCard(\'gift-row' + gift_row + '\')" class="dc-promo-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>';
	  html += '  </div>';
	  html += '  <input type="hidden" name="product_gift[' + gift_row + '][date_added]" value="' + new Date().toISOString().slice(0, 19).replace('T', ' ') + '" />';
	  html += '  <span class="dc-promo-badge dc-promo-badge--gift">{{ text_promo_gift }}</span>';
	  html += '  <div class="dc-promo-card__grid">';
	  html += '    <div class="dc-promo-card__field dc-promo-card__field--wide">';
	  html += '      <label>{{ entry_gift_product }}</label>';
	  html += '      <div style="position:relative;">';
	  html += '        <input type="text" name="product_gift[' + gift_row + '][gift_product_name]" value="" placeholder="{{ entry_gift_product }}" class="form-control" autocomplete="off" />';
	  html += '        <input type="hidden" name="product_gift[' + gift_row + '][gift_product_id]" value="" />';
	  html += '      </div>';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_minimum }}</label>';
	  html += '      <input type="text" name="product_gift[' + gift_row + '][minimum_quantity]" value="1" placeholder="{{ entry_minimum }}" class="form-control" />';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_date_start }}</label>';
	  html += '      <div class="input-group date"><input type="text" name="product_gift[' + gift_row + '][date_start]" value="" placeholder="{{ entry_date_start }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_date_end }}</label>';
	  html += '      <div class="input-group date"><input type="text" name="product_gift[' + gift_row + '][date_end]" value="" placeholder="{{ entry_date_end }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>';
	  html += '    </div>';
	  html += '  </div>';
	  html += '</div>';

	  $('#promo-container').append(html);
	  lucide.createIcons();
	  giftAutocomplete(gift_row);
	  $('.date').datetimepicker({ language: '{{ datepicker }}', pickTime: false });
	  gift_row++;
  }

  function giftAutocomplete(gift_row) {
	  var $input = $('input[name=\'product_gift[' + gift_row + '][gift_product_name]\']');
	  var $idInput = $('input[name=\'product_gift[' + gift_row + '][gift_product_id]\']');

	  $input.autocomplete({
		  'source': function(request, response) {
			  $.ajax({
				  url: 'index.php?route=catalog/product/autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
				  dataType: 'json',
				  success: function(json) {
					  response($.map(json, function(item) {
						  return {
							  label: item['name'],
							  value: item['product_id']
						  }
					  }));
				  }
			  });
		  },
		  'select': function(item) {
			  $input.val(item['label']);
			  $idInput.val(item['value']);
		  }
	  });

	  $input.on('input change', function() {
		  if ($(this).val() === '') {
			  $idInput.val('');
		  }
	  });
  }

  $('#promo-container').on('focus', 'input[name$="[gift_product_name]"]', function() {
	  if ($(this).closest('.dc-autocomplete-wrap').length) return;
	  var match = $(this).attr('name').match(/\[(\d+)\]/);
	  if (match) {
		  giftAutocomplete(match[1]);
	  }
  });

  var bxgy_row = {{ bxgy_row }};

  function addBxgy() {
	  $('#promo-empty').hide();
	  var html = '<div class="dc-promo-card" id="bxgy-row' + bxgy_row + '">';
	  html += '  <div class="dc-promo-card__actions">';
	  html += '    <label class="dc-promo-card__toggle"><input type="checkbox" name="product_bxgy[' + bxgy_row + '][auto_renew]" value="1" class="auto-renew-cb" /><span>{{ entry_auto_renew }}</span></label>';
	  html += '    <button type="button" onclick="removePromoCard(\'bxgy-row' + bxgy_row + '\')" class="dc-promo-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>';
	  html += '  </div>';
	  html += '  <input type="hidden" name="product_bxgy[' + bxgy_row + '][date_added]" value="' + new Date().toISOString().slice(0, 19).replace('T', ' ') + '" />';
	  html += '  <span class="dc-promo-badge dc-promo-badge--bxgy">{{ text_promo_bxgy }}</span>';
	  html += '  <div class="dc-promo-card__grid">';
	  html += '    <div class="dc-promo-card__field dc-promo-card__field--wide">';
	  html += '      <label>{{ entry_bxgy_reward_product }}</label>';
	  html += '      <div style="position:relative;">';
	  html += '        <input type="text" name="product_bxgy[' + bxgy_row + '][reward_product_name]" value="" placeholder="{{ entry_bxgy_reward_product }}" class="form-control" autocomplete="off" />';
	  html += '        <input type="hidden" name="product_bxgy[' + bxgy_row + '][reward_product_id]" value="" />';
	  html += '      </div>';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_bxgy_trigger_quantity }}</label>';
	  html += '      <input type="text" name="product_bxgy[' + bxgy_row + '][trigger_quantity]" value="1" placeholder="{{ entry_bxgy_trigger_quantity }}" class="form-control" />';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_bxgy_discount_value }}</label>';
	  html += '      <input type="text" name="product_bxgy[' + bxgy_row + '][discount_value]" value="" placeholder="{{ entry_bxgy_discount_value }}" class="form-control" />';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_date_start }}</label>';
	  html += '      <div class="input-group date"><input type="text" name="product_bxgy[' + bxgy_row + '][date_start]" value="" placeholder="{{ entry_date_start }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_date_end }}</label>';
	  html += '      <div class="input-group date"><input type="text" name="product_bxgy[' + bxgy_row + '][date_end]" value="" placeholder="{{ entry_date_end }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>';
	  html += '    </div>';
	  html += '  </div>';
	  html += '</div>';

	  $('#promo-container').append(html);
	  lucide.createIcons();
	  bxgyAutocomplete(bxgy_row);
	  $('.date').datetimepicker({ language: '{{ datepicker }}', pickTime: false });
	  bxgy_row++;
  }

  function bxgyAutocomplete(bxgy_row) {
	  var $input = $('input[name=\'product_bxgy[' + bxgy_row + '][reward_product_name]\']');
	  var $idInput = $('input[name=\'product_bxgy[' + bxgy_row + '][reward_product_id]\']');

	  $input.autocomplete({
		  'source': function(request, response) {
			  $.ajax({
				  url: 'index.php?route=catalog/product/autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
				  dataType: 'json',
				  success: function(json) {
					  response($.map(json, function(item) {
						  return {
							  label: item['name'],
							  value: item['product_id']
						  }
					  }));
				  }
			  });
		  },
		  'select': function(item) {
			  $input.val(item['label']);
			  $idInput.val(item['value']);
		  }
	  });

	  $input.on('input change', function() {
		  if ($(this).val() === '') {
			  $idInput.val('');
		  }
	  });
  }

  $('#promo-container').on('focus', 'input[name$="[reward_product_name]"]', function() {
	  if ($(this).closest('.dc-autocomplete-wrap').length) return;
	  var match = $(this).attr('name').match(/\[(\d+)\]/);
	  if (match) {
		  bxgyAutocomplete(match[1]);
	  }
  });

  var bundle_row = {{ bundle_row }};

  function addBundle() {
	  $('#promo-empty').hide();
	  var html = '<div class="dc-promo-card dc-promo-card--bundle" id="bundle-row' + bundle_row + '">';
	  html += '  <div class="dc-promo-card__actions">';
	  html += '    <label class="dc-promo-card__toggle"><input type="checkbox" name="product_bundle[' + bundle_row + '][auto_renew]" value="1" class="auto-renew-cb" /><span>{{ entry_auto_renew }}</span></label>';
	  html += '    <button type="button" onclick="removeBundleCard(\'bundle-row' + bundle_row + '\', 0)" class="dc-promo-card__remove" title="{{ button_remove }}"><i data-lucide="x" width="14" height="14"></i></button>';
	  html += '  </div>';
	  html += '  <input type="hidden" name="product_bundle[' + bundle_row + '][date_added]" value="' + new Date().toISOString().slice(0, 19).replace('T', ' ') + '" />';
	  html += '  <span class="dc-promo-badge dc-promo-badge--bundle">{{ text_promo_bundle }}</span>';
	  html += '  <div class="dc-promo-card__grid">';
	  html += '    <div class="dc-promo-card__field" style="display:none;"><input type="hidden" name="product_bundle[' + bundle_row + '][bundle_id]" value="" /></div>';
	  html += '    <div class="dc-promo-card__field dc-promo-card__field--wide">';
	  html += '      <label>{{ entry_bundle_products }}</label>';
	  html += '      <div style="position:relative;">';
	  html += '        <input type="text" name="product_bundle[' + bundle_row + '][bundle_products_text]" value="" placeholder="{{ entry_bundle_products }}" class="form-control bundle-product-input" autocomplete="off" />';
	  html += '      </div>';
	  html += '      <input type="hidden" name="product_bundle[' + bundle_row + '][bundle_product_ids]" value="" />';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_discount_type }}</label>';
	  html += '      <select name="product_bundle[' + bundle_row + '][discount_type]" class="form-control">';
	  html += '        <option value="percentage">{{ text_percentage }}</option>';
	  html += '        <option value="fixed">{{ text_fixed }}</option>';
	  html += '      </select>';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_discount_value }}</label>';
	  html += '      <input type="text" name="product_bundle[' + bundle_row + '][discount_value]" value="" placeholder="{{ entry_discount_value }}" class="form-control" />';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_date_start }}</label>';
	  html += '      <div class="input-group date"><input type="text" name="product_bundle[' + bundle_row + '][date_start]" value="" placeholder="{{ entry_date_start }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>';
	  html += '    </div>';
	  html += '    <div class="dc-promo-card__field">';
	  html += '      <label>{{ entry_date_end }}</label>';
	  html += '      <div class="input-group date"><input type="text" name="product_bundle[' + bundle_row + '][date_end]" value="" placeholder="{{ entry_date_end }}" data-date-format="YYYY-MM-DD" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i data-lucide="calendar" width="14" height="14"></i></button></span></div>';
	  html += '    </div>';
	  html += '  </div>';
	  html += '  <label style="font-size:12px;font-weight:500;color:var(--dc-text-muted);">{{ text_bundle_selected_products }}</label>';
	  html += '  <div class="bundle-selected-products" id="bundle-selected-' + bundle_row + '"></div>';
	  html += '</div>';

	  $('#promo-container').append(html);
	  lucide.createIcons();
	  bundleAutocomplete(bundle_row);
	  $('.date').datetimepicker({ language: '{{ datepicker }}', pickTime: false });
	  bundle_row++;
  }

  function bundleAutocomplete(row) {
	  var $input = $('input[name="product_bundle[' + row + '][bundle_products_text]"]');
	  var $idsInput = $('input[name="product_bundle[' + row + '][bundle_product_ids]"]');
	  var $well = $('#bundle-selected-' + row);

	  $input.autocomplete({
		  'source': function(request, response) {
			  $.ajax({
				  url: 'index.php?route=catalog/product/autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
				  dataType: 'json',
				  success: function(json) {
					  response($.map(json, function(item) {
						  return {
							  label: item['name'] + ' (' + item['model'] + ')',
							  value: item['product_id'],
							  name: item['name'],
							  price: item['price'] ? item['price'] : ''
						  }
					  }));
				  }
			  });
		  },
		  'select': function(item) {
			  if ($well.find('[data-product-id="' + item.value + '"]').length) {
				  $input.val('');
				  return;
			  }
			  var chipHtml = '<span class="dc-chip" data-product-id="' + item.value + '">' + escapeHtml(item.name) + ' <button type="button" onclick="removeBundleProduct(this, ' + item.value + ')" class="dc-chip__remove"><i data-lucide="x" width="12" height="12"></i></button></span>';
			  $well.append(chipHtml);
			  lucide.createIcons();
			  updateBundleIds(row);
			  $input.val('');
		  }
	  });
  }

  function removeBundleProduct(btn, productId) {
	  $(btn).closest('.dc-chip').remove();
	  var $card = $(btn).closest('.dc-promo-card');
	  var match = $card.attr('id').match(/bundle-row(\d+)/);
	  if (match) {
		  updateBundleIds(match[1]);
	  }
  }

  function updateBundleIds(row) {
	  var ids = [];
	  $('#bundle-selected-' + row + ' .dc-chip').each(function() {
		  ids.push($(this).data('product-id'));
	  });
	  $('input[name="product_bundle[' + row + '][bundle_product_ids]"]').val(ids.join(','));
  }

  function removeBundleCard(cardId, bundleId) {
	  if (bundleId > 0) {
		  if (!confirm('{{ text_bundle_delete_confirm }}')) return;
		  $.post('index.php?route=catalog/product/deleteBundle&user_token={{ user_token }}', { bundle_id: bundleId }, function(json) {
			  $('#' + cardId).remove();
			  if ($('#promo-container .dc-promo-card').length === 0) {
				  $('#promo-empty').show();
			  }
		  }, 'json');
	  } else {
		  $('#' + cardId).remove();
		  if ($('#promo-container .dc-promo-card').length === 0) {
			  $('#promo-empty').show();
		  }
	  }
  }

  $('.bundle-product-input').each(function() {
	  var match = $(this).attr('name').match(/\[(\d+)\]/);
	  if (match) {
		  bundleAutocomplete(match[1]);
	  }
  });

  var customer_group_price_row = {{ customer_group_price_row }};

  function addCustomerGroupPrice() {
    var html = '<div class="dc-cgp-row" id="customer-group-price-row' + customer_group_price_row + '">';
    html += '<select name="product_customer_group_price[' + customer_group_price_row + '][customer_group_id]" class="form-control dc-cgp-select">';
    {% for customer_group in customer_groups %}
    html += '<option value="{{ customer_group.customer_group_id }}">{{ customer_group.name|escape('js') }}</option>';
    {% endfor %}
    html += '</select>';
    html += '<input type="text" name="product_customer_group_price[' + customer_group_price_row + '][price]" value="" placeholder="{{ entry_price }}" class="form-control dc-cgp-price"/>';
    html += '<button type="button" onclick="$(\'#customer-group-price-row' + customer_group_price_row + '\').remove();" class="btn btn-link dc-cgp-remove"><i data-lucide="x" width="14" height="14"></i></button>';
    html += '</div>';
    $('#customer-group-prices').append(html);
    lucide.createIcons();
    customer_group_price_row++;
  }

  var image_row = {{ image_row }};

  function addImage(language_id) {
    var $container = language_id === ''
      ? $('#product-images-all')
      : $('#product-images-' + language_id);
    var html = '<div class="dc-img-card" data-row="' + image_row + '" title="{{ text_drag_to_reorder }}">';
    html += '<span class="dc-img-card__handle" title="{{ text_drag_to_reorder }}" aria-hidden="true"><i data-lucide="grip-vertical" width="12" height="12" class="slide-drag-handle"></i></span>';
    html += '<input type="hidden" name="product_image[' + image_row + '][image]" value="" id="input-image' + image_row + '"/>';
    html += '<input type="hidden" name="product_image[' + image_row + '][sort_order]" value="0" class="dc-img-sort-order"/>';
    html += '<input type="hidden" name="product_image[' + image_row + '][language_id]" value="' + language_id + '"/>';
    html += '<a href="" id="thumb-image' + image_row + '" data-toggle="image" class="dc-img-link"><img src="{{ placeholder }}" alt="" title="" data-placeholder="{{ placeholder }}" draggable="false"/></a>';
    html += '<button type="button" onclick="removeImage(' + image_row + ');" class="dc-img-remove"><i data-lucide="x" width="14" height="14"></i></button>';
    html += '</div>';
    $container.append(html);
    lucide.createIcons();
    image_row++;
  }

  function removeImage(row) {
    var $card = $('.product-images .dc-img-card[data-row="' + row + '"]');
    $card.remove();
  }

  function updateImageSortOrders() {
    $('.product-images').each(function() {
      $(this).find('.dc-img-card').each(function(i) {
        $(this).find('.dc-img-sort-order').val(i);
      });
    });
  }

  if (typeof Sortable !== 'undefined') {
    $('.product-images').each(function() {
      Sortable.create(this, {
        animation: 150,
        filter: '.dc-img-remove, input',
        ghostClass: 'dc-img-ghost',
        chosenClass: 'dc-img-chosen',
        onEnd: function() {
          updateImageSortOrders();
        }
      });
    });

    $('.dc-opt-cards').each(function() {
      Sortable.create(this, {
        animation: 150,
        filter: '.dc-opt-card__remove, input, select, textarea, button, .dc-img-link',
        preventOnFilter: false,
        ghostClass: 'dc-opt-ghost',
        chosenClass: 'dc-opt-chosen',
        onEnd: function() {
          updateOptionValueSortOrders();
        }
      });
    });
  }

  function updateOptionValueSortOrders() {
    $('.dc-opt-cards').each(function() {
      $(this).find('.dc-opt-card').each(function(i) {
        $(this).find('.dc-opt-sort-order').val(i);
      });
    });
  }

  function clearProductVideo() {
    $('#input-video-youtube').val('');
    $('#video-youtube-preview').hide();
    var $card = $('#video-mp4-block').find('.dc-video-card');
    $card.find('.dc-video-card__img').attr('src', $card.find('.dc-video-card__img').attr('data-placeholder'));
    $card.find('.dc-video-card__overlay').hide();
    $('#input-product-video').val('');
    $('#video-mp4-block').hide();
  }

  function clearModel3d() {
    var $card = $('#thumb-model-3d').closest('.dc-video-card');
    $card.find('.dc-video-card__img').attr('src', $card.find('.dc-video-card__img').attr('data-placeholder'));
    $card.find('.dc-video-card__overlay').hide();
    $('#input-model-3d').val('');
  }

  function clearImage360() {
    var $card = $('#thumb-image-360').closest('.dc-video-card');
    $card.find('.dc-video-card__img').attr('src', $card.find('.dc-video-card__img').attr('data-placeholder'));
    $card.find('.dc-video-card__overlay').hide();
    $('#input-image-360').val('');
  }

  function openVideoFileManager() {
    $('#modal-image').remove();
    $.ajax({
      url: 'index.php?route=common/filemanager&user_token=' + getURLVar('user_token')
           + '&target=input-product-video&thumb=thumb-product-video&type=video',
      dataType: 'html',
      beforeSend: function() {
        $('#button-browse-mp4').prop('disabled', true);
      },
      complete: function() {
        $('#button-browse-mp4').prop('disabled', false);
      },
      success: function(html) {
        $('body').append('<div id="modal-image" class="modal">' + html + '</div>');
        $('#modal-image').modal('show');
      }
    });
  }

  function dcExtractYouTubeId(value) {
    value = (value || '').trim();
    if (/^[A-Za-z0-9_-]{11}$/.test(value)) {
      return value;
    }
    var patterns = [
      /(?:youtube\.com|youtu\.be|youtube-nocookie\.com)\/(?:watch\?v=|embed\/|shorts\/)?([A-Za-z0-9_-]{11})/,
      /^([A-Za-z0-9_-]{11})$/
    ];
    for (var i = 0; i < patterns.length; i++) {
      var m = value.match(patterns[i]);
      if (m) {
        return m[1];
      }
    }
    return '';
  }

  $(document).on('input', '#input-video-youtube', function() {
    var val = $(this).val().trim();
    if (val) {
      var ytId = dcExtractYouTubeId(val);
      $('#video-mp4-block').hide();
      $('#input-product-video').val('');
      $('#video-mp4-block').find('.dc-video-card__img').attr('src', $('#video-mp4-block').find('.dc-video-card__img').attr('data-placeholder'));
      $('#video-mp4-block').find('.dc-video-card__overlay').hide();
      $('#video-youtube-preview').show();
      if (ytId) {
        $('#video-youtube-thumb-img').attr('src', 'https://img.youtube.com/vi/' + ytId + '/hqdefault.jpg');
      } else {
        $('#video-youtube-thumb-img').attr('src', $('#video-youtube-thumb-img').attr('data-placeholder') || '');
      }
    } else {
      $('#video-youtube-preview').hide();
    }
  });

  $(document).on('hidden.bs.modal', '#modal-image', function() {
    var mp4Val = $('#input-product-video').val();
    if (mp4Val) {
      $('#video-youtube-preview').hide();
      $('#input-video-youtube').val('');
      $('#video-mp4-block').show();
      $('#video-mp4-block').find('.dc-video-card__overlay').show();
      $('#video-mp4-block').find('.dc-video-card__name').text(mp4Val);
    }
    var modelVal = $('#input-model-3d').val();
    if (modelVal) {
      $('#thumb-model-3d').closest('.dc-video-card').find('.dc-video-card__overlay').show();
      $('#thumb-model-3d').closest('.dc-video-card').find('.dc-video-card__name').text(modelVal);
    }
  });

  $('.date').datetimepicker({
	  language: '{{ datepicker }}',
	  pickTime: false
  });

  $('.time').datetimepicker({
	  language: '{{ datepicker }}',
	  pickDate: false
  });

  $('.datetime').datetimepicker({
	  language: '{{ datepicker }}',
	  pickDate: true,
	  pickTime: true
  });

  $('#language a:first').tab('show');

  var articlesVisible = false;
  $('#toggle-articles').on('click', function() {
    articlesVisible = !articlesVisible;
    if (articlesVisible) {
      $('.dc-article-extra').show();
      $('#toggle-articles-text').text('{{ button_hide_articles }}');
      $(this).find('i').replaceWith('<i data-lucide="toggle-right" width="14" height="14"></i>');
      lucide.createIcons({ nodes: [this] });
    } else {
      $('.dc-article-extra').hide();
      $('#toggle-articles-text').text('{{ button_show_articles }}');
      $(this).find('i').replaceWith('<i data-lucide="toggle-left" width="14" height="14"></i>');
      lucide.createIcons({ nodes: [this] });
    }
  });

  $('input[name^="product_seo_url"]').on('input', function() {
    var firstVal = $('input[name^="product_seo_url"]:first').val();
    if (firstVal) {
      $('#seo-url-preview').text(firstVal);
    } else {
      $('#seo-url-preview').text('...');
    }
  });

var categoryTreeInstance = null;

$('#btn-open-category-modal').on('click', function() {
  var $tree = $('#category-modal-tree');
  categoryTreeInstance = $tree.data('dcx-tree-select');
  if (!categoryTreeInstance) {
    DcxTreeSelect.init('#category-modal-tree');
    categoryTreeInstance = $tree.data('dcx-tree-select');
  }

  var currentIds = [];
  $('#product-category .dcx-tree-select__well-item').each(function() {
    currentIds.push($(this).data('id'));
  });
  categoryTreeInstance.setSelected(currentIds);

  $('#category-tree-modal').modal('show');
});

$('#category-tree-confirm').on('click', function() {
  if (!categoryTreeInstance) {
    $('#category-tree-modal').modal('hide');
    return;
  }

  var ids = categoryTreeInstance.getSelectedIds();
  var $well = $('#product-category');
  $well.empty();

  var allItems = categoryTreeInstance.cache || [];
  for (var i = 0; i < ids.length; i++) {
    for (var j = 0; j < allItems.length; j++) {
      if (String(allItems[j].category_id) === String(ids[i])) {
        var name = allItems[j].path || allItems[j].name;
        $well.append('<div class="dcx-tree-select__well-item" data-id="' + ids[i] + '"><i data-lucide="circle-minus" width="14" height="14" class="dcx-tree-select__well-remove"></i> <span class="dcx-tree-select__well-label">' + name + '</span><input type="hidden" name="product_category[]" value="' + ids[i] + '"/></div>');
        break;
      }
    }
  }

  $well.toggle(ids.length > 0);
  $('#category-modal-count').text(ids.length > 0 ? ids.length + ' {{ entry_category }}' : '{{ button_select_category }}');
  lucide.createIcons();
  categoryTreeInstance.close();
  $('#category-tree-modal').modal('hide');
});

$('#category-tree-modal').on('hidden.bs.modal', function() {
  if (categoryTreeInstance) categoryTreeInstance.close();
});

$(document).on('click', '#product-category .dcx-tree-select__well-remove', function() {
  $(this).closest('.dcx-tree-select__well-item').remove();
  var count = $('#product-category .dcx-tree-select__well-item').length;
  $('#category-modal-count').text(count > 0 ? count + ' {{ entry_category }}' : '{{ button_select_category }}');
  $('#product-category').toggle(count > 0);
});

// ── Tag chip input ──
function dcTagSyncHidden($field) {
  var $wrap = $field.closest('.dc-tag-input');
  var tags = [];
  $wrap.find('.dc-tag-chip').each(function() {
    tags.push($(this).data('tag'));
  });
  $wrap.find('input[type="hidden"]').val(tags.join(','));
}

function dcTagAddChip($field, value) {
  value = value.replace(/[,;]\s*$/, '').trim();
  if (!value) return false;
  var $wrap = $field.closest('.dc-tag-input');
  var $tags = $wrap.find('.dc-tag-input__tags');
  var exists = false;
  $tags.find('.dc-tag-chip').each(function() {
    if ($(this).data('tag').toLowerCase() === value.toLowerCase()) {
      exists = true;
      return false;
    }
  });
  if (exists) return false;
  var chip = '<span class="dc-tag-chip" data-tag="' + value.replace(/"/g, '&quot;') + '">' +
    escapeHtml(value) +
    ' <button type="button" class="dc-tag-chip__remove"><i data-lucide="x" width="12" height="12"></i></button></span>';
  $tags.append(chip);
  lucide.createIcons({ nodes: [$tags[0]] });
  dcTagSyncHidden($field);
  return true;
}

{% for language in languages %}
(function() {
  var $input = $('#input-tag{{ language.language_id }}');
  var $wrap = $input.closest('.dc-tag-input');
  var val = $input.closest('.dc-tag-input').find('input[type="hidden"]').val();
  if (val) {
    var parts = val.split(',');
    for (var i = 0; i < parts.length; i++) {
      var t = parts[i].trim();
      if (t) dcTagAddChip($input, t);
    }
  }
  $input.on('keydown', function(e) {
    if (e.key === 'Enter' || e.key === ',') {
      e.preventDefault();
      if (dcTagAddChip($input, $input.val())) {
        $input.val('');
      }
    }
    if (e.key === 'Backspace' && !$input.val()) {
      var $chips = $wrap.find('.dc-tag-chip');
      if ($chips.length) {
        $chips.last().remove();
        dcTagSyncHidden($input);
      }
    }
  });
  $input.on('blur', function() {
    var v = $input.val();
    if (v.trim()) {
      if (dcTagAddChip($input, v)) {
        $input.val('');
      } else {
        $input.val('');
      }
    }
  });
  $wrap.on('click', '.dc-tag-chip__remove', function() {
    $(this).closest('.dc-tag-chip').remove();
    dcTagSyncHidden($input);
  });
})();
{% endfor %}

function escapeHtml(text) {
  if (!text) return '';
  var div = document.createElement('div');
  div.appendChild(document.createTextNode(text));
  return div.innerHTML;
}
//--></script>

{{ footer }}
