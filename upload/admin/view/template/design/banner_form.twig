{{ header }}{{ column_left }}
<div id="content">
  <link rel="stylesheet" href="view/stylesheet/coloris.min.css" />
  <style>
    .banner-slide-card { border: 1px solid #e5e7eb; border-radius: 8px; background: #fff; margin-bottom: 16px; overflow: hidden; transition: border-color .2s, box-shadow .2s; }
    .banner-slide-card:hover { border-color: #d1d5db; }
    .banner-slide-card.drag-over { border-color: #6366f1; box-shadow: 0 0 0 2px rgba(99,102,241,.25); }
    .banner-slide-card.dragging { opacity: .4; }
    .banner-slide-card__head { display: flex; align-items: center; justify-content: space-between; gap: 10px; padding: 12px 16px; background: #f8fafc; border-bottom: 1px solid #e5e7eb; }
    .banner-slide-card__num { font-weight: 700; color: #374151; font-size: 13px; }
    .banner-slide-card__body { padding: 18px; }
    .banner-slide-card .form-group { padding: 0; margin: 0 0 14px; border-top: none; box-sizing: border-box; }
    .dcx-panel-body .banner-slide-card .form-group + .form-group { border-top: none; padding-top: 0; margin-top: 0; }
    .banner-slide-card .form-group:before,
    .banner-slide-card .form-group:after { display: none; }

    /* Banner edit form — scoped (mirrors dcx-form look, no global impact) */
    #form-banner .dcx-banner-general .form-group {
      margin: 0 0 12px;
      padding: 0;
      border-top: none;
    }
    #form-banner .dcx-banner-general .form-group + .form-group {
      border-top: none;
      margin-top: 0;
      padding-top: 12px;
    }
    #form-banner .dcx-banner-general .control-label {
      font-size: 13px;
      font-weight: 600;
      color: var(--dc-text-dark);
      line-height: 1.4;
    }
    #form-banner .dcx-banner-general .form-control,
    #form-banner .dcx-banner-general select.form-control,
    #form-banner .dcx-banner-general textarea.form-control {
      height: 38px;
      border: 1px solid var(--dc-border-medium);
      border-radius: var(--dc-radius-md);
      background: var(--dc-surface);
      font-size: 14px;
      line-height: 1.5;
      padding: 6px 10px;
      color: var(--dc-gray-900);
      transition: border-color .2s, box-shadow .2s;
    }
    #form-banner .dcx-banner-general .form-control:focus {
      border-color: var(--dc-primary);
      box-shadow: 0 0 0 3px rgba(47, 101, 185, .08);
    }
    .banner-slide-card .form-group label { font-size: 12px; font-weight: 600; color: #4a5568; margin-bottom: 5px; display: block; }
    .banner-slide-card .form-control,
    .banner-slide-card select.form-control,
    .banner-slide-card textarea.form-control { font-size: 13px; height: 36px; width: 100%; display: block; box-sizing: border-box; border: 1px solid #d1d5db; border-radius: 6px; padding: 6px 10px; transition: border-color .15s, box-shadow .15s; }
    .banner-slide-card textarea.form-control { height: auto; }
    .banner-slide-card .form-control:focus { border-color: #6366f1; box-shadow: 0 0 0 3px rgba(99,102,241,.12); }
    .banner-slide-card .input-group { max-width: 400px; }
    .banner-slide-card .input-group-addon,
    .banner-slide-card .input-group .input-group-addon { background: #f8fafc; border-color: #d1d5db; color: #6b7280; }
    .accent-hint { font-size: 11px; color: #9ca3af; margin-top: 3px; }
    .accent-hint code { background: #f3f4f6; border-radius: 3px; padding: 1px 4px; color: #6366f1; }
    .banner-slide-card .accent-hint--spacer { visibility: hidden; }
    .slide-images-row { display: flex; gap: 16px; flex-wrap: wrap; }
    .slide-image-block { text-align: center; }
    .slide-image-block small,
    .slide-video-block small { display: block; color: #9ca3af; font-size: 11px; margin-top: 4px; }
    .btn-row-2col { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; align-items: start; }
    .banner-slide-card .dc-link-row { display: flex; align-items: flex-start; gap: 8px; }
    .banner-slide-card .dc-link-row .dc-link-type { flex: 0 0 auto; margin-bottom: 0; }
    .banner-slide-card .dc-link-row .dc-link-custom,
    .banner-slide-card .dc-link-row .dc-link-entity { flex: 1 1 auto; min-width: 0; }
    @media (max-width: 600px) {
      .btn-row-2col { grid-template-columns: 1fr; }
      .banner-slide-card .dc-link-row { flex-direction: column; gap: 8px; }
      .banner-slide-card .dc-link-row .dc-link-type { width: 100%; max-width: 100%; }
      .banner-slide-card .input-group { max-width: 100%; }
      .banner-slide-card .accent-hint--spacer { display: none; }
      .banner-slide-card .form-control,
      .banner-slide-card select.form-control,
      .banner-slide-card textarea.form-control { font-size: 16px; }
    }
    .no-slides-msg { text-align: center; color: #9ca3af; padding: 28px; font-style: italic; }
    .add-slide-btn-wrap { padding: 10px 0 4px; text-align: right; }
    .dcx-divider { border: none; border-top: 1px solid #e5e7eb; margin: 20px 0 18px; }
    .slide-section { margin-bottom: 18px; }
    .slide-section__title { font-size: 12px; font-weight: 700; color: #4a5568; margin-bottom: 10px; padding-bottom: 6px; border-bottom: 1px solid #e5e7eb; }
    .slide-section__title i { margin-right: 6px; }
    .position-radio-group { display: flex; gap: 0; max-width: 300px; }
    @media (max-width: 600px) {
      .position-radio-group { max-width: 100%; }
    }
    .position-radio-group label { display: inline-flex; align-items: center; gap: 5px; padding: 7px 14px; font-size: 12px; font-weight: 500; border: 1px solid #e5e7eb; cursor: pointer; background: #fff; color: #6b7280; transition: all .15s; }
    .position-radio-group label:first-child { border-radius: 6px 0 0 6px; }
    .position-radio-group label:last-child { border-radius: 0 6px 6px 0; }
    .position-radio-group label:has(input:checked) { background: #eef2ff; border-color: #6366f1; color: #4f46e5; }
    .position-radio-group input[type="radio"] { display: none; }
    .slide-advanced-toggle { display: inline-flex; align-items: center; gap: 4px; font-size: 11px; color: #9ca3af; cursor: pointer; margin-top: 10px; border: none; background: none; padding: 0; }
    .slide-advanced-toggle:hover { color: #6366f1; }
    .slide-advanced-toggle svg { transition: transform .2s; font-size: 10px; }
    .slide-advanced-toggle.open svg { transform: rotate(90deg); }
    .slide-advanced-body { display: none; margin-top: 10px; padding-top: 10px; border-top: 1px solid #f3f4f6; }
    .slide-advanced-body.open { display: block; }
    .banner-slide-card.collapsed .banner-slide-card__body { display: none; }
    .banner-slide-card__toggle { cursor: pointer; margin-right: 6px; transition: transform .2s; vertical-align: middle; font-size: 14px; }
    .banner-slide-card:not(.collapsed) .banner-slide-card__toggle { transform: rotate(90deg); }
    .dc-video-thumb { position: relative; display: inline-flex; align-items: center; justify-content: center; min-width: 100px; min-height: 90px; }
    .dc-video-thumb__overlay { position: absolute; inset: 0; display: flex; flex-direction: column; align-items: center; justify-content: center; background: #1f2937; border-radius: 5px; gap: 4px; color: #9ca3af; padding: 8px; }
    .dc-video-thumb__icon { width: 28px; height: 28px; stroke: #6b7280; flex-shrink: 0; }
    .dc-video-thumb__name { font-size: 10px; color: #9ca3af; word-break: break-all; text-align: center; line-height: 1.2; max-width: 100%; overflow: hidden; text-overflow: ellipsis; }
    .dc-youtube-block { display: flex; gap: 12px; align-items: flex-start; flex-wrap: wrap; }
    .dc-youtube-thumb img { display: block; border: 1px solid #e5e7eb; border-radius: 4px; max-height: 60px; width: auto; }
    .slide-drag-handle { cursor: grab; color: #9ca3af; margin-right: 6px; font-size: 13px; vertical-align: middle; }
    .slide-drag-handle:active { cursor: grabbing; }
    .banner-slide-card.drag-over { border-color: #6366f1; box-shadow: 0 0 0 2px rgba(99,102,241,.25); }
    .banner-slide-card.dragging { opacity: .4; }
    #language li.drag-over-tab > a { background: #eef2ff; border-color: #6366f1; color: #4f46e5; }
    .btn-duplicate { color: #6366f1; margin-left: 4px; }
    .btn-duplicate:hover { color: #4f46e5; background: #eef2ff; }
  </style>

  <div class="page-header">
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-12 col-lg-offset-2 col-lg-8 col-xl-offset-2 col-xl-8">
          <div class="pull-right">
            <button type="submit" form="form-banner" data-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary btn--icon"><i data-lucide="save" width="16" height="16"></i></button>
            <a href="{{ cancel }}" data-toggle="tooltip" title="{{ button_cancel }}" class="btn btn-default btn--icon"><i data-lucide="x" width="16" height="16"></i></a>
          </div>
          <a href="{{ cancel }}" class="page-header__link">
            <span class="page-header__back">
              <i data-lucide="arrow-left" width="20" height="20"></i>
            </span>
            <span class="page-header__title">
              {{ text_form }}
              <span class="page-header__badge {{ status ? 'page-header__badge--active' : 'page-header__badge--inactive' }}">
                {{ status ? text_active : text_inactive }}
              </span>
            </span>
          </a>
        </div>
      </div>
    </div>
  </div>

  <div class="container-fluid">
    {% if error_warning %}
    <div class="alert alert-danger alert-dismissible"><i data-lucide="alert-circle" width="16" height="16"></i> {{ error_warning }}
      <button type="button" class="close" data-dismiss="alert">&times;</button>
    </div>
    {% endif %}

    <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-banner">
    <div class="row">
      <div class="col-sm-12 col-lg-offset-2 col-lg-8 col-xl-offset-2 col-xl-8">

      <div class="dcx-panel">
        <div class="dcx-panel-head">
          <div class="dcx-panel-head__icon"><i data-lucide="settings" width="18" height="18"></i></div>
          <div>
            <div class="dcx-panel-title">{{ text_general_settings }}</div>
          </div>
        </div>
        <div class="dcx-panel-body">
          <div class="dcx-fields">
            <div class="dcx-field required">
              <label class="dcx-label">{{ entry_name }}</label>
              {% for language in languages %}
              <div class="dc-attr-row__value">
                <span class="lang-code">{{ language.code|split('-')|first|upper }}</span>
                <input type="text" name="banner_description[{{ language.language_id }}][name]" value="{{ banner_descriptions[language.language_id].name ?? '' }}" placeholder="{{ entry_name }}" class="form-control" />
              </div>
              {% if error_name[language.language_id] is defined %}<div class="text-danger">{{ error_name[language.language_id] }}</div>{% endif %}
              {% endfor %}
            </div>
            <div class="dcx-field">
              <label class="dcx-label" for="input-status">{{ entry_status }}</label>
              <select name="status" id="input-status" class="form-control">
                <option value="1" {% if status %}selected="selected"{% endif %}>{{ text_enabled }}</option>
                <option value="0" {% if not status %}selected="selected"{% endif %}>{{ text_disabled }}</option>
              </select>
            </div>
          </div>
        </div>
      </div>

      <div class="dcx-panel" style="margin-top: 16px;">
        <div class="dcx-panel-head">
          <div class="dcx-panel-head__icon"><i data-lucide="layers" width="18" height="18"></i></div>
          <div>
            <div class="dcx-panel-title">{{ text_slides_by_language }}</div>
          </div>
        </div>
        <div class="dcx-panel-body">

              <ul class="nav nav-tabs" id="language">
            {% for language in languages %}
            <li {% if loop.first %}class="active"{% endif %}>
              <a href="#language{{ language.language_id }}" data-toggle="tab">
                <span class="lang-code">{{ language.code|split('-')|first|upper }}</span> {{ language.name }}
              </a>
            </li>
            {% endfor %}
              </ul>

              <div class="tab-content">
            {% set image_row = 0 %}
            {% for language in languages %}
            <div class="tab-pane {% if loop.first %}active{% endif %}" id="language{{ language.language_id }}">

              <div id="slide-list-{{ language.language_id }}">
                {% if banner_images[language.language_id] %}
                {% for banner_image in banner_images[language.language_id] %}
                <div class="banner-slide-card collapsed" draggable="true" id="image-row{{ image_row }}">
                  <div class="banner-slide-card__head" style="cursor:pointer;">
                    <span>
                      <i data-lucide="grip-vertical" width="14" height="14" class="slide-drag-handle"></i>
                      <i data-lucide="chevron-right" width="14" height="14" class="banner-slide-card__toggle"></i>
                      <span class="banner-slide-card__num"><i data-lucide="image" width="14" height="14" style="margin-right:6px;"></i><span class="slide-label">{% if banner_image.title %}{{ banner_image.title }}{% else %}{{ text_slide }} #{{ loop.index }}{% endif %}</span></span>
                    </span>
                    <span>
                      <button type="button" onclick="duplicateSlide({{ image_row }}, {{ language.language_id }}); event.stopPropagation();" class="btn btn-default btn-xs btn-duplicate"><i data-lucide="copy" width="14" height="14"></i></button>
                      <button type="button" onclick="removeSlide({{ image_row }}); event.stopPropagation();" class="btn btn-danger btn-xs"><i data-lucide="trash-2" width="14" height="14"></i> {{ button_remove }}</button>
                    </span>
                  </div>
                  <div class="banner-slide-card__body">

                    {# ── Content ── #}
                    <div class="slide-section">
                      <div class="slide-section__title"><i data-lucide="pencil" width="14" height="14"></i> {{ text_tab_general }}</div>
                      <div class="btn-row-2col">
                        <div class="form-group">
                          <label>{{ entry_title }}</label>
                          <input type="text" name="banner_image[{{ language.language_id }}][{{ image_row }}][title]" value="{{ banner_image.title }}" placeholder="{{ entry_title }}" class="form-control slide-title-input" />
                          <div class="accent-hint">{{ entry_title_hint }}</div>
                        </div>
                        <div class="form-group">
                          <label>{{ entry_subtitle }}</label>
                          <input type="text" name="banner_image[{{ language.language_id }}][{{ image_row }}][subtitle]" value="{{ banner_image.subtitle }}" placeholder="{{ entry_subtitle }}" class="form-control" />
                          <div class="accent-hint accent-hint--spacer">&nbsp;</div>
                        </div>
                      </div>
                      <div class="form-group">
                        <label>{{ entry_content_position }}</label>
                        <div class="position-radio-group">
                          <label><input type="radio" name="banner_image[{{ language.language_id }}][{{ image_row }}][content_position]" value="left" {% if banner_image.content_position == 'left' %}checked{% endif %} /><i data-lucide="align-left" width="14" height="14"></i> {{ text_content_position_left }}</label>
                          <label><input type="radio" name="banner_image[{{ language.language_id }}][{{ image_row }}][content_position]" value="center" {% if banner_image.content_position == 'center' %}checked{% endif %} /><i data-lucide="align-center" width="14" height="14"></i> {{ text_content_position_center }}</label>
                          <label><input type="radio" name="banner_image[{{ language.language_id }}][{{ image_row }}][content_position]" value="right" {% if banner_image.content_position == 'right' %}checked{% endif %} /><i data-lucide="align-right" width="14" height="14"></i> {{ text_content_position_right }}</label>
                        </div>
                      </div>
                    </div>

                    {# ── Media ── #}
                    <div class="slide-section">
                      <div class="slide-section__title"><i data-lucide="image" width="14" height="14"></i> {{ entry_image }}</div>
                      <div class="form-group">
                        <div class="slide-images-row">
                          <div class="slide-image-block">
                            <a href="" id="thumb-image{{ image_row }}" data-toggle="image" class="img-thumbnail"><img src="{{ banner_image.thumb }}" alt="" title="" data-placeholder="{{ placeholder }}" style="max-height:90px;" /></a>
                            <input type="hidden" name="banner_image[{{ language.language_id }}][{{ image_row }}][image]" value="{{ banner_image.image }}" id="input-image{{ image_row }}" />
                            <small>{{ text_landscape }}</small>
                          </div>
                          <div class="slide-image-block">
                            <a href="" id="thumb-image-portrait{{ image_row }}" data-toggle="image" class="img-thumbnail"><img src="{{ banner_image.thumb_portrait }}" alt="" title="" data-placeholder="{{ placeholder }}" style="max-height:90px;" /></a>
                            <input type="hidden" name="banner_image[{{ language.language_id }}][{{ image_row }}][image_portrait]" value="{{ banner_image.image_portrait }}" id="input-image-portrait{{ image_row }}" />
                            <small>{{ text_portrait }}</small>
                          </div>
                        </div>
                      </div>
                    </div>

                    {# ── Video ── #}
                    <div class="slide-section">
                      <div class="slide-section__title"><i data-lucide="video" width="14" height="14"></i> {{ entry_video }}</div>
                      <div class="form-group">
                        <label>{{ entry_video_youtube }}</label>
                        <div class="input-group" style="max-width:400px;">
                          <span class="input-group-addon"><i data-lucide="youtube" width="14" height="14"></i></span>
                          <input type="text" name="banner_image[{{ language.language_id }}][{{ image_row }}][video_youtube]" value="{{ banner_image.video_type == 'youtube' ? banner_image.video : '' }}" class="form-control dc-banner-video-youtube" placeholder="{{ entry_video_youtube_placeholder }}" />
                        </div>
                        <div class="dc-banner-youtube-preview" style="margin-top:8px;{{ banner_image.video_type == 'youtube' and banner_image.video ? '' : 'display:none;' }}">
                          <img src="{{ (banner_image.video_type == 'youtube' and banner_image.video_youtube_id) ? ('https://img.youtube.com/vi/' ~ banner_image.video_youtube_id ~ '/hqdefault.jpg') : video_placeholder }}" class="dc-banner-youtube-thumb" style="max-height:90px;border-radius:4px;" data-placeholder="{{ video_placeholder }}" onerror="this.onerror=null;this.src='{{ video_placeholder }}';" />
                          <div style="font-size:11px;color:#6b7280;margin-top:4px;">YouTube</div>
                        </div>
                      </div>
                      <div class="form-group">
                        <label>{{ entry_video_mp4 }}</label>
                        <div class="slide-video-block">
                          <a href="" id="thumb-video{{ image_row }}" data-toggle="image" data-type="video" class="img-thumbnail dc-video-thumb" style="width:130px;height:100px;display:flex;align-items:center;justify-content:center;overflow:hidden;">
                            <img src="{{ video_placeholder }}" alt="" title="" data-placeholder="{{ video_placeholder }}" class="dc-video-thumb__img" style="max-height:90px;{{ banner_image.video_type == 'mp4' and banner_image.video ? 'display:none;' : '' }}" />
                            <div class="dc-video-thumb__overlay" style="{{ banner_image.video_type == 'mp4' and banner_image.video ? '' : 'display:none;' }};display:flex;flex-direction:column;align-items:center;gap:4px;">
                              <i data-lucide="film" class="dc-video-thumb__icon" style="width:20px;height:20px;stroke:#9ca3af;"></i>
                              <span class="dc-video-thumb__name" style="font-size:10px;color:#9ca3af;word-break:break-all;text-align:center;">{{ banner_image.video }}</span>
                            </div>
                          </a>
                          <input type="hidden" name="banner_image[{{ language.language_id }}][{{ image_row }}][video_mp4]" value="{{ banner_image.video }}" id="input-video{{ image_row }}" />
                          <small>{{ text_mp4 }}</small>
                        </div>
                      </div>
                    </div>

                    {# ── Link ── #}
                    <div class="slide-section">
                      <div class="slide-section__title"><i data-lucide="link" width="14" height="14"></i> {{ entry_link }}</div>
                      <div class="form-group">
                        <label>{{ entry_link }}</label>
                        <div class="dc-link-row">
                          <select class="form-control dc-link-type" style="max-width:200px;">
                            <option value="custom" {{ banner_image.link_type == 'custom' or not banner_image.link_type ? 'selected' : '' }}>{{ text_link_custom }}</option>
                            <option value="product" {{ banner_image.link_type == 'product' ? 'selected' : '' }}>{{ text_link_product }}</option>
                            <option value="category" {{ banner_image.link_type == 'category' ? 'selected' : '' }}>{{ text_link_category }}</option>
                            <option value="manufacturer" {{ banner_image.link_type == 'manufacturer' ? 'selected' : '' }}>{{ text_link_manufacturer }}</option>
                            <option value="information" {{ banner_image.link_type == 'information' ? 'selected' : '' }}>{{ text_link_information }}</option>
                            <option value="blog" {{ banner_image.link_type == 'blog' ? 'selected' : '' }}>{{ text_link_blog }}</option>
                          </select>
                          <div class="dc-link-custom"{{ banner_image.link_type is not empty and banner_image.link_type != 'custom' ? ' style="display:none;"' : '' }}>
                            <input type="text" class="form-control dc-link-custom-input" value="{{ banner_image.link }}" placeholder="{{ entry_link }}" />
                          </div>
                          <div class="dc-link-entity"{{ not banner_image.link_type or banner_image.link_type == 'custom' ? ' style="display:none;"' : '' }}>
                            <input type="text" class="form-control dc-link-autocomplete" value="{{ banner_image.link_entity_name }}" placeholder="{{ entry_link_search }}" data-type="{{ banner_image.link_type }}" data-value="{{ banner_image.link_value }}" />
                          </div>
                        </div>
                      </div>
                      <input type="hidden" name="banner_image[{{ language.language_id }}][{{ image_row }}][link]" value="{{ banner_image.link }}" class="dc-link-hidden" />
                    </div>

                    {# ── Badge ── #}
                    <div class="slide-section">
                      <div class="slide-section__title"><i data-lucide="tag" width="14" height="14"></i> {{ entry_accent_text }}</div>
                      <div class="btn-row-2col">
                        <div class="form-group">
                          <label>{{ entry_accent_text }}</label>
                          <input type="text" name="banner_image[{{ language.language_id }}][{{ image_row }}][accent_text]" value="{{ banner_image.accent_text }}" placeholder="{{ placeholder_accent_text }}" class="form-control" />
                        </div>
                        <div class="form-group">
                          <label>{{ entry_accent_color }}</label>
                          <div style="display:flex;gap:8px;align-items:center;">
                            <input type="text" name="banner_image[{{ language.language_id }}][{{ image_row }}][accent_color]" value="{{ banner_image.accent_color|default('#6366f1') }}" class="form-control dc-color-input accent-color-text" data-default-color="#6366f1" placeholder="#6366f1" style="flex:1;" maxlength="16" />
                          </div>
                          <div class="accent-hint">{{ entry_accent_hint }}</div>
                        </div>
                      </div>
                    </div>

                    {# ── Button ── #}
                    <div class="slide-section">
                      <div class="slide-section__title"><i data-lucide="mouse-pointer-click" width="14" height="14"></i> {{ entry_primary_btn_text }}</div>
                      <div class="form-group">
                        <input type="text" name="banner_image[{{ language.language_id }}][{{ image_row }}][primary_btn_text]" value="{{ banner_image.primary_btn_text }}" class="form-control" />
                      </div>
                    </div>

                  </div>
                </div>
                {% set image_row = image_row + 1 %}
                {% endfor %}
                {% else %}
                <div class="no-slides-msg" id="no-slides-{{ language.language_id }}"><i data-lucide="image" width="22" height="22" style="margin-bottom:8px;display:block;"></i>{{ text_no_slides }}</div>
                {% endif %}
              </div>

              <div class="add-slide-btn-wrap">
                <button type="button" onclick="addSlide({{ language.language_id }});" class="btn btn-primary btn-sm"><i data-lucide="plus-circle" width="14" height="14"></i> {{ button_add_slide }}</button>
              </div>

            </div>
            {% endfor %}
              </div>

        </div>{# /dcx-panel-body #}
      </div>{# /dcx-panel #}

      </div>{# /col #}
    </div>{# /row #}
    </form>
  </div>

  <script src="view/javascript/coloris.min.js"></script>
<script type="text/javascript">
var image_row = {{ image_row }};
var placeholder = '{{ placeholder }}';
var _dragCard = null;
var _dragSrcLangId = null;

function removeSlide(row) {
  $('#image-row' + row + ', .tooltip').not('.tooltip:not([id])').remove();
  $('#image-row' + row).remove();
}

function duplicateSlide(row, language_id) {
  var $original = $('#image-row' + row);
  if (!$original.length) return;

  var newRow = image_row;
  var $clone = $original.clone(true);

  $clone.attr('id', 'image-row' + newRow);

  $clone.find('[name]').each(function() {
    var name = $(this).attr('name');
    if (name) {
      $(this).attr('name', name.replace(/\[(\d+)\](?=\[[^\]]+\]$)/, '[' + newRow + ']'));
    }
  });

  $clone.find('[id]').each(function() {
    var id = $(this).attr('id');
    if (id) {
      $(this).attr('id', id.replace(/(\d+)(?!.*\d)/, newRow));
    }
  });

  $clone.find('[onclick]').each(function() {
    var onclick = $(this).attr('onclick');
    if (onclick) {
      $(this).attr('onclick', onclick.replace(/removeSlide\(\d+\)/g, 'removeSlide(' + newRow + ')').replace(/duplicateSlide\(\d+,(\d+)\)/g, 'duplicateSlide(' + newRow + ',$1)'));
    }
  });

  $clone.find('.dc-video-thumb__img').each(function() {
    var src = $(this).attr('src');
    var placeholder = $(this).attr('data-placeholder') || window.placeholder || '';
    if (src && src !== placeholder) {
      $(this).attr('src', src);
    }
  });

  $clone.attr('draggable', 'true');
  $clone.removeClass('collapsed');
  $clone.insertAfter($original);

  initColorInputs($clone);
  initVideoFields($clone);
  initLinkFields($clone);
  if (window.lucide && typeof window.lucide.createIcons === 'function') {
    window.lucide.createIcons();
  }

  image_row++;
  renumberSlides(language_id);
}

function renumberSlides(language_id) {
  var $list = $('#slide-list-' + language_id);
  var $cards = $list.children('.banner-slide-card');

  $cards.each(function(i) {
    var $card = $(this);
    var oldId = $card.attr('id');
    var oldRow = oldId ? oldId.replace('image-row', '') : '0';

    $card.attr('id', 'image-row' + i);

    $card.find('[name]').each(function() {
      var name = $(this).attr('name');
      if (name) {
        $(this).attr('name', name.replace(/\[\d+\](?=\[[^\]]+\]$)/, '[' + i + ']'));
      }
    });

    $card.find('[id]').each(function() {
      var id = $(this).attr('id');
      if (id) {
        $(this).attr('id', id.replace(/\d+(?!.*\d)/, i));
      }
    });

    $card.find('[onclick]').each(function() {
      var onclick = $(this).attr('onclick');
      if (onclick) {
        $(this).attr('onclick', onclick.replace(/removeSlide\(\d+\)/g, 'removeSlide(' + i + ')').replace(/duplicateSlide\(\d+,(\d+)\)/g, 'duplicateSlide(' + i + ',$1)'));
      }
    });

    var $title = $card.find('.slide-title-input');
    var title = $title.length ? $title.val().trim() : '';
    var fallback = '{{ text_slide }} #' + (i + 1);
    $card.find('.slide-label').text(title || fallback);

    var $sortInput = $card.find('[name$="[sort_order]"]');
    if ($sortInput.length) {
      $sortInput.val(i);
    }
  });
}

function updateSlideLabel($input) {
  var $card = $input.closest('.banner-slide-card');
  var title = $input.val().trim();
  var idx = $card.index() + 1;
  var fallback = '{{ text_slide }} #' + idx;
  $card.find('.slide-label').text(title || fallback);
}

function changeSlideLanguage($card, newLangId) {
  $card.find('[name]').each(function() {
    var name = $(this).attr('name');
    if (name) {
      $(this).attr('name', name.replace(/banner_image\[\d+\]/, 'banner_image[' + newLangId + ']'));
    }
  });
  $card.find('[onclick]').each(function() {
    var onclick = $(this).attr('onclick');
    if (onclick) {
      $(this).attr('onclick', onclick.replace(
        /duplicateSlide\(\d+,\s*\d+\)/g,
        'duplicateSlide($1,' + newLangId + ')'
      ));
    }
  });
}

function normalizeHexColor(value, fallback) {
  var val = String(value || '').trim();
  var defaultColor = fallback || '#000000';

  if (/^[0-9a-fA-F]{6}$/.test(val)) {
    return '#' + val.toLowerCase();
  }

  if (/^#[0-9a-fA-F]{3}$/.test(val)) {
    return '#' + val[1] + val[1] + val[2] + val[2] + val[3] + val[3];
  }

  if (/^#[0-9a-fA-F]{6}$/.test(val)) {
    return val.toLowerCase();
  }

  return defaultColor;
}

function initColorInputs(container) {
  container.find('.dc-color-input').each(function() {
    var $input = $(this);
    var fallback = $input.data('default-color') || '#000000';

    $input.val(normalizeHexColor($input.val(), fallback));
    $input.off('blur.dcColor').on('blur.dcColor', function() {
      $input.val(normalizeHexColor($input.val(), fallback));
    });
  });
}

function initColorisPicker() {
  if (typeof Coloris === 'undefined') {
    return;
  }

  Coloris({
    el: '.dc-color-input',
    alpha: false,
    format: 'hex',
    formatToggle: false,
    swatches: [
      '#6366f1',
      '#2563eb',
      '#ffffff',
      '#111827',
      '#f59e0b',
      '#10b981',
      '#ef4444'
    ]
  });
}

function addSlide(language_id) {
  var row = image_row;
  var pfx = 'banner_image[' + language_id + '][' + row + ']';
  var html = '<div class="banner-slide-card collapsed" draggable="true" id="image-row' + row + '">';
  html += '<div class="banner-slide-card__head" style="cursor:pointer;">';
  html += '<span>';
  html += '<i data-lucide="grip-vertical" width="14" height="14" class="slide-drag-handle"></i> ';
  html += '<i data-lucide="chevron-right" width="14" height="14" class="banner-slide-card__toggle"></i> ';
  html += '<span class="banner-slide-card__num"><i data-lucide="image" width="14" height="14" style="margin-right:6px;"></i><span class="slide-label">{{ text_slide }} #' + row + '</span></span></span>';
  html += '<span>';
  html += '<button type="button" onclick="duplicateSlide(' + row + ',' + language_id + '); event.stopPropagation();" class="btn btn-default btn-xs btn-duplicate"><i data-lucide="copy" width="14" height="14"></i></button> ';
  html += '<button type="button" onclick="removeSlide(' + row + '); event.stopPropagation();" class="btn btn-danger btn-xs"><i data-lucide="trash-2" width="14" height="14"></i> {{ button_remove }}</button>';
  html += '</span>';
  html += '</div>';
  html += '<div class="banner-slide-card__body">';

  // Content
  html += '<div class="slide-section">';
  html += '<div class="slide-section__title"><i data-lucide="pencil" width="14" height="14"></i> {{ text_tab_general }}</div>';
  html += '<div class="btn-row-2col">';
  html += '<div class="form-group"><label>{{ entry_title }}</label><input type="text" name="' + pfx + '[title]" value="" placeholder="{{ entry_title }}" class="form-control slide-title-input" /><div class="accent-hint">{{ entry_title_hint }}</div></div>';
  html += '<div class="form-group"><label>{{ entry_subtitle }}</label><input type="text" name="' + pfx + '[subtitle]" value="" placeholder="{{ entry_subtitle }}" class="form-control" /><div class="accent-hint accent-hint--spacer">&nbsp;</div></div>';
  html += '</div>';
  html += '<div class="form-group"><label>{{ entry_content_position }}</label>';
  html += '<div class="position-radio-group">';
  html += '<label><input type="radio" name="' + pfx + '[content_position]" value="left" checked /><i data-lucide="align-left" width="14" height="14"></i> {{ text_content_position_left }}</label>';
  html += '<label><input type="radio" name="' + pfx + '[content_position]" value="center" /><i data-lucide="align-center" width="14" height="14"></i> {{ text_content_position_center }}</label>';
  html += '<label><input type="radio" name="' + pfx + '[content_position]" value="right" /><i data-lucide="align-right" width="14" height="14"></i> {{ text_content_position_right }}</label>';
  html += '</div></div>';
  html += '</div>';

  // Media
  html += '<div class="slide-section">';
  html += '<div class="slide-section__title"><i data-lucide="image" width="14" height="14"></i> {{ entry_image }}</div>';
  html += '<div class="form-group">';
  html += '<div class="slide-images-row">';
  html += '<div class="slide-image-block">';
  html += '<a href="" id="thumb-image' + row + '" data-toggle="image" class="img-thumbnail"><img src="{{ placeholder }}" alt="" title="" data-placeholder="{{ placeholder }}" style="max-height:90px;" /></a>';
  html += '<input type="hidden" name="' + pfx + '[image]" value="" id="input-image' + row + '" />';
  html += '<small>{{ text_landscape }}</small>';
  html += '</div>';
  html += '<div class="slide-image-block">';
  html += '<a href="" id="thumb-image-portrait' + row + '" data-toggle="image" class="img-thumbnail"><img src="{{ placeholder }}" alt="" title="" data-placeholder="{{ placeholder }}" style="max-height:90px;" /></a>';
  html += '<input type="hidden" name="' + pfx + '[image_portrait]" value="" id="input-image-portrait' + row + '" />';
  html += '<small>{{ text_portrait }}</small>';
  html += '</div>';
  html += '</div>';
  html += '</div>';
  html += '</div>';

  // Video
  html += '<div class="slide-section">';
  html += '<div class="slide-section__title"><i data-lucide="video" width="14" height="14"></i> {{ entry_video }}</div>';
  html += '<div class="form-group">';
  html += '<label>{{ entry_video_youtube }}</label>';
  html += '<div class="input-group" style="max-width:400px;">';
  html += '<span class="input-group-addon"><i data-lucide="youtube" width="14" height="14"></i></span>';
  html += '<input type="text" name="' + pfx + '[video_youtube]" value="" class="form-control dc-banner-video-youtube" placeholder="{{ entry_video_youtube_placeholder }}" />';
  html += '</div>';
  html += '<div class="dc-banner-youtube-preview" style="margin-top:8px;display:none;"></div>';
  html += '</div>';
  html += '<div class="form-group">';
  html += '<label>{{ entry_video_mp4 }}</label>';
  html += '<div class="slide-video-block">';
  html += '<a href="" id="thumb-video' + row + '" data-toggle="image" data-type="video" class="img-thumbnail dc-video-thumb" style="width:130px;height:100px;display:flex;align-items:center;justify-content:center;overflow:hidden;">';
  html += '<img src="{{ video_placeholder }}" alt="" title="" data-placeholder="{{ video_placeholder }}" class="dc-video-thumb__img" style="max-height:90px;" />';
  html += '<div class="dc-video-thumb__overlay" style="display:none;position:absolute;inset:0;flex-direction:column;align-items:center;justify-content:center;background:#1f2937;border-radius:5px;gap:4px;">';
  html += '<i data-lucide="film" class="dc-video-thumb__icon" style="width:20px;height:20px;stroke:#9ca3af;"></i>';
  html += '<span class="dc-video-thumb__name" style="font-size:10px;color:#9ca3af;word-break:break-all;text-align:center;"></span>';
  html += '</div>';
  html += '</a>';
  html += '<input type="hidden" name="' + pfx + '[video_mp4]" value="" id="input-video' + row + '" />';
  html += '<small>{{ text_mp4 }}</small>';
  html += '</div>';
  html += '</div>';
  html += '</div>';

  // Link
  html += '<div class="slide-section">';
  html += '<div class="slide-section__title"><i data-lucide="link" width="14" height="14"></i> {{ entry_link }}</div>';
  html += '<div class="form-group">';
  html += '<label>{{ entry_link }}</label>';
  html += '<div class="dc-link-row">';
  html += '<select class="form-control dc-link-type" style="max-width:200px;">';
  html += '<option value="custom">{{ text_link_custom }}</option>';
  html += '<option value="product">{{ text_link_product }}</option>';
  html += '<option value="category">{{ text_link_category }}</option>';
  html += '<option value="manufacturer">{{ text_link_manufacturer }}</option>';
  html += '<option value="information">{{ text_link_information }}</option>';
  html += '<option value="blog">{{ text_link_blog }}</option>';
  html += '</select>';
  html += '<div class="dc-link-custom"><input type="text" class="form-control dc-link-custom-input" value="" placeholder="{{ entry_link }}" /></div>';
  html += '<div class="dc-link-entity" style="display:none;"><input type="text" class="form-control dc-link-autocomplete" value="" placeholder="{{ entry_link_search }}" data-type="" data-value="" /></div>';
  html += '</div>';
  html += '</div>';
  html += '<input type="hidden" name="' + pfx + '[link]" value="" class="dc-link-hidden" />';
  html += '</div>';

  // Badge
  html += '<div class="slide-section">';
  html += '<div class="slide-section__title"><i data-lucide="tag" width="14" height="14"></i> {{ entry_accent_text }}</div>';
  html += '<div class="btn-row-2col">';
  html += '<div class="form-group"><label>{{ entry_accent_text }}</label><input type="text" name="' + pfx + '[accent_text]" value="" placeholder="{{ placeholder_accent_text }}" class="form-control" /></div>';
  html += '<div class="form-group"><label>{{ entry_accent_color }}</label><div style="display:flex;gap:8px;align-items:center;"><input type="text" name="' + pfx + '[accent_color]" value="#6366f1" class="form-control dc-color-input accent-color-text" data-default-color="#6366f1" placeholder="#6366f1" style="flex:1;" maxlength="16" /></div><div class="accent-hint">{{ entry_accent_hint }}</div></div>';
  html += '</div>';
  html += '</div>';

  // Button
  html += '<div class="slide-section">';
  html += '<div class="slide-section__title"><i data-lucide="mouse-pointer-click" width="14" height="14"></i> {{ entry_primary_btn_text }}</div>';
  html += '<div class="form-group"><input type="text" name="' + pfx + '[primary_btn_text]" value="" class="form-control" /></div>';
  html += '</div>';

  html += '</div></div>';

  $('#no-slides-' + language_id).hide();
  var $card = $(html);
  $('#slide-list-' + language_id).append($card);
  initColorInputs($card);
  initLinkFields($card);
  initVideoFields($card);
  if (window.lucide && typeof window.lucide.createIcons === 'function') {
    window.lucide.createIcons();
  }

  image_row++;
}

function updateMp4Preview($block) {
  var $a = $block.find('.dc-video-thumb');
  if (!$a.length) return;
  var $img = $a.find('.dc-video-thumb__img');
  var $overlay = $a.find('.dc-video-thumb__overlay');
  var $input = $block.find('input[type="hidden"]');
  var videoVal = $input.val();

  if (videoVal) {
    $img.hide();
    $overlay.show();
    $overlay.find('.dc-video-thumb__name').text(videoVal);
  } else {
    $img.show();
    $overlay.hide();
    $img.attr('src', $img.attr('data-placeholder'));
  }
}

function dcExtractYouTubeId(value) {
  value = (value || '').trim();
  if (/^[A-Za-z0-9_-]{11}$/.test(value)) {
    return value;
  }
  var patterns = [
    /(?:youtube\.com|youtu\.be|youtube-nocookie\.com)\/(?:watch\?v=|embed\/|shorts\/)?([A-Za-z0-9_-]{11})/,
    /^([A-Za-z0-9_-]{11})$/
  ];
  for (var i = 0; i < patterns.length; i++) {
    var m = value.match(patterns[i]);
    if (m) {
      return m[1];
    }
  }
  return '';
}

function updateYoutubePreview($input) {
  var val = $input.val().trim();
  var $preview = $input.closest('.form-group').find('.dc-banner-youtube-preview');
  if (val) {
    var ytId = dcExtractYouTubeId(val);
    if (ytId) {
      $preview.find('.dc-banner-youtube-thumb').attr('src', 'https://img.youtube.com/vi/' + ytId + '/hqdefault.jpg');
    } else {
      $preview.find('.dc-banner-youtube-thumb').attr('src', $preview.find('.dc-banner-youtube-thumb').attr('data-placeholder') || '');
    }
    $preview.show();
  } else {
    $preview.hide();
  }
}

function updateAllVideoPreviews() {
  $('.slide-video-block').each(function() {
    updateMp4Preview($(this));
  });
  $('.dc-banner-video-youtube').each(function() {
    updateYoutubePreview($(this));
  });
}

function initVideoFields(container) {
  container.find('.dc-banner-video-youtube').each(function() {
    updateYoutubePreview($(this));
  });
  container.find('.slide-video-block').each(function() {
    updateMp4Preview($(this));
  });
}

$(document).ready(function() {
  initColorisPicker();

  $(document).on('click', '.banner-slide-card__head', function() {
    $(this).closest('.banner-slide-card').toggleClass('collapsed');
  });

  $(document).on('input', '.dc-banner-video-youtube', function() {
    var $section = $(this).closest('.slide-section');
    $section.find('.slide-video-block input[type="hidden"]').val('');
    $section.find('.dc-video-thumb__img').show();
    $section.find('.dc-video-thumb__overlay').hide();
    updateYoutubePreview($(this));
  });

  $(document).on('input', '.slide-video-block input[type="hidden"]', function() {
    var $section = $(this).closest('.slide-section');
    $section.find('.dc-banner-video-youtube').val('');
    $section.find('.dc-banner-youtube-preview').hide();
    updateMp4Preview($(this).closest('.slide-video-block'));
  });

  $(document).on('hidden.bs.modal', '#modal-image', function() {
    updateAllVideoPreviews();
  });

  $(document).on('click', '#button-clear', function() {
    setTimeout(function() {
      updateAllVideoPreviews();
    }, 100);
  });

  $('.banner-slide-card').each(function() {
    initColorInputs($(this));
    DcLinkField.init($(this), { hiddenClass: null });
  });

  $(document).on('dragstart', '.banner-slide-card', function(e) {
    var $card = $(this);
    if (!$card.hasClass('collapsed')) {
      e.preventDefault();
      return;
    }
    if (!$(e.target).closest('.slide-drag-handle').length && e.target !== this) {
      e.preventDefault();
      return;
    }
    var $list = $card.closest('[id^="slide-list-"]');
    $card.addClass('dragging');
    _dragCard = $card;
    _dragSrcLangId = $list.attr('id').replace('slide-list-', '');
    e.originalEvent.dataTransfer.effectAllowed = 'move';
    e.originalEvent.dataTransfer.setData('text/plain', '');
  });

  $(document).on('dragover', '.banner-slide-card', function(e) {
    e.preventDefault();
    e.originalEvent.dataTransfer.dropEffect = 'move';
    var $target = $(this);
    var $list = $target.closest('[id^="slide-list-"]');
    $list.find('.banner-slide-card').not('.dragging').removeClass('drag-over');
    if (!$target.hasClass('dragging')) {
      $target.addClass('drag-over');
    }
  });

  $(document).on('drop', '.banner-slide-card', function(e) {
    e.preventDefault();
    var $target = $(this);
    var $list = $target.closest('[id^="slide-list-"]');
    if (!_dragCard || _dragCard.is($target)) return;
    if ($list.attr('id') !== 'slide-list-' + _dragSrcLangId) return;

    var targetIndex = $target.index();
    var draggedIndex = _dragCard.index();

    if (targetIndex < draggedIndex) {
      _dragCard.insertBefore($target);
    } else {
      _dragCard.insertAfter($target);
    }

    renumberSlides(_dragSrcLangId);
    _dragCard = null;
    _dragSrcLangId = null;
  });

  $(document).on('dragend', '.banner-slide-card', function() {
    $(this).removeClass('dragging');
    $('[id^="slide-list-"]').find('.banner-slide-card').removeClass('drag-over');
    $('#language li').removeClass('drag-over-tab');
    _dragCard = null;
    _dragSrcLangId = null;
  });

  $(document).on('dragover', '#language li', function(e) {
    if (!_dragCard) return;
    e.preventDefault();
    var targetLangId = $(this).find('a').attr('href').replace('#language', '');
    if (targetLangId !== _dragSrcLangId) {
      $(this).addClass('drag-over-tab');
    }
  });

  $(document).on('dragleave', '#language li', function() {
    $(this).removeClass('drag-over-tab');
  });

  $(document).on('drop', '#language li a', function(e) {
    e.preventDefault();
    e.stopPropagation();
    $(this).closest('li').removeClass('drag-over-tab');

    var targetLangId = $(this).attr('href').replace('#language', '');
    if (!_dragCard || targetLangId === _dragSrcLangId) return;

    var $targetList = $('#slide-list-' + targetLangId);
    $('#no-slides-' + targetLangId).hide();
    $targetList.append(_dragCard);

    changeSlideLanguage(_dragCard, targetLangId);

    $(this).tab('show');

    renumberSlides(_dragSrcLangId);
    renumberSlides(targetLangId);

    _dragCard = null;
    _dragSrcLangId = null;
  });

  $(document).on('input', '.slide-title-input', function() {
    updateSlideLabel($(this));
  });

  $('#form-banner').on('submit', function() {
    $('[id^="slide-list-"]').each(function() {
      $(this).children('.banner-slide-card').each(function(i) {
        var $card = $(this);
        var $sortInput = $card.find('[name$="[sort_order]"]');
        if (!$sortInput.length) {
          var $firstInput = $card.find('[name^="banner_image["]');
          if ($firstInput.length) {
            var baseName = $firstInput.attr('name').replace(/\[[^\]]+\]$/, '');
            $sortInput = $('<input type="hidden">').attr('name', baseName + '[sort_order]');
            $card.append($sortInput);
          }
        }
        if ($sortInput.length) {
          $sortInput.val(i);
        }
      });
    });
  });

  $('#language a:first').tab('show');

});
</script>

</div>
{{ footer }}
