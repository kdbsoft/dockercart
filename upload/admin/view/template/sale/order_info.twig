{{ header }}{{ column_left }}
<div id="content">
  <div class="page-header">
    <div class="container-fluid">
      <div class="pull-right"><a href="{{ invoice }}" target="_blank" data-toggle="tooltip" title="{{ button_invoice_print }}" class="btn btn-info"><i class="fa fa-print"></i></a> <a href="{{ shipping }}" target="_blank" data-toggle="tooltip" title="{{ button_shipping_print }}" class="btn btn-info"><i class="fa fa-truck"></i></a> <a href="{{ cancel }}" data-toggle="tooltip" title="{{ button_cancel }}" class="btn btn-default"><i class="fa fa-reply"></i></a></div>
      <h1>{{ heading_title }} {% if order_status %}<span class="order-status-badge {{ status_badge_class }}">{{ order_status }}</span>{% endif %}</h1>
      <ul class="breadcrumb">
        {% for breadcrumb in breadcrumbs %}
          <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
        {% endfor %}
      </ul>
    </div>
  </div>
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-8">

        <div class="order-address-row">
          <div class="order-address-card">
            <div class="order-address-card__header">
              <h4 class="order-address-card__title">{{ text_payment_address }}</h4>
              <button type="button" class="btn btn-default btn-xs btn-quick-edit" data-field="payment_address" data-title="{{ text_payment_address }}" data-target="#quick-payment-address"><i class="fa fa-pencil"></i></button>
            </div>
            <div class="order-address-card__body" id="quick-payment-address">{{ payment_address }}</div>
          </div>
          {% if shipping_method %}
          <div class="order-address-card">
            <div class="order-address-card__header">
              <h4 class="order-address-card__title">{{ text_shipping_address }}</h4>
              <button type="button" class="btn btn-default btn-xs btn-quick-edit" data-field="shipping_address" data-title="{{ text_shipping_address }}" data-target="#quick-shipping-address"><i class="fa fa-pencil"></i></button>
            </div>
            <div class="order-address-card__body" id="quick-shipping-address">{{ shipping_address }}</div>
          </div>
          {% endif %}
        </div>

        <div class="panel panel-default">
          <div class="panel-heading">
            <div class="panel-heading__icon"><i class="fa fa-info-circle"></i></div>
            <div>
              <h3 class="panel-title">{{ text_panel_order_contents }}</h3>
              <div class="panel-heading__subtitle">{{ text_order_contents_subtitle }}</div>
            </div>
          </div>
          <div class="panel-body">
            <div class="table-responsive">
            <table class="order-product-table">
              <thead>
                <tr>
                  <th class="text-left">{{ column_product }}</th>
                  <th class="text-left">{{ column_model }}</th>
                  <th class="text-right">{{ column_quantity }}</th>
                  <th class="text-right">{{ column_discount_percent }}</th>
                  <th class="text-right">{{ column_price }}</th>
                  <th class="text-right">{{ column_total }}</th>
                  <th class="text-right"></th>
                </tr>
              </thead>
              <tbody id="inline-cart">
                {% set inline_product_row = 0 %}
                {% for product in products %}
                <tr>
                  <td class="text-left order-product-name">
                    {% if product.thumb %}
                    <a href="#" class="order-product-thumb btn-product-info" data-product-id="{{ product.product_id }}"><img src="{{ product.thumb }}" alt="{{ product.name|e('html_attr') }}" /></a>
                    {% endif %}
                    <a href="#" class="btn-product-info" data-product-id="{{ product.product_id }}">{{ product.name }}</a>
                    <input type="hidden" name="product[{{ inline_product_row }}][product_id]" value="{{ product.product_id }}" />
                    {% for option in product.option %}
                    <div class="order-product-option">- {{ option.name }}: {{ option.value }}</div>
                    {% if option.type == 'select' or option.type == 'radio' or option.type == 'image' %}
                    <input type="hidden" name="product[{{ inline_product_row }}][option][{{ option.product_option_id }}]" value="{{ option.product_option_value_id }}" />
                    {% endif %}
                    {% if option.type == 'checkbox' %}
                    <input type="hidden" name="product[{{ inline_product_row }}][option][{{ option.product_option_id }}][]" value="{{ option.product_option_value_id }}" />
                    {% endif %}
                    {% if option.type == 'text' or option.type == 'textarea' or option.type == 'file' or option.type == 'date' or option.type == 'datetime' or option.type == 'time' %}
                    <input type="hidden" name="product[{{ inline_product_row }}][option][{{ option.product_option_id }}]" value="{{ (option.raw_value is defined ? option.raw_value : option.value)|e('html_attr') }}" />
                    {% endif %}
                    {% endfor %}
                  </td>
                  <td class="text-left">{{ product.model }}</td>
                  <td class="text-right"><input type="text" name="product[{{ inline_product_row }}][quantity]" value="{{ product.quantity }}" class="order-product-input gt-no-translate" data-gt-inline-ignore="1" /></td>
                  <td class="text-right"><input type="text" name="product[{{ inline_product_row }}][discount]" value="{{ product.discount_percent is defined ? product.discount_percent : 0 }}" class="order-product-input gt-no-translate" data-gt-inline-ignore="1" /></td>
                  <td class="text-right">{{ product.price }}</td>
                  <td class="text-right">{{ product.total }}</td>
                  <td class="text-right"><button type="button" class="btn btn-danger btn-xs button-inline-remove-item"><i class="fa fa-trash"></i></button></td>
                </tr>
                {% set inline_product_row = inline_product_row + 1 %}
                {% endfor %}
                {% if not products %}
                <tr class="inline-cart-empty">
                  <td colspan="7" class="text-center">{{ text_no_results }}</td>
                </tr>
                {% endif %}
              </tbody>
              <tfoot id="inline-totals">
                {% for total in totals %}
                <tr>
                  <td colspan="5" class="text-right">{{ total.title }}</td>
                  <td class="text-right">{{ total.text }}</td>
                  <td></td>
                </tr>
                {% endfor %}
              </tfoot>
            </table>
            </div>

            <div class="order-product-add-row">
              <div class="order-product-add-field order-product-add-field--product">
                <label class="control-label" for="input-inline-product">{{ entry_product }}</label>
                <input type="text" id="input-inline-product" class="form-control" placeholder="{{ entry_product }}" />
                <input type="hidden" id="input-inline-product-id" value="" />
              </div>
              <div class="order-product-add-field order-product-add-field--quantity">
                <label class="control-label" for="input-inline-quantity">{{ entry_quantity }}</label>
                <input type="text" id="input-inline-quantity" value="1" class="form-control order-product-input gt-no-translate" data-gt-inline-ignore="1" />
              </div>
              <div class="order-product-add-field order-product-add-field--action">
                <label class="control-label">&nbsp;</label>
                <button type="button" id="button-inline-add-product" class="btn btn-primary"><i class="fa fa-plus"></i> {{ button_product_add }}</button>
              </div>
            </div>

            <div class="row">
              <div class="col-sm-6">
                <div class="form-group">
                  <label class="control-label" for="input-inline-coupon">{{ entry_coupon }}</label>
                  <div class="input-group">
                    <input type="text" id="input-inline-coupon" value="" class="form-control" placeholder="{{ entry_coupon }}" />
                    <span class="input-group-btn"><button type="button" id="button-inline-apply-coupon" class="btn btn-warning"><i class="fa fa-ticket"></i> {{ button_apply }}</button></span>
                  </div>
                </div>
              </div>
            </div>

            <div class="text-right" style="padding-top:16px;border-top:1px solid #e5e7eb;margin-top:4px">
              <button type="button" id="button-inline-save-items" data-loading-text="{{ text_loading }}" class="btn btn-success"><i class="fa fa-check-circle"></i> {{ button_save }}</button>
            </div>
          </div>
        </div>

      </div>
      <div class="col-md-4">
        <div class="order-sidebar">

          <div class="order-sidebar-card">
            <div class="order-sidebar-card__header">
              <div class="order-sidebar-card__icon order-sidebar-card__icon--blue"><i class="fa fa-user"></i></div>
              <h3 class="order-sidebar-card__title">{{ text_panel_customer }}</h3>
            </div>
            <div class="order-sidebar-card__body">
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-user"></i></span>
                <span class="order-sidebar-row__label">{{ text_customer }}</span>
                <span class="order-sidebar-row__value" id="quick-customer-name">{% if customer %}<a href="{{ customer }}" target="_blank">{{ firstname }} {{ lastname }}</a>{% else %}{{ firstname }} {{ lastname }}{% endif %}</span>
                <span class="order-sidebar-row__actions"><button type="button" class="btn btn-default btn-xs btn-quick-edit" data-field="customer_name" data-title="{{ text_customer }}" data-firstname="{{ firstname|e('html_attr') }}" data-lastname="{{ lastname|e('html_attr') }}" data-target="#quick-customer-name"><i class="fa fa-pencil"></i></button></span>
              </div>
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-group"></i></span>
                <span class="order-sidebar-row__label">{{ text_customer_group }}</span>
                <span class="order-sidebar-row__value">{{ customer_group }}</span>
              </div>
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-envelope-o"></i></span>
                <span class="order-sidebar-row__label">{{ text_email }}</span>
                <span class="order-sidebar-row__value" id="quick-email"><a href="mailto:{{ email }}">{{ email }}</a></span>
                <span class="order-sidebar-row__actions"><button type="button" class="btn btn-default btn-xs btn-quick-edit" data-field="email" data-title="{{ text_email }}" data-value="{{ email|e('html_attr') }}" data-target="#quick-email"><i class="fa fa-pencil"></i></button></span>
              </div>
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-phone"></i></span>
                <span class="order-sidebar-row__label">{{ text_telephone }}</span>
                <span class="order-sidebar-row__value" id="quick-telephone">{{ telephone }}</span>
                <span class="order-sidebar-row__actions"><button type="button" class="btn btn-default btn-xs btn-quick-edit" data-field="telephone" data-title="{{ text_telephone }}" data-value="{{ telephone|e('html_attr') }}" data-target="#quick-telephone"><i class="fa fa-pencil"></i></button></span>
              </div>
              {% if tax_number %}
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-id-card-o"></i></span>
                <span class="order-sidebar-row__label">{{ text_tax_number }}</span>
                <span class="order-sidebar-row__value" id="quick-tax-number">{{ tax_number }}</span>
                <span class="order-sidebar-row__actions"><button type="button" class="btn btn-default btn-xs btn-quick-edit" data-field="tax_number" data-title="{{ text_tax_number }}" data-value="{{ tax_number|e('html_attr') }}" data-target="#quick-tax-number"><i class="fa fa-pencil"></i></button></span>
              </div>
              {% endif %}
            </div>
          </div>

          <div class="order-sidebar-card">
            <div class="order-sidebar-card__header">
              <div class="order-sidebar-card__icon order-sidebar-card__icon--green"><i class="fa fa-credit-card"></i></div>
              <h3 class="order-sidebar-card__title">{{ text_panel_order_info }}</h3>
            </div>
            <div class="order-sidebar-card__body">
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-shopping-cart"></i></span>
                <span class="order-sidebar-row__label">{{ text_store }}</span>
                <span class="order-sidebar-row__value"><a href="{{ store_url }}" target="_blank">{{ store_name }}</a></span>
              </div>
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-calendar"></i></span>
                <span class="order-sidebar-row__label">{{ text_date_added }}</span>
                <span class="order-sidebar-row__value">{{ date_added }}</span>
              </div>
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-credit-card"></i></span>
                <span class="order-sidebar-row__label">{{ text_payment_method }}</span>
                <span class="order-sidebar-row__value" id="quick-payment-method">{{ payment_method }}</span>
                <span class="order-sidebar-row__actions"><button type="button" class="btn btn-default btn-xs btn-quick-edit" data-field="payment_method" data-title="{{ text_payment_method }}" data-value="{{ payment_method|e('html_attr') }}" data-code="{{ payment_code|e('html_attr') }}" data-target="#quick-payment-method"><i class="fa fa-pencil"></i></button></span>
              </div>
              {% if shipping_method %}
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-truck"></i></span>
                <span class="order-sidebar-row__label">{{ text_shipping_method }}</span>
                <span class="order-sidebar-row__value" id="quick-shipping-method">{{ shipping_method }}</span>
                <span class="order-sidebar-row__actions"><button type="button" class="btn btn-default btn-xs btn-quick-edit" data-field="shipping_method" data-title="{{ text_shipping_method }}" data-value="{{ shipping_method|e('html_attr') }}" data-code="{{ shipping_code|e('html_attr') }}" data-target="#quick-shipping-method"><i class="fa fa-pencil"></i></button></span>
              </div>
              {% endif %}
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-map-marker"></i></span>
                <span class="order-sidebar-row__label">{{ text_tracking_number }}</span>
                <span class="order-sidebar-row__value" id="quick-tracking-number">{{ tracking_number }}</span>
                <span class="order-sidebar-row__actions"><button type="button" class="btn btn-default btn-xs btn-quick-edit" data-field="tracking_number" data-title="{{ text_tracking_number }}" data-value="{{ tracking_number|e('html_attr') }}" data-target="#quick-tracking-number"><i class="fa fa-pencil"></i></button></span>
              </div>
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-comment"></i></span>
                <span class="order-sidebar-row__label">{{ text_comment }}</span>
                <span class="order-sidebar-row__value" id="quick-comment">{% if comment %}{{ comment|raw }}{% else %}<span class="text-muted">{{ text_none }}</span>{% endif %}</span>
                <span class="order-sidebar-row__actions"><button type="button" class="btn btn-default btn-xs btn-quick-edit" data-field="comment" data-title="{{ entry_comment }}" data-value="{{ order_comment_raw|e('html_attr') }}" data-target="#quick-comment"><i class="fa fa-pencil"></i></button></span>
              </div>
            </div>
          </div>

          <div class="order-sidebar-card">
            <div class="order-sidebar-card__header">
              <div class="order-sidebar-card__icon order-sidebar-card__icon--amber"><i class="fa fa-cog"></i></div>
              <h3 class="order-sidebar-card__title">{{ text_panel_order_tools }}</h3>
            </div>
            <div class="order-sidebar-card__body">
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-file-text-o"></i></span>
                <span class="order-sidebar-row__label">{{ text_invoice }}</span>
                <span class="order-sidebar-row__value" id="invoice">{{ invoice_no }}</span>
                <span class="order-sidebar-row__actions">
                  {% if not invoice_no %}
                  <button id="button-invoice" data-loading-text="{{ text_loading }}" data-toggle="tooltip" title="{{ button_generate }}" class="btn btn-success btn-xs"><i class="fa fa-cog"></i></button>
                  {% else %}
                  <button disabled="disabled" class="btn btn-success btn-xs"><i class="fa fa-refresh"></i></button>
                  {% endif %}
                </span>
              </div>
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-star"></i></span>
                <span class="order-sidebar-row__label">{{ text_reward }}</span>
                <span class="order-sidebar-row__value">{{ reward }}</span>
                <span class="order-sidebar-row__actions">
                  {% if customer and reward %}
                    {% if not reward_total %}
                    <button id="button-reward-add" data-loading-text="{{ text_loading }}" data-toggle="tooltip" title="{{ button_reward_add }}" class="btn btn-success btn-xs"><i class="fa fa-plus-circle"></i></button>
                    {% else %}
                    <button id="button-reward-remove" data-loading-text="{{ text_loading }}" data-toggle="tooltip" title="{{ button_reward_remove }}" class="btn btn-danger btn-xs"><i class="fa fa-minus-circle"></i></button>
                    {% endif %}
                  {% else %}
                  <button disabled="disabled" class="btn btn-success btn-xs"><i class="fa fa-plus-circle"></i></button>
                  {% endif %}
                </span>
              </div>
              <div class="order-sidebar-row">
                <span class="order-sidebar-row__icon"><i class="fa fa-users"></i></span>
                <span class="order-sidebar-row__label">{{ text_affiliate }}</span>
                <span class="order-sidebar-row__value">{{ commission }}{% if affiliate %} ({{ affiliate_firstname }} {{ affiliate_lastname }}){% endif %}</span>
                <span class="order-sidebar-row__actions">
                  {% if affiliate %}
                    {% if not commission_total %}
                    <button id="button-commission-add" data-loading-text="{{ text_loading }}" data-toggle="tooltip" title="{{ button_commission_add }}" class="btn btn-success btn-xs"><i class="fa fa-plus-circle"></i></button>
                    {% else %}
                    <button id="button-commission-remove" data-loading-text="{{ text_loading }}" data-toggle="tooltip" title="{{ button_commission_remove }}" class="btn btn-danger btn-xs"><i class="fa fa-minus-circle"></i></button>
                    {% endif %}
                  {% else %}
                  <button disabled="disabled" class="btn btn-success btn-xs"><i class="fa fa-plus-circle"></i></button>
                  {% endif %}
                </span>
              </div>
            </div>
          </div>

        </div>
      </div>
    </div>
    <div class="panel panel-default" style="margin-top:20px">
      <div class="panel-heading">
  <div class="panel-heading__icon"><i class="fa fa-comment-o"></i></div>
  <div>
    <h3 class="panel-title">{{ text_history }}</h3>
    <div class="panel-heading__subtitle">{{ text_history_subtitle }}</div>
  </div>
</div>
      <div class="panel-body">
        <ul class="nav nav-tabs">
          <li class="active"><a href="#tab-history" data-toggle="tab"><i class="fa fa-history"></i> {{ tab_history }}</a></li>
          <li><a href="#tab-additional" data-toggle="tab"><i class="fa fa-plus-circle"></i> {{ tab_additional }}</a></li>
          {% for tab in tabs %}
            <li><a href="#tab-{{ tab.code }}" data-toggle="tab">{{ tab.title }}</a></li>
          {% endfor %}
        </ul>
        <div class="tab-content">
          <div class="tab-pane active" id="tab-history">
            <div id="history"></div>
            <br/>
            <fieldset>
              <legend>{{ text_history_add }}</legend>
              <form class="form-horizontal">
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-order-status">{{ entry_order_status }}</label>
                  <div class="col-sm-10">
                    <select name="order_status_id" id="input-order-status" class="form-control">
                      {% for order_statuses in order_statuses %}
                        {% if order_statuses.order_status_id == order_status_id %}
                          <option value="{{ order_statuses.order_status_id }}" selected="selected">{{ order_statuses.name }}</option>
                        {% else %}
                          <option value="{{ order_statuses.order_status_id }}">{{ order_statuses.name }}</option>
                        {% endif %}
                      {% endfor %}
                    </select>
                  </div>
                </div>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-override"><span data-toggle="tooltip" title="{{ help_override }}">{{ entry_override }}</span></label>
                  <div class="col-sm-10">
                    <div class="checkbox">
                      <input type="checkbox" name="override" value="1" id="input-override"/>
                    </div>
                  </div>
                </div>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-notify">{{ entry_notify }}</label>
                  <div class="col-sm-10">
                    <div class="checkbox">
                      <input type="checkbox" name="notify" value="1" id="input-notify"/>
                    </div>
                  </div>
                </div>
                <div class="form-group">
                  <label class="col-sm-2 control-label" for="input-comment">{{ entry_comment }}</label>
                  <div class="col-sm-10">
                    <textarea name="comment" rows="8" id="input-comment" class="form-control"></textarea>
                  </div>
                </div>
              </form>
            </fieldset>
            <div class="text-right">
              <button id="button-history" data-loading-text="{{ text_loading }}" class="btn btn-primary"><i class="fa fa-plus-circle"></i> {{ button_history_add }}</button>
            </div>
          </div>
          <div class="tab-pane" id="tab-additional"> {% if account_custom_fields %}
              <div class="table-responsive">
                <table class="table table-bordered">
                  <thead>
                    <tr>
                      <td colspan="2">{{ text_account_custom_field }}</td>
                    </tr>
                  </thead>
                  <tbody>

                    {% for custom_field in account_custom_fields %}
                      <tr>
                        <td>{{ custom_field.name }}</td>
                        <td>{{ custom_field.value }}</td>
                      </tr>
                    {% endfor %}
                  </tbody>

                </table>
              </div>
            {% endif %}
            {% if payment_custom_fields %}
              <div class="table-responsive">
                <table class="table table-bordered">
                  <thead>
                    <tr>
                      <td colspan="2">{{ text_payment_custom_field }}</td>
                    </tr>
                  </thead>
                  <tbody>

                    {% for custom_field in payment_custom_fields %}
                      <tr>
                        <td>{{ custom_field.name }}</td>
                        <td>{{ custom_field.value }}</td>
                      </tr>
                    {% endfor %}
                  </tbody>

                </table>
              </div>
            {% endif %}
            {% if shipping_method and shipping_custom_fields %}
              <div class="table-responsive">
                <table class="table table-bordered">
                  <thead>
                    <tr>
                      <td colspan="2">{{ text_shipping_custom_field }}</td>
                    </tr>
                  </thead>
                  <tbody>

                    {% for custom_field in shipping_custom_fields %}
                      <tr>
                        <td>{{ custom_field.name }}</td>
                        <td>{{ custom_field.value }}</td>
                      </tr>
                    {% endfor %}
                  </tbody>

                </table>
              </div>
            {% endif %}
            <div class="table-responsive">
              <table class="table table-bordered">
                <thead>
                  <tr>
                    <td colspan="2">{{ text_browser }}</td>
                  </tr>
                </thead>
                <tbody>
                  <tr>
                    <td>{{ text_ip }}</td>
                    <td>{{ ip }}</td>
                  </tr>
                  {% if forwarded_ip %}
                    <tr>
                      <td>{{ text_forwarded_ip }}</td>
                      <td>{{ forwarded_ip }}</td>
                    </tr>
                  {% endif %}
                  <tr>
                    <td>{{ text_user_agent }}</td>
                    <td>{{ user_agent }}</td>
                  </tr>
                  <tr>
                    <td>{{ text_accept_language }}</td>
                    <td>{{ accept_language }}</td>
                  </tr>
                </tbody>

              </table>
            </div>
          </div>
          {% for tab in tabs %}
            <div class="tab-pane" id="tab-{{ tab.code }}">{{ tab.content }}</div>
          {% endfor %} </div>
      </div>
    </div>
  </div>

  <div id="modal-quick-edit" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title" id="modal-quick-edit-title"></h4>
        </div>
        <div class="modal-body">
          <div id="modal-quick-edit-fields"></div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">{{ button_cancel }}</button>
          <button type="button" id="button-quick-edit-save" class="btn btn-primary" data-loading-text="{{ text_loading }}"><i class="fa fa-check"></i> {{ button_save }}</button>
        </div>
      </div>
    </div>
  </div>

  <div id="modal-inline-product-options" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title" id="modal-inline-product-title">{{ entry_option }}</h4>
        </div>
        <div class="modal-body">
          <form id="form-inline-product-options" class="form-horizontal">
            <input type="hidden" name="product_id" id="input-inline-option-product-id" value="" />
            <input type="hidden" name="quantity" id="input-inline-option-quantity" value="1" />
            <div id="inline-product-options-body"></div>
          </form>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">{{ button_cancel }}</button>
          <button type="button" id="button-inline-option-add" class="btn btn-primary" data-loading-text="{{ text_loading }}"><i class="fa fa-plus"></i> {{ button_product_add }}</button>
        </div>
      </div>
    </div>
  </div>

  <div id="modal-product-info" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title" id="modal-product-info-title">{{ text_product_info }}</h4>
        </div>
        <div class="modal-body">
          <div class="modal-product-info-body">
            <div class="modal-product-info-image">
              <div class="modal-product-info-main-image" id="modal-product-info-main-image"></div>
              <div class="modal-product-info-thumbs" id="modal-product-info-thumbs"></div>
            </div>
            <div class="modal-product-info-details">
              <h4 id="modal-product-info-name"></h4>
              <div class="modal-product-info-description" id="modal-product-info-description"></div>
              <div class="modal-product-info-grid" id="modal-product-info-grid"></div>
              <div class="modal-product-info-discounts" id="modal-product-info-discounts" style="display:none"></div>
            </div>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">{{ button_cancel }}</button>
        </div>
      </div>
    </div>
  </div>

  <script type="text/javascript"><!--
  var quickCountries = {{ countries|json_encode|raw }};

  var inlineState = {
    customer: {
      customer_id: '{{ customer_id|e('js') }}',
      customer_group_id: '{{ customer_group_id|e('js') }}',
      firstname: '{{ firstname|e('js') }}',
      lastname: '{{ lastname|e('js') }}',
      email: '{{ email|e('js') }}',
      telephone: '{{ telephone|e('js') }}',
      tax_number: '{{ tax_number|e('js') }}',
      tracking_number: '{{ tracking_number|e('js') }}'
    },
    payment: {
      firstname: '{{ payment_firstname|e('js') }}',
      lastname: '{{ payment_lastname|e('js') }}',
      company: '{{ payment_company|e('js') }}',
      address_1: '{{ payment_address_1|e('js') }}',
      address_2: '{{ payment_address_2|e('js') }}',
      city: '{{ payment_city|e('js') }}',
      postcode: '{{ payment_postcode|e('js') }}',
      country_id: '{{ payment_country_id|e('js') }}',
      zone_id: '{{ payment_zone_id|e('js') }}'
    },
    shipping: {
      firstname: '{{ shipping_firstname|e('js') }}',
      lastname: '{{ shipping_lastname|e('js') }}',
      company: '{{ shipping_company|e('js') }}',
      address_1: '{{ shipping_address_1|e('js') }}',
      address_2: '{{ shipping_address_2|e('js') }}',
      city: '{{ shipping_city|e('js') }}',
      postcode: '{{ shipping_postcode|e('js') }}',
      country_id: '{{ shipping_country_id|e('js') }}',
      zone_id: '{{ shipping_zone_id|e('js') }}'
    },
    payment_code: '{{ payment_code|e('js') }}',
    payment_method: '{{ payment_method|e('js') }}',
    shipping_code: '{{ shipping_code|e('js') }}',
    shipping_method: '{{ shipping_method|e('js') }}',
    has_shipping: {{ shipping_method ? 'true' : 'false' }},
    comment: '{{ order_comment_raw|e('js') }}'
  };

  var quickEditState = {
    field: '',
    target: ''
  };

  var productInfoData = {
    {% for product in products %}
    {{ product.product_id }}: {
      name: '{{ product.name|e('js') }}',
      model: '{{ product.model|e('js') }}',
      thumb_large: '{{ product.thumb_large|e('js') }}',
      description: '{{ product.description|e('js') }}',
      sku: '{{ product.sku|e('js') }}',
      upc: '{{ product.upc|e('js') }}',
      ean: '{{ product.ean|e('js') }}',
      jan: '{{ product.jan|e('js') }}',
      isbn: '{{ product.isbn|e('js') }}',
      mpn: '{{ product.mpn|e('js') }}',
      price: '{{ product.price|e('js') }}',
      special: '{{ product.special|e('js') }}',
      stock_quantity: '{{ product.stock_quantity|e('js') }}',
      status: {{ product.status }},
      manufacturer: '{{ product.manufacturer|e('js') }}',
      weight: '{{ product.weight|e('js') }}',
      weight_class_id: '{{ product.weight_class_id|e('js') }}',
      dimensions: '{% if product.length > 0 or product.width > 0 or product.height > 0 %}{{ product.length|e('js') }} × {{ product.width|e('js') }} × {{ product.height|e('js') }}{% endif %}',
      discounts: {{ product.discounts|json_encode|raw }},
      images: {{ product.images|json_encode|raw }}
    }{% if not loop.last %},{% endif %}
    {% endfor %}
  };

  var inlineRecalcTimer = null;
  var inlineDiscountPreviewTimer = null;

  function htmlDecode(value) {
    return $('<textarea/>').html(value || '').text();
  }

  function parseApiError(error) {
    if (typeof error === 'string') {
      return error;
    }

    if (error && typeof error === 'object') {
      if (error.warning) {
        return error.warning;
      }

      var messages = [];

      for (var key in error) {
        if (error.hasOwnProperty(key) && typeof error[key] === 'string') {
          messages.push(error[key]);
        }
      }

      if (messages.length) {
        return messages.join('<br/>');
      }
    }

    return '{{ error_warning|escape('js') }}';
  }

  function quickCountryOptions(selectedId) {
    var html = '<option value="">{{ text_select }}</option>';

    for (var i = 0; i < quickCountries.length; i++) {
      var country = quickCountries[i];
      html += '<option value="' + country.country_id + '"' + (String(country.country_id) === String(selectedId) ? ' selected="selected"' : '') + '>' + country.name + '</option>';
    }

    return html;
  }

  function quickLoadZones(countryId, selectedZoneId) {
    $.ajax({
      url: 'index.php?route=localisation/country/country&user_token={{ user_token }}&country_id=' + countryId,
      dataType: 'json',
      success: function(json) {
        var html = '<option value="">{{ text_select }}</option>';

        if (json['zone'] && json['zone'].length) {
          for (var i = 0; i < json['zone'].length; i++) {
            var zone = json['zone'][i];
            html += '<option value="' + zone.zone_id + '"' + (String(zone.zone_id) === String(selectedZoneId) ? ' selected="selected"' : '') + '>' + zone.name + '</option>';
          }
        } else {
          html += '<option value="0" selected="selected">{{ text_none }}</option>';
        }

        $('#quick-edit-zone').html(html);
      }
    });
  }

  function quickLoadApiMethodOptions(field, selectedCode) {
    $('#quick-edit-method').html('<option value="">{{ text_loading }}</option>');

    if (field === 'payment_method') {
      inlineSyncApiCartFromCurrentTable().then(function() {
        return inlineApiRequest({
        url: '{{ catalog }}index.php?route=api/payment/address&api_token={{ api_token }}&store_id={{ store_id }}',
        type: 'post',
        data: $.param(inlineState.payment)
        });
      }).then(function() {
        return inlineApiRequest({
          url: '{{ catalog }}index.php?route=api/payment/methods&api_token={{ api_token }}&store_id={{ store_id }}'
        });
      }).done(function(json) {
        var html = '<option value="">{{ text_select }}</option>';
        var hasOptions = false;

        if (json.payment_methods) {
          for (var i in json.payment_methods) {
            html += '<option value="' + json.payment_methods[i].code + '"' + (String(json.payment_methods[i].code) === String(selectedCode) ? ' selected="selected"' : '') + '>' + json.payment_methods[i].title + '</option>';
            hasOptions = true;
          }
        }

        if (!hasOptions && selectedCode) {
          html += '<option value="' + selectedCode + '" selected="selected">' + (inlineState.payment_method || selectedCode) + '</option>';
        }

        $('#quick-edit-method').html(html);
      }).fail(function(error) {
        $('#quick-edit-method').html('<option value="">{{ text_select }}</option>');
        showInlineAlert('danger', parseApiError(error));
      });
    }

    if (field === 'shipping_method') {
      inlineSyncApiCartFromCurrentTable().then(function() {
        return inlineApiRequest({
        url: '{{ catalog }}index.php?route=api/shipping/address&api_token={{ api_token }}&store_id={{ store_id }}',
        type: 'post',
        data: $.param(inlineState.shipping)
        });
      }).then(function() {
        return inlineApiRequest({
          url: '{{ catalog }}index.php?route=api/shipping/methods&api_token={{ api_token }}&store_id={{ store_id }}'
        });
      }).done(function(json) {
        var html = '<option value="">{{ text_select }}</option>';
        var hasOptions = false;

        if (json.shipping_methods) {
          for (var i in json.shipping_methods) {
            if (!json.shipping_methods[i].error && json.shipping_methods[i].quote) {
              for (var j in json.shipping_methods[i].quote) {
                var quote = json.shipping_methods[i].quote[j];
                html += '<option value="' + quote.code + '"' + (String(quote.code) === String(selectedCode) ? ' selected="selected"' : '') + '>' + quote.title + ' - ' + quote.text + '</option>';
                hasOptions = true;
              }
            }
          }
        }

        if (!hasOptions && selectedCode) {
          html += '<option value="' + selectedCode + '" selected="selected">' + (inlineState.shipping_method || selectedCode) + '</option>';
        }

        $('#quick-edit-method').html(html);
      }).fail(function(error) {
        $('#quick-edit-method').html('<option value="">{{ text_select }}</option>');
        showInlineAlert('danger', parseApiError(error));
      });
    }
  }

  function inlineApiRequest(options) {
    var deferred = $.Deferred();

    $.ajax($.extend({
      dataType: 'json',
      crossDomain: true,
      type: 'get'
    }, options)).done(function(json) {
      if (json && json['error']) {
        deferred.reject(json['error']);
      } else {
        deferred.resolve(json || {});
      }
    }).fail(function(xhr, ajaxOptions, thrownError) {
      deferred.reject(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
    });

    return deferred.promise();
  }

  function inlineSyncApiCartFromCurrentTable() {
    var cartData = $('#inline-cart input[name^=\'product\'][type=\'text\'], #inline-cart input[name^=\'product\'][type=\'hidden\'], #inline-cart input[name^=\'product\'][type=\'radio\']:checked, #inline-cart input[name^=\'product\'][type=\'checkbox\']:checked, #inline-cart select[name^=\'product\'], #inline-cart textarea[name^=\'product\']').serialize();

    if (!cartData) {
      return $.Deferred().resolve().promise();
    }

    return inlineApiRequest({
      url: '{{ catalog }}index.php?route=api/cart/add&api_token={{ api_token }}&store_id={{ store_id }}',
      type: 'post',
      data: cartData
    });
  }

  function showInlineAlert(type, text) {
    $('.alert-dismissible').remove();
    $('#content > .container-fluid').prepend('<div class="alert alert-' + type + ' alert-dismissible"><i class="fa ' + (type === 'danger' ? 'fa-exclamation-circle' : 'fa-check-circle') + '"></i> ' + text + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');
  }

  function inlineReindexCartRows() {
    $('#inline-cart tr').not('.inline-cart-empty').each(function(index) {
      $(this).find('input, select, textarea').each(function() {
        var name = $(this).attr('name');

        if (name && name.indexOf('product[') === 0) {
          $(this).attr('name', name.replace(/product\[\d+\]/, 'product[' + index + ']'));
        }
      });
    });
  }

  function quickEditBuildForm(field, data) {
    var html = '';

    if (field === 'customer_name') {
      html += '<div class="form-group">';
      html += '  <label class="control-label" for="quick-edit-firstname">{{ entry_firstname|e('js') }}</label>';
      html += '  <input type="text" id="quick-edit-firstname" class="form-control" value="' + (data.firstname || '') + '" />';
      html += '</div>';
      html += '<div class="form-group">';
      html += '  <label class="control-label" for="quick-edit-lastname">{{ entry_lastname }}</label>';
      html += '  <input type="text" id="quick-edit-lastname" class="form-control" value="' + (data.lastname || '') + '" />';
      html += '</div>';
    } else if (field === 'comment') {
      html += '<div class="form-group">';
      html += '  <label class="control-label" for="quick-edit-value">' + data.title + '</label>';
      html += '  <textarea id="quick-edit-value" rows="6" class="form-control">' + (data.value || '') + '</textarea>';
      html += '</div>';
    } else if (field === 'payment_method' || field === 'shipping_method') {
      html += '<div class="form-group">';
      html += '  <label class="control-label" for="quick-edit-method">' + data.title + '</label>';
      html += '  <select id="quick-edit-method" class="form-control"><option value="">{{ text_loading }}</option></select>';
      html += '</div>';
    } else if (field === 'payment_address' || field === 'shipping_address') {
      var state = field === 'payment_address' ? inlineState.payment : inlineState.shipping;

      html += '<div class="row">';
      html += '  <div class="col-sm-6"><div class="form-group"><label class="control-label" for="quick-edit-firstname">{{ entry_firstname|e('js') }}</label><input type="text" id="quick-edit-firstname" class="form-control" value="' + (state.firstname || '') + '" /></div></div>';
      html += '  <div class="col-sm-6"><div class="form-group"><label class="control-label" for="quick-edit-lastname">{{ entry_lastname }}</label><input type="text" id="quick-edit-lastname" class="form-control" value="' + (state.lastname || '') + '" /></div></div>';
      html += '</div>';
      html += '<div class="form-group"><label class="control-label" for="quick-edit-company">{{ entry_company }}</label><input type="text" id="quick-edit-company" class="form-control" value="' + (state.company || '') + '" /></div>';
      html += '<div class="form-group"><label class="control-label" for="quick-edit-address-1">{{ entry_address_1 }}</label><input type="text" id="quick-edit-address-1" class="form-control" value="' + (state.address_1 || '') + '" /></div>';
      html += '<div class="form-group"><label class="control-label" for="quick-edit-address-2">{{ entry_address_2 }}</label><input type="text" id="quick-edit-address-2" class="form-control" value="' + (state.address_2 || '') + '" /></div>';
      html += '<div class="row">';
      html += '  <div class="col-sm-6"><div class="form-group"><label class="control-label" for="quick-edit-city">{{ entry_city }}</label><input type="text" id="quick-edit-city" class="form-control" value="' + (state.city || '') + '" /></div></div>';
      html += '  <div class="col-sm-6"><div class="form-group"><label class="control-label" for="quick-edit-postcode">{{ entry_postcode }}</label><input type="text" id="quick-edit-postcode" class="form-control" value="' + (state.postcode || '') + '" /></div></div>';
      html += '</div>';
      html += '<div class="row">';
      html += '  <div class="col-sm-6"><div class="form-group"><label class="control-label" for="quick-edit-country">{{ entry_country }}</label><select id="quick-edit-country" class="form-control">' + quickCountryOptions(state.country_id) + '</select></div></div>';
      html += '  <div class="col-sm-6"><div class="form-group"><label class="control-label" for="quick-edit-zone">{{ entry_zone }}</label><select id="quick-edit-zone" class="form-control"></select></div></div>';
      html += '</div>';
    } else {
      html += '<div class="form-group">';
      html += '  <label class="control-label" for="quick-edit-value">' + data.title + '</label>';
      html += '  <input type="text" id="quick-edit-value" class="form-control" value="' + (data.value || '') + '" />';
      html += '</div>';
    }

    return html;
  }

  function quickEditOpen($button) {
    var field = $button.data('field');
    var title = $button.data('title') || '';

    quickEditState.field = field;
    quickEditState.target = $button.data('target') || '';

    $('#modal-quick-edit-title').text(title);

    $('#modal-quick-edit-fields').html(quickEditBuildForm(field, {
      title: title,
      value: $button.data('value') || '',
      code: $button.data('code') || '',
      firstname: $button.data('firstname') || '',
      lastname: $button.data('lastname') || ''
    }));

    if (field === 'payment_address' || field === 'shipping_address') {
      var state = field === 'payment_address' ? inlineState.payment : inlineState.shipping;
      quickLoadZones(state.country_id, state.zone_id);

      $('#modal-quick-edit-fields').off('change.quickCountry').on('change.quickCountry', '#quick-edit-country', function() {
        quickLoadZones(this.value, '');
      });
    }

    if (field === 'payment_method' || field === 'shipping_method') {
      quickLoadApiMethodOptions(field, $button.data('code') || '');
    }

    $('#modal-quick-edit').modal('show');
  }

  function quickEditSave() {
    var payload = {
      field: quickEditState.field
    };

    if (quickEditState.field === 'customer_name') {
      payload.firstname = $('#quick-edit-firstname').val();
      payload.lastname = $('#quick-edit-lastname').val();
    } else if (quickEditState.field === 'payment_method' || quickEditState.field === 'shipping_method') {
      payload.code = $('#quick-edit-method').val();
      payload.title = $('#quick-edit-method option:selected').text();
      payload.value = payload.title;
    } else if (quickEditState.field === 'payment_address' || quickEditState.field === 'shipping_address') {
      payload.firstname = $('#quick-edit-firstname').val();
      payload.lastname = $('#quick-edit-lastname').val();
      payload.company = $('#quick-edit-company').val();
      payload.address_1 = $('#quick-edit-address-1').val();
      payload.address_2 = $('#quick-edit-address-2').val();
      payload.city = $('#quick-edit-city').val();
      payload.postcode = $('#quick-edit-postcode').val();
      payload.country_id = $('#quick-edit-country').val();
      payload.zone_id = $('#quick-edit-zone').val();
    } else {
      payload.value = $('#quick-edit-value').val();
    }

    $.ajax({
      url: '{{ quick_edit }}',
      type: 'post',
      dataType: 'json',
      data: payload,
      beforeSend: function() {
        $('#button-quick-edit-save').button('loading');
      },
      complete: function() {
        $('#button-quick-edit-save').button('reset');
      },
      success: function(json) {
        $('.alert-dismissible').remove();

        if (json['error']) {
          $('#content > .container-fluid').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> ' + json['error'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');
          return;
        }

        if (json['success']) {
          if (quickEditState.target && json['value_html'] !== undefined) {
            $(quickEditState.target).html(json['value_html']);
          }

          if (quickEditState.field === 'customer_name') {
            inlineState.customer.firstname = payload.firstname;
            inlineState.customer.lastname = payload.lastname;
          }

          if (quickEditState.field === 'email') {
            inlineState.customer.email = payload.value;
            $('.btn-quick-edit[data-field="email"]').data('value', payload.value);
          }

          if (quickEditState.field === 'telephone') {
            inlineState.customer.telephone = payload.value;
            $('.btn-quick-edit[data-field="telephone"]').data('value', payload.value);
          }

          if (quickEditState.field === 'tax_number') {
            inlineState.customer.tax_number = payload.value;
            $('.btn-quick-edit[data-field="tax_number"]').data('value', payload.value);
          }

          if (quickEditState.field === 'tracking_number') {
            inlineState.customer.tracking_number = payload.value;
            $('.btn-quick-edit[data-field="tracking_number"]').data('value', payload.value);
          }

          if (quickEditState.field === 'comment') {
            inlineState.comment = payload.value;
            $('.btn-quick-edit[data-field="comment"]').data('value', payload.value);
          }

          if (quickEditState.field === 'payment_method') {
            inlineState.payment_code = payload.code;
            inlineState.payment_method = payload.title;
            $('.btn-quick-edit[data-field="payment_method"]').data('value', payload.title).data('code', payload.code);
          }

          if (quickEditState.field === 'shipping_method') {
            inlineState.shipping_code = payload.code;
            inlineState.shipping_method = payload.title;
            $('.btn-quick-edit[data-field="shipping_method"]').data('value', payload.title).data('code', payload.code);
          }

          if (quickEditState.field === 'payment_address') {
            inlineState.payment = {
              firstname: payload.firstname,
              lastname: payload.lastname,
              company: payload.company,
              address_1: payload.address_1,
              address_2: payload.address_2,
              city: payload.city,
              postcode: payload.postcode,
              country_id: payload.country_id,
              zone_id: payload.zone_id
            };
          }

          if (quickEditState.field === 'shipping_address') {
            inlineState.shipping = {
              firstname: payload.firstname,
              lastname: payload.lastname,
              company: payload.company,
              address_1: payload.address_1,
              address_2: payload.address_2,
              city: payload.city,
              postcode: payload.postcode,
              country_id: payload.country_id,
              zone_id: payload.zone_id
            };
          }

          if (quickEditState.field === 'payment_method' || quickEditState.field === 'shipping_method' || quickEditState.field === 'payment_address' || quickEditState.field === 'shipping_address') {
            inlinePersistOrderChanges({
              reloadOnSuccess: false,
              showSuccess: false
            }).fail(function(error) {
              if (error && error.responseJSON && error.responseJSON.error) {
                showInlineAlert('danger', parseApiError(error.responseJSON.error));
              } else {
                showInlineAlert('danger', parseApiError(error));
              }
            });
          }

          $('#modal-quick-edit').modal('hide');
          $('#content > .container-fluid').prepend('<div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> ' + json['success'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');
        }
      },
      error: function(xhr, ajaxOptions, thrownError) {
        alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
      }
    });
  }

  function inlineBuildProductInfoModal(productId) {
    var data = productInfoData[productId];

    if (!data) {
      $('#modal-product-info').modal('show');
      return;
    }

    $('#modal-product-info-name').text(data.name);

    var mainImageHtml = '';
    if (data.thumb_large) {
      mainImageHtml = '<img src="' + data.thumb_large + '" alt="' + data.name.replace(/'/g, '&#39;') + '" />';
    }
    $('#modal-product-info-main-image').html(mainImageHtml);

    var thumbsHtml = '';
    if (data.thumb_large) {
      thumbsHtml += '<img src="' + data.thumb_large + '" class="active" data-img="' + data.thumb_large + '" alt="" />';
    }
    if (data.images && data.images.length) {
      for (var g = 0; g < data.images.length; g++) {
        thumbsHtml += '<img src="' + data.images[g].thumb + '" data-img="' + data.images[g].thumb + '" alt="" />';
      }
    }
    $('#modal-product-info-thumbs').html(thumbsHtml);

    $('#modal-product-info-description').text(data.description || '');

    var gridHtml = '';
    gridHtml += '<span class="label">{{ text_sku|e('js') }}</span><span class="value">' + (data.sku || '—') + '</span>';
    gridHtml += '<span class="label">{{ column_model|e('js') }}</span><span class="value">' + (data.model || '—') + '</span>';

    if (data.upc) gridHtml += '<span class="label">{{ text_upc|e('js') }}</span><span class="value">' + data.upc + '</span>';
    if (data.ean) gridHtml += '<span class="label">{{ text_ean|e('js') }}</span><span class="value">' + data.ean + '</span>';
    if (data.jan) gridHtml += '<span class="label">{{ text_jan|e('js') }}</span><span class="value">' + data.jan + '</span>';
    if (data.isbn) gridHtml += '<span class="label">{{ text_isbn|e('js') }}</span><span class="value">' + data.isbn + '</span>';
    if (data.mpn) gridHtml += '<span class="label">{{ text_mpn|e('js') }}</span><span class="value">' + data.mpn + '</span>';

    gridHtml += '<span class="label">{{ column_price|e('js') }}</span><span class="value">' + (data.price || '—') + '</span>';

    if (data.special) {
      gridHtml += '<span class="label">{{ text_special|e('js') }}</span><span class="value">' + data.special + '</span>';
    }

    if (data.manufacturer) {
      gridHtml += '<span class="label">{{ text_manufacturer|e('js') }}</span><span class="value">' + data.manufacturer + '</span>';
    }

    gridHtml += '<span class="label">{{ text_stock_quantity|e('js') }}</span><span class="value">' + (data.stock_quantity !== '' ? data.stock_quantity : '—') + '</span>';

    gridHtml += '<span class="label">{{ column_status|e('js') }}</span><span class="value ' + (data.status ? 'enabled' : 'disabled') + '">' + (data.status ? '{{ text_enabled|e('js') }}' : '{{ text_disabled|e('js') }}') + '</span>';

    if (data.weight) {
      gridHtml += '<span class="label">{{ text_weight|e('js') }}</span><span class="value">' + data.weight + '</span>';
    }

    if (data.dimensions) {
      gridHtml += '<span class="label">{{ text_dimensions|e('js') }}</span><span class="value">' + data.dimensions + '</span>';
    }

    $('#modal-product-info-grid').html(gridHtml);

    if (data.discounts && data.discounts.length) {
      var discountsHtml = '<table><thead><tr><th>{{ text_quantity_short|e('js') }}</th><th>{{ text_price_short|e('js') }}</th></tr></thead><tbody>';
      for (var d = 0; d < data.discounts.length; d++) {
        discountsHtml += '<tr><td>' + data.discounts[d].quantity + '+</td><td>' + data.discounts[d].price + '</td></tr>';
      }
      discountsHtml += '</tbody></table>';
      $('#modal-product-info-discounts').html(discountsHtml).show();
    } else {
      $('#modal-product-info-discounts').hide();
    }

    $('#modal-product-info').modal('show');
  }

  $(document).on('click', '.btn-product-info', function(e) {
    e.preventDefault();
    var productId = $(this).data('product-id');
    inlineBuildProductInfoModal(productId);
  });

  $(document).on('click', '.modal-product-info-thumbs img', function() {
    var $thumb = $(this);
    var imgSrc = $thumb.data('img');

    if (imgSrc) {
      $('#modal-product-info-main-image img').attr('src', imgSrc);
      $thumb.closest('.modal-product-info-thumbs').find('img').removeClass('active');
      $thumb.addClass('active');
    }
  });

  function inlineBuildProductOptionFields(options) {
    var html = '';

    if (!options || !options.length) {
      return html;
    }

    html += '<fieldset><legend>{{ entry_option }}</legend>';

    for (var i = 0; i < options.length; i++) {
      var option = options[i];
      var optionId = option['product_option_id'];
      var requiredClass = option['required'] ? ' required' : '';

      if (option['type'] == 'select' || option['type'] == 'radio' || option['type'] == 'image') {
        html += '<div class="form-group' + requiredClass + '">';
        html += '  <label class="col-sm-3 control-label" for="inline-input-option' + optionId + '">' + option['name'] + '</label>';
        html += '  <div class="col-sm-9"><select name="option[' + optionId + ']" id="inline-input-option' + optionId + '" class="form-control">';
        html += '    <option value="">{{ text_select }}</option>';

        if (option['product_option_value']) {
          for (var j = 0; j < option['product_option_value'].length; j++) {
            var optionValue = option['product_option_value'][j];
            html += '<option value="' + optionValue['product_option_value_id'] + '">' + optionValue['name'];

            if (optionValue['price']) {
              html += ' (' + optionValue['price_prefix'] + optionValue['price'] + ')';
            }

            html += '</option>';
          }
        }

        html += '  </select></div>';
        html += '</div>';
      }

      if (option['type'] == 'checkbox') {
        html += '<div class="form-group' + requiredClass + '">';
        html += '  <label class="col-sm-3 control-label">' + option['name'] + '</label>';
        html += '  <div class="col-sm-9" id="inline-input-option' + optionId + '">';

        if (option['product_option_value']) {
          for (var k = 0; k < option['product_option_value'].length; k++) {
            var optionCheckbox = option['product_option_value'][k];
            html += '<div class="checkbox"><label><input type="checkbox" name="option[' + optionId + '][]" value="' + optionCheckbox['product_option_value_id'] + '" /> ' + optionCheckbox['name'];

            if (optionCheckbox['price']) {
              html += ' (' + optionCheckbox['price_prefix'] + optionCheckbox['price'] + ')';
            }

            html += '</label></div>';
          }
        }

        html += '  </div>';
        html += '</div>';
      }

      if (option['type'] == 'text') {
        html += '<div class="form-group' + requiredClass + '"><label class="col-sm-3 control-label" for="inline-input-option' + optionId + '">' + option['name'] + '</label><div class="col-sm-9"><input type="text" name="option[' + optionId + ']" value="' + (option['value'] || '') + '" id="inline-input-option' + optionId + '" class="form-control" /></div></div>';
      }

      if (option['type'] == 'textarea') {
        html += '<div class="form-group' + requiredClass + '"><label class="col-sm-3 control-label" for="inline-input-option' + optionId + '">' + option['name'] + '</label><div class="col-sm-9"><textarea name="option[' + optionId + ']" rows="5" id="inline-input-option' + optionId + '" class="form-control">' + (option['value'] || '') + '</textarea></div></div>';
      }

      if (option['type'] == 'file') {
        html += '<div class="form-group' + requiredClass + '">';
        html += '  <label class="col-sm-3 control-label">' + option['name'] + '</label>';
        html += '  <div class="col-sm-9">';
        html += '    <button type="button" id="inline-button-upload' + optionId + '" data-loading-text="{{ text_loading }}" class="btn btn-default"><i class="fa fa-upload"></i> {{ button_upload }}</button>';
        html += '    <input type="hidden" name="option[' + optionId + ']" value="' + (option['value'] || '') + '" id="inline-input-option' + optionId + '" />';
        html += '  </div>';
        html += '</div>';
      }

      if (option['type'] == 'date') {
        html += '<div class="form-group' + requiredClass + '"><label class="col-sm-3 control-label" for="inline-input-option' + optionId + '">' + option['name'] + '</label><div class="col-sm-9"><div class="input-group date"><input type="text" name="option[' + optionId + ']" value="' + (option['value'] || '') + '" data-date-format="YYYY-MM-DD" id="inline-input-option' + optionId + '" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button></span></div></div></div>';
      }

      if (option['type'] == 'datetime') {
        html += '<div class="form-group' + requiredClass + '"><label class="col-sm-3 control-label" for="inline-input-option' + optionId + '">' + option['name'] + '</label><div class="col-sm-9"><div class="input-group datetime"><input type="text" name="option[' + optionId + ']" value="' + (option['value'] || '') + '" data-date-format="YYYY-MM-DD HH:mm" id="inline-input-option' + optionId + '" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button></span></div></div></div>';
      }

      if (option['type'] == 'time') {
        html += '<div class="form-group' + requiredClass + '"><label class="col-sm-3 control-label" for="inline-input-option' + optionId + '">' + option['name'] + '</label><div class="col-sm-9"><div class="input-group time"><input type="text" name="option[' + optionId + ']" value="' + (option['value'] || '') + '" data-date-format="HH:mm" id="inline-input-option' + optionId + '" class="form-control" /><span class="input-group-btn"><button type="button" class="btn btn-default"><i class="fa fa-calendar"></i></button></span></div></div></div>';
      }
    }

    html += '</fieldset>';

    return html;
  }

  function inlineCollectDiscounts() {
    var discounts = [];

    $('#inline-cart tr').not('.inline-cart-empty').each(function() {
      var $row = $(this);
      var $productIdInput = $row.find('input[name$="[product_id]"]');

      if (!$productIdInput.length) {
        return;
      }

      var $discountInput = $row.find('input[name$="[discount]"]');
      var value = parseFloat($discountInput.val());

      if (!$discountInput.length) {
        value = 0;
      }

      if (isNaN(value) || value < 0) {
        value = 0;
      }

      if (value > 100) {
        value = 100;
      }

      if ($discountInput.length) {
        $discountInput.val(value);
      }

      discounts.push(value);
    });

    return discounts;
  }

  function inlineRequestDiscountPreview() {
    return inlineRecalculateCartPreview(false, {
      skipCartReplace: true,
      couponCode: $('#input-inline-coupon').val(),
      applyDiscountPreview: false
    });
  }

  function inlineRenderCartFromApi(json, discounts) {
    discounts = discounts || [];

    var productsHtml = '';

    if (json['products'] && json['products'].length) {
      for (var i = 0; i < json['products'].length; i++) {
        var product = json['products'][i];
        var productId = product['product_id'];
        var productThumb = '';

        if (productId && !productInfoData[productId]) {
          productInfoData[productId] = {
            name: product['name'] || '',
            model: product['model'] || '',
            thumb_large: product['thumb'] || '',
            description: '',
            sku: '',
            upc: '',
            ean: '',
            jan: '',
            isbn: '',
            mpn: '',
            price: product['price'] || '',
            special: '',
            stock_quantity: '',
            status: 1,
            manufacturer: '',
            weight: '',
            weight_class_id: 0,
            dimensions: '',
            discounts: [],
            images: []
          };
        }

        if (product['thumb']) {
          productThumb = '<a href="#" class="order-product-thumb btn-product-info" data-product-id="' + productId + '"><img src="' + product['thumb'] + '" alt="" /></a>';
        }

        productsHtml += '<tr>';
        productsHtml += '  <td class="text-left order-product-name">' + productThumb + '<a href="#" class="btn-product-info" data-product-id="' + productId + '">' + product['name'] + '</a><input type="hidden" name="product[' + i + '][product_id]" value="' + productId + '" />';

        if (product['option']) {
          for (var j = 0; j < product['option'].length; j++) {
            var option = product['option'][j];
            productsHtml += '<div class="order-product-option">- ' + option['name'] + ': ' + option['value'] + '</div>';

            if (option['type'] == 'select' || option['type'] == 'radio' || option['type'] == 'image') {
              productsHtml += '<input type="hidden" name="product[' + i + '][option][' + option['product_option_id'] + ']" value="' + option['product_option_value_id'] + '" />';
            }

            if (option['type'] == 'checkbox') {
              productsHtml += '<input type="hidden" name="product[' + i + '][option][' + option['product_option_id'] + '][]" value="' + option['product_option_value_id'] + '" />';
            }

            if (option['type'] == 'text' || option['type'] == 'textarea' || option['type'] == 'file' || option['type'] == 'date' || option['type'] == 'datetime' || option['type'] == 'time') {
              productsHtml += '<input type="hidden" name="product[' + i + '][option][' + option['product_option_id'] + ']" value="' + option['value'] + '" />';
            }
          }
        }

        productsHtml += '  </td>';
        productsHtml += '  <td class="text-left">' + product['model'] + '</td>';
        productsHtml += '  <td class="text-right"><input type="text" name="product[' + i + '][quantity]" value="' + product['quantity'] + '" class="order-product-input gt-no-translate" data-gt-inline-ignore="1" /></td>';
        productsHtml += '  <td class="text-right"><input type="text" name="product[' + i + '][discount]" value="' + (discounts[i] !== undefined ? discounts[i] : 0) + '" class="order-product-input gt-no-translate" data-gt-inline-ignore="1" /></td>';
        productsHtml += '  <td class="text-right">' + product['price'] + '</td>';
        productsHtml += '  <td class="text-right">' + product['total'] + '</td>';
        productsHtml += '  <td class="text-right"><button type="button" class="btn btn-danger btn-xs button-inline-remove-item"><i class="fa fa-trash"></i></button></td>';
        productsHtml += '</tr>';
      }
    }

    if (json['vouchers'] && json['vouchers'].length) {
      for (var v = 0; v < json['vouchers'].length; v++) {
        var voucher = json['vouchers'][v];
        productsHtml += '<tr>';
        productsHtml += '  <td class="text-left">' + voucher['description'] + '</td>';
        productsHtml += '  <td class="text-left"></td>';
        productsHtml += '  <td class="text-right">1</td>';
        productsHtml += '  <td class="text-right"></td>';
        productsHtml += '  <td class="text-right">' + voucher['price'] + '</td>';
        productsHtml += '  <td class="text-right">' + voucher['price'] + '</td>';
        productsHtml += '  <td class="text-right"></td>';
        productsHtml += '</tr>';
      }
    }

    if (!productsHtml) {
      productsHtml = '<tr class="inline-cart-empty"><td colspan="7" class="text-center">{{ text_no_results }}</td></tr>';
    }

    $('#inline-cart').html(productsHtml);
    inlineReindexCartRows();

    var totalsHtml = '';

    if (json['totals'] && json['totals'].length) {
      for (var t = 0; t < json['totals'].length; t++) {
        totalsHtml += '<tr><td colspan="5" class="text-right">' + json['totals'][t]['title'] + '</td><td class="text-right">' + json['totals'][t]['text'] + '</td><td></td></tr>';
      }
    }

    $('#inline-totals').html(totalsHtml);
  }

  function inlineSyncCheckoutContext(couponCode) {
    return inlineApiRequest({
      url: '{{ catalog }}index.php?route=api/payment/address&api_token={{ api_token }}&store_id={{ store_id }}',
      type: 'post',
      data: $.param(inlineState.payment)
    }).then(function() {
      return inlineApiRequest({
        url: '{{ catalog }}index.php?route=api/payment/methods&api_token={{ api_token }}&store_id={{ store_id }}'
      });
    }).then(function() {
      if (inlineState.payment_code) {
        return inlineApiRequest({
          url: '{{ catalog }}index.php?route=api/payment/method&api_token={{ api_token }}&store_id={{ store_id }}',
          type: 'post',
          data: 'payment_method=' + encodeURIComponent(inlineState.payment_code)
        });
      }

      return $.Deferred().resolve().promise();
    }).then(function() {
      if (!inlineState.has_shipping) {
        return $.Deferred().resolve().promise();
      }

      return inlineApiRequest({
        url: '{{ catalog }}index.php?route=api/shipping/address&api_token={{ api_token }}&store_id={{ store_id }}',
        type: 'post',
        data: $.param(inlineState.shipping)
      }).then(function() {
        return inlineApiRequest({
          url: '{{ catalog }}index.php?route=api/shipping/methods&api_token={{ api_token }}&store_id={{ store_id }}'
        });
      }).then(function() {
        if (inlineState.shipping_code) {
          return inlineApiRequest({
            url: '{{ catalog }}index.php?route=api/shipping/method&api_token={{ api_token }}&store_id={{ store_id }}',
            type: 'post',
            data: 'shipping_method=' + encodeURIComponent(inlineState.shipping_code)
          });
        }

        return $.Deferred().resolve().promise();
      });
    }).then(function() {
      if (couponCode) {
        return inlineApiRequest({
          url: '{{ catalog }}index.php?route=api/coupon&api_token={{ api_token }}&store_id={{ store_id }}',
          type: 'post',
          data: 'coupon=' + encodeURIComponent(couponCode)
        });
      }

      return $.Deferred().resolve().promise();
    });
  }

  function inlineRecalculateCartPreview(showSuccess, options) {
    options = options || {};

    var skipCartReplace = options.skipCartReplace ? true : false;
    var applyDiscountPreview = options.applyDiscountPreview ? true : false;
    var couponCode = options.couponCode !== undefined ? options.couponCode : $('#input-inline-coupon').val();
    var discounts = inlineCollectDiscounts();
    var cartData = $('#inline-cart input[name^=\'product\'][type=\'text\'], #inline-cart input[name^=\'product\'][type=\'hidden\'], #inline-cart input[name^=\'product\'][type=\'radio\']:checked, #inline-cart input[name^=\'product\'][type=\'checkbox\']:checked, #inline-cart select[name^=\'product\'], #inline-cart textarea[name^=\'product\']').serialize();

    if (!cartData && !skipCartReplace) {
      $('#inline-totals').html('');
      return $.Deferred().resolve({ products: [], totals: [] }).promise();
    }

    var chain = $.Deferred().resolve().promise();

    if (!skipCartReplace) {
      chain = chain.then(function() {
        return inlineApiRequest({
          url: '{{ catalog }}index.php?route=api/cart/add&api_token={{ api_token }}&store_id={{ store_id }}',
          type: 'post',
          data: cartData
        });
      });
    }

    return chain.then(function() {
      return inlineSyncCheckoutContext(couponCode);
    }).then(function() {
      return inlineApiRequest({
        url: '{{ catalog }}index.php?route=api/cart/products&api_token={{ api_token }}&store_id={{ store_id }}',
        type: 'post',
        data: $.param({ discount: discounts })
      });
    }).done(function(json) {
      if (json.error && (json.error.warning || json.error.minimum || json.error.stock)) {
        showInlineAlert('danger', parseApiError(json.error));
      }

      inlineRenderCartFromApi(json, discounts);

      if (applyDiscountPreview) {
        inlineRequestDiscountPreview();
      }

      if (showSuccess) {
        showInlineAlert('success', '{{ text_success|escape('js') }}');
      }
    });
  }

  function inlinePersistOrderChanges(options) {
    options = options || {};

    var reloadOnSuccess = options.reloadOnSuccess ? true : false;
    var showSuccess = options.showSuccess === undefined ? true : options.showSuccess;
    var coupon = $('#input-inline-coupon').val();
    var discounts = inlineCollectDiscounts();

    return inlineRecalculateCartPreview(false, {
      couponCode: coupon,
      applyDiscountPreview: false
    }).then(function() {
      return inlineApiRequest({
        url: '{{ catalog }}index.php?route=api/customer&api_token={{ api_token }}&store_id={{ store_id }}',
        type: 'post',
        data: $.param(inlineState.customer)
      });
    }).then(function() {
      return inlineSyncCheckoutContext(coupon);
    }).then(function() {
      var orderData = 'comment=' + encodeURIComponent(inlineState.comment) + '&affiliate_id={{ affiliate_id|e('js') }}&skip_history=1';

      if (inlineState.payment_code) {
        orderData += '&payment_method=' + encodeURIComponent(inlineState.payment_code);
      }

      if (inlineState.has_shipping && inlineState.shipping_code) {
        orderData += '&shipping_method=' + encodeURIComponent(inlineState.shipping_code);
      }

      return inlineApiRequest({
        url: '{{ catalog }}index.php?route=api/order/edit&api_token={{ api_token }}&store_id={{ store_id }}&order_id={{ order_id }}',
        type: 'post',
        data: orderData
      });
    }).then(function() {
      return $.ajax({
        url: 'index.php?route=sale/order/applyLineDiscounts&user_token={{ user_token }}&order_id={{ order_id }}',
        type: 'post',
        dataType: 'json',
        data: {
          discount: discounts
        }
      });
    }).then(function() {
      return inlineRequestDiscountPreview();
    }).done(function() {
      if (showSuccess) {
        showInlineAlert('success', '{{ text_success|escape('js') }}');
      }

      if (reloadOnSuccess) {
        location.reload();
      }
    });
  }

  function inlineAddProductToApi(data) {
    return inlineApiRequest({
      url: '{{ catalog }}index.php?route=api/cart/add&api_token={{ api_token }}&store_id={{ store_id }}',
      type: 'post',
      data: data
    });
  }

  $(document).on('click', '.btn-quick-edit', function() {
    quickEditOpen($(this));
  });

  $('#button-quick-edit-save').on('click', function() {
    quickEditSave();
  });

  $('#inline-cart').on('click', '.button-inline-remove-item', function() {
    $(this).closest('tr').remove();

    if (!$('#inline-cart tr').not('.inline-cart-empty').length) {
      $('#inline-cart').html('<tr class="inline-cart-empty"><td colspan="7" class="text-center">{{ text_no_results }}</td></tr>');
    }

    inlineReindexCartRows();

    inlineRecalculateCartPreview(false).fail(function(error) {
      showInlineAlert('danger', parseApiError(error));
    });
  });

  $('#inline-cart').on('keyup change', 'input[name$="[quantity]"]', function() {
    clearTimeout(inlineRecalcTimer);
    inlineRecalcTimer = setTimeout(function() {
      inlineRecalculateCartPreview(false).fail(function(error) {
        showInlineAlert('danger', parseApiError(error));
      });
    }, 400);
  });

  $('#inline-cart').on('keyup change', 'input[name$="[discount]"]', function() {
    var $input = $(this);

    clearTimeout(inlineDiscountPreviewTimer);
    inlineDiscountPreviewTimer = setTimeout(function() {
      var value = parseFloat($input.val());

      if (isNaN(value) || value < 0) {
        value = 0;
      }

      if (value > 100) {
        value = 100;
      }

      $input.val(value);
      inlineRequestDiscountPreview();
    }, 350);
  });

  $('#input-inline-product').autocomplete({
    'source': function(request, response) {
      $.ajax({
        url: 'index.php?route=catalog/product/autocomplete&user_token={{ user_token }}&filter_name=' + encodeURIComponent(request),
        dataType: 'json',
        success: function(json) {
          response($.map(json, function(item) {
            return {
              label: item['name'],
              value: item['product_id'],
              model: item['model'],
              option: item['option']
            };
          }));
        }
      });
    },
    'select': function(item) {
      $('#input-inline-product').val(item['label']);
      $('#input-inline-product-id').val(item['value']);
      $('#input-inline-product').data('model', item['model'] || '');
      $('#input-inline-product').data('option', item['option'] || []);
    }
  });

  $('#button-inline-add-product').on('click', function() {
    var productId = $('#input-inline-product-id').val();
    var quantity = $('#input-inline-quantity').val() || '1';
    var option = $('#input-inline-product').data('option') || [];

    if (!productId) {
      showInlineAlert('danger', '{{ error_warning|escape('js') }}');
      return;
    }

    $('#form-inline-product-options')[0].reset();
    $('#input-inline-option-product-id').val(productId);
    $('#input-inline-option-quantity').val(quantity);

    var optionHtml = inlineBuildProductOptionFields(option);

    if (optionHtml) {
      $('#inline-product-options-body').html(optionHtml);
      $('#modal-inline-product-title').text($('#input-inline-product').val());
      $('#modal-inline-product-options').modal('show');

      $('.date', '#modal-inline-product-options').datetimepicker({
        language: '{{ datepicker }}',
        pickTime: false
      });

      $('.datetime', '#modal-inline-product-options').datetimepicker({
        language: '{{ datepicker }}',
        pickDate: true,
        pickTime: true
      });

      $('.time', '#modal-inline-product-options').datetimepicker({
        language: '{{ datepicker }}',
        pickDate: false
      });
    } else {
      inlineSyncApiCartFromCurrentTable().then(function() {
        return inlineAddProductToApi({
          product_id: productId,
          quantity: quantity
        });
      }).done(function() {
        inlineRecalculateCartPreview(false, {
          skipCartReplace: true,
          couponCode: $('#input-inline-coupon').val()
        });
        $('#input-inline-product').val('');
        $('#input-inline-product-id').val('');
        $('#input-inline-quantity').val('1');
      }).fail(function(error) {
        showInlineAlert('danger', parseApiError(error));
      });
    }
  });

  $('#button-inline-option-add').on('click', function() {
    var $button = $(this);

    $button.button('loading');

    $('.text-danger').remove();
    $('#modal-inline-product-options .form-group').removeClass('has-error');

    inlineSyncApiCartFromCurrentTable().then(function() {
      return inlineAddProductToApi($('#form-inline-product-options').serialize());
    }).done(function(json) {
      $('#modal-inline-product-options').modal('hide');
      $('#input-inline-product').val('');
      $('#input-inline-product-id').val('');
      $('#input-inline-quantity').val('1');
      inlineRecalculateCartPreview(false, {
        skipCartReplace: true,
        couponCode: $('#input-inline-coupon').val()
      });
    }).fail(function(error) {
      if (error && error.option) {
        for (var i in error.option) {
          var element = $('#inline-input-option' + i);

          if (element.parent().hasClass('input-group')) {
            element.parent().after('<div class="text-danger">' + error.option[i] + '</div>');
          } else {
            element.after('<div class="text-danger">' + error.option[i] + '</div>');
          }
        }

        $('.text-danger').parentsUntil('.form-group').parent().addClass('has-error');
      } else {
        showInlineAlert('danger', parseApiError(error));
      }
    }).always(function() {
      $button.button('reset');
    });
  });

  $('#modal-inline-product-options').delegate('button[id^="inline-button-upload"]', 'click', function() {
    var node = this;

    $('#form-upload').remove();

    $('body').prepend('<form enctype="multipart/form-data" id="form-upload" style="display:none;"><input type="file" name="file" /></form>');

    $('#form-upload input[name=\'file\']').trigger('click');

    if (typeof timer != 'undefined') {
      clearInterval(timer);
    }

    timer = setInterval(function() {
      if ($('#form-upload input[name=\'file\']').val() != '') {
        clearInterval(timer);

        $.ajax({
          url: 'index.php?route=tool/upload/upload&user_token={{ user_token }}',
          type: 'post',
          dataType: 'json',
          data: new FormData($('#form-upload')[0]),
          cache: false,
          contentType: false,
          processData: false,
          beforeSend: function() {
            $(node).button('loading');
          },
          complete: function() {
            $(node).button('reset');
          },
          success: function(json) {
            $(node).parent().find('.text-danger').remove();

            if (json['error']) {
              $(node).parent().find('input[type=\'hidden\']').after('<div class="text-danger">' + json['error'] + '</div>');
            }

            if (json['success']) {
              alert(json['success']);
            }

            if (json['code']) {
              $(node).parent().find('input[type=\'hidden\']').val(json['code']);
            }
          },
          error: function(xhr, ajaxOptions, thrownError) {
            alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
          }
        });
      }
    }, 500);
  });

  $('#button-inline-save-items').on('click', function() {
    var $button = $(this);

    $button.button('loading');

    inlinePersistOrderChanges({
      reloadOnSuccess: true,
      showSuccess: true
    }).fail(function(error) {
      if (error && error.responseJSON && error.responseJSON.error) {
        showInlineAlert('danger', parseApiError(error.responseJSON.error));
      } else {
        showInlineAlert('danger', parseApiError(error));
      }
    }).always(function() {
      $button.button('reset');
    });
  });

  $('#button-inline-apply-coupon').on('click', function() {
    var coupon = $('#input-inline-coupon').val();

    if (!coupon) {
      showInlineAlert('danger', '{{ error_warning|escape('js') }}');
      return;
    }

    inlineRecalculateCartPreview(false).then(function() {
      return inlineApiRequest({
        url: '{{ catalog }}index.php?route=api/coupon&api_token={{ api_token }}&store_id={{ store_id }}',
        type: 'post',
        data: 'coupon=' + encodeURIComponent(coupon)
      });
    }).then(function() {
      return inlineRecalculateCartPreview(false);
    }).done(function() {
      showInlineAlert('success', '{{ text_success|escape('js') }}');
    }).fail(function(error) {
      showInlineAlert('danger', parseApiError(error));
    });
  });

  // Intentionally do not auto-rebuild products/totals from API cart on page load,
  // because order view must reflect persisted order_product/order_total values.

  $(document).delegate('#button-invoice', 'click', function() {
	  $.ajax({
		  url: 'index.php?route=sale/order/createinvoiceno&user_token={{ user_token }}&order_id={{ order_id }}',
		  dataType: 'json',
		  beforeSend: function() {
			  $('#button-invoice').button('loading');
		  },
		  complete: function() {
			  $('#button-invoice').button('reset');
		  },
		  success: function(json) {
			  $('.alert-dismissible').remove();

			  if (json['error']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> ' + json['error'] + '</div>');
			  }

			  if (json['invoice_no']) {
				  $('#invoice').html(json['invoice_no']);

				  $('#button-invoice').replaceWith('<button disabled="disabled" class="btn btn-success btn-xs"><i class="fa fa-cog"></i></button>');
			  }
		  },
		  error: function(xhr, ajaxOptions, thrownError) {
			  alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
		  }
	  });
  });

  $(document).delegate('#button-reward-add', 'click', function() {
	  $.ajax({
		  url: 'index.php?route=sale/order/addreward&user_token={{ user_token }}&order_id={{ order_id }}',
		  type: 'post',
		  dataType: 'json',
		  beforeSend: function() {
			  $('#button-reward-add').button('loading');
		  },
		  complete: function() {
			  $('#button-reward-add').button('reset');
		  },
		  success: function(json) {
			  $('.alert-dismissible').remove();

			  if (json['error']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> ' + json['error'] + '</div>');
			  }

			  if (json['success']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> ' + json['success'] + '</div>');

				  $('#button-reward-add').replaceWith('<button id="button-reward-remove" data-toggle="tooltip" title="{{ button_reward_remove }}" class="btn btn-danger btn-xs"><i class="fa fa-minus-circle"></i></button>');
			  }
		  },
		  error: function(xhr, ajaxOptions, thrownError) {
			  alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
		  }
	  });
  });

  $(document).delegate('#button-reward-remove', 'click', function() {
	  $.ajax({
		  url: 'index.php?route=sale/order/removereward&user_token={{ user_token }}&order_id={{ order_id }}',
		  type: 'post',
		  dataType: 'json',
		  beforeSend: function() {
			  $('#button-reward-remove').button('loading');
		  },
		  complete: function() {
			  $('#button-reward-remove').button('reset');
		  },
		  success: function(json) {
			  $('.alert-dismissible').remove();

			  if (json['error']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> ' + json['error'] + '</div>');
			  }

			  if (json['success']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> ' + json['success'] + '</div>');

				  $('#button-reward-remove').replaceWith('<button id="button-reward-add" data-toggle="tooltip" title="{{ button_reward_add }}" class="btn btn-success btn-xs"><i class="fa fa-plus-circle"></i></button>');
			  }
		  },
		  error: function(xhr, ajaxOptions, thrownError) {
			  alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
		  }
	  });
  });

  $(document).delegate('#button-commission-add', 'click', function() {
	  $.ajax({
		  url: 'index.php?route=sale/order/addcommission&user_token={{ user_token }}&order_id={{ order_id }}',
		  type: 'post',
		  dataType: 'json',
		  beforeSend: function() {
			  $('#button-commission-add').button('loading');
		  },
		  complete: function() {
			  $('#button-commission-add').button('reset');
		  },
		  success: function(json) {
			  $('.alert-dismissible').remove();

			  if (json['error']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> ' + json['error'] + '</div>');
			  }

			  if (json['success']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> ' + json['success'] + '</div>');

				  $('#button-commission-add').replaceWith('<button id="button-commission-remove" data-toggle="tooltip" title="{{ button_commission_remove }}" class="btn btn-danger btn-xs"><i class="fa fa-minus-circle"></i></button>');
			  }
		  },
		  error: function(xhr, ajaxOptions, thrownError) {
			  alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
		  }
	  });
  });

  $(document).delegate('#button-commission-remove', 'click', function() {
	  $.ajax({
		  url: 'index.php?route=sale/order/removecommission&user_token={{ user_token }}&order_id={{ order_id }}',
		  type: 'post',
		  dataType: 'json',
		  beforeSend: function() {
			  $('#button-commission-remove').button('loading');
		  },
		  complete: function() {
			  $('#button-commission-remove').button('reset');
		  },
		  success: function(json) {
			  $('.alert-dismissible').remove();

			  if (json['error']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> ' + json['error'] + '</div>');
			  }

			  if (json['success']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> ' + json['success'] + '</div>');

				  $('#button-commission-remove').replaceWith('<button id="button-commission-add" data-toggle="tooltip" title="{{ button_commission_add }}" class="btn btn-success btn-xs"><i class="fa fa-plus-circle"></i></button>');
			  }
		  },
		  error: function(xhr, ajaxOptions, thrownError) {
			  alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
		  }
	  });
  });

  var api_token = '';

  $.ajax({
	  url: '{{ catalog }}index.php?route=api/login',
	  type: 'post',
	  dataType: 'json',
	  data: 'key={{ api_key }}',
	  crossDomain: true,
	  success: function(json) {
		  $('.alert').remove();
		  if (json['error']) {
			  if (json['error']['key']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-danger"><i class="fa fa-exclamation-circle"></i> ' + json['error']['key'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');
			  }
			  if (json['error']['ip']) {
				  $('#content > .container-fluid').prepend('<div class="alert alert-danger"><i class="fa fa-exclamation-circle"></i> ' + json['error']['ip'] + ' <button type="button" id="button-ip-add" data-loading-text="{{ text_loading }}" class="btn btn-danger btn-xs pull-right"><i class="fa fa-plus"></i>{{ button_ip_add }}</button></div>');
			  }
		  }
		  if (json['token']) {
			  api_token = json['token'];
		  }
	  },
	  error: function(xhr, ajaxOptions, thrownError) {
		  alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
	  }
  });

  $('#history').delegate('.pagination a', 'click', function(e) {
	  e.preventDefault();

	  $('#history').load(this.href);
  });

  $('#history').load('index.php?route=sale/order/history&user_token={{ user_token }}&order_id={{ order_id }}');

  $('#button-history').on('click', function() {
	  $.ajax({
		  url: '{{ catalog }}index.php?route=api/order/history&api_token={{ api_token }}&store_id={{ store_id }}&order_id={{ order_id }}',
		  type: 'post',
		  dataType: 'json',
		  data: 'order_status_id=' + encodeURIComponent($('select[name=\'order_status_id\']').val()) + '&notify=' + ($('input[name=\'notify\']').prop('checked') ? 1 : 0) + '&override=' + ($('input[name=\'override\']').prop('checked') ? 1 : 0) + '&append=' + ($('input[name=\'append\']').prop('checked') ? 1 : 0) + '&comment=' + encodeURIComponent($('textarea[name=\'comment\']').val()),
		  beforeSend: function() {
			  $('#button-history').button('loading');
		  },
		  complete: function() {
			  $('#button-history').button('reset');
		  },
		  success: function(json) {
			  $('.alert-dismissible').remove();

			  if (json['error']) {
				  $('#history').before('<div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> ' + json['error'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');
			  }

			  if (json['success']) {
				  $('#history').load('index.php?route=sale/order/history&user_token={{ user_token }}&order_id={{ order_id }}');

				  $('#history').before('<div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> ' + json['success'] + ' <button type="button" class="close" data-dismiss="alert">&times;</button></div>');

				  $('textarea[name=\'comment\']').val('');
			  }
		  },
		  error: function(xhr, ajaxOptions, thrownError) {
			  alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
		  }
	  });
  });
  //--></script>
</div>
{{ footer }} 
