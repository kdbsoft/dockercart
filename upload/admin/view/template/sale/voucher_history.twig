{% if histories %}
<div class="voucher-history-list">
  {% for history in histories %}
  <div class="voucher-history-entry">
    <div class="voucher-history-entry__icon">
      <i data-lucide="receipt" width="16" height="16"></i>
    </div>
    <div class="voucher-history-entry__info">
      <div class="voucher-history-entry__title">
        {{ column_order_id }} #{{ history.order_id }} &mdash; {{ history.customer }}
      </div>
      <div class="voucher-history-entry__meta">
        {{ history.amount }} &middot; {{ history.date_added }}
      </div>
    </div>
  </div>
  {% endfor %}
</div>
<div class="row">
  <div class="col-sm-6 text-left">{{ pagination }}</div>
  <div class="col-sm-6 text-right">{{ results }}</div>
</div>
{% else %}
<div class="voucher-history-empty">
  <i data-lucide="inbox" width="32" height="32"></i>
  <p>{{ text_no_results }}</p>
</div>
{% endif %}
