<style>
  .dc-star { position: relative; display: inline-block; color: #e5e7eb; line-height: 1; }
  .dc-star.active { color: #f59e0b; }
  .dc-star.dc-star-partial { color: #e5e7eb; }
  .dc-star.dc-star-partial::after { content: '★'; position: absolute; inset: 0; color: #f59e0b; clip-path: inset(0 calc(100% - var(--pct)) 0 0); }
  .dc-stars { display: inline-flex; gap: 0; }
  .dc-stars .dc-star { font-size: 16px; }
  .dc-stars-sm .dc-star { font-size: 13px; }
  .dc-stars-hero .dc-star { font-size: 20px; }
  .dc-stars-summary .dc-star { font-size: 14px; }

  .dc-stars-input { display: inline-flex; gap: 4px; cursor: pointer; user-select: none; }
  .dc-stars-input .dc-star { font-size: 1.65rem; transition: color .15s, transform .15s; }
  .dc-stars-input .dc-star.lit { color: #f59e0b; }
  .dc-stars-input .dc-star:hover { transform: translateY(-2px) scale(1.1); }

  .dc-avatar {
    width: 40px;
    height: 40px;
    border-radius: 9999px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #fff;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.02em;
  }

  .dc-review-hero { background: #f9fafb; border: 1px solid #f3f4f6; border-radius: 1rem; }
  .dc-review-overview { background: #f9fafb; border: 1px solid #f3f4f6; border-radius: 1rem; }
  .dc-review-overview-compact { background: #f9fafb; border: 1px solid #f3f4f6; border-radius: 1rem; }
  .dc-rating-number { color: #111827; letter-spacing: -.06em; }
  .dc-dist-bar { height: 8px; border-radius: 9999px; background: #f3f4f6; overflow: hidden; }
  .dc-dist-bar > span { display: block; height: 100%; border-radius: 9999px; background: #f59e0b; }
  .dc-rating-popover { opacity: 0; pointer-events: none; transition: opacity .15s ease; }
  .dc-rating-hover:hover .dc-rating-popover { opacity: 1; pointer-events: auto; }
  .dc-rating-popover, .dc-rating-popover * { cursor: auto; }
  .dc-rating-popover a { cursor: pointer; }

  .dc-review-card { position: relative; transition: border-color .2s; }
  .dc-review-card-divider { height: 1px; background: #f3f4f6; }

  .dc-review-media { display: flex; flex-direction: column; gap: 14px; }
  .dc-review-thumbs { display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 10px; }
  .dc-review-thumb { position: relative; aspect-ratio: 1; max-width: 220px; border-radius: 16px; overflow: hidden; border: 1px solid #e5e7eb; cursor: zoom-in; background: #f9fafb; transition: border-color .2s, box-shadow .2s, transform .2s; }
  .dc-review-thumb:hover { border-color: #93c5fd; box-shadow: 0 0 0 3px #dbeafe; transform: translateY(-2px); }
  .dc-review-thumb img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .dc-review-thumb-zoom { position: absolute; right: 9px; bottom: 9px; display: inline-flex; align-items: center; justify-content: center; width: 32px; height: 32px; color: #fff; background: rgba(15,23,42,.7); border-radius: 9999px; opacity: 0; transition: opacity .2s; }
  .dc-review-thumb:hover .dc-review-thumb-zoom, .dc-review-thumb:focus-visible .dc-review-thumb-zoom { opacity: 1; }
  .dc-review-video { width: min(100%, 640px); }

  .dc-lightbox { position: fixed; inset: 0; z-index: 2000; background: rgba(0,0,0,.92); display: flex; align-items: center; justify-content: center; padding: 56px 72px; }
  .dc-lightbox.dc-hidden { display: none; }
  .dc-lightbox img { max-width: min(92vw, 1600px); max-height: 86vh; border-radius: 12px; object-fit: contain; box-shadow: 0 18px 60px rgba(0,0,0,.35); }
  .dc-lightbox-close, .dc-lightbox-nav { position: absolute; display: inline-flex; align-items: center; justify-content: center; background: rgba(255,255,255,.14); border: 1px solid rgba(255,255,255,.2); color: #fff; cursor: pointer; transition: background .15s; }
  .dc-lightbox-close:hover, .dc-lightbox-nav:hover { background: rgba(255,255,255,.28); }
  .dc-lightbox-close { top: 18px; right: 22px; width: 42px; height: 42px; border-radius: 9999px; font-size: 22px; }
  .dc-lightbox-nav { top: 50%; width: 48px; height: 64px; margin-top: -32px; border-radius: 14px; font-size: 28px; }
  .dc-lightbox-prev { left: 18px; }
  .dc-lightbox-next { right: 18px; }
  .dc-lightbox-counter { position: absolute; left: 50%; bottom: 18px; transform: translateX(-50%); color: rgba(255,255,255,.85); font-size: 13px; font-variant-numeric: tabular-nums; }

  .dc-review-upload-preview { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 8px; }
  .dc-review-upload-preview .item { position: relative; width: 76px; height: 76px; border-radius: 12px; overflow: hidden; border: 1px solid #e5e7eb; }
  .dc-review-upload-preview .item img { width: 100%; height: 100%; object-fit: cover; }
  .dc-review-upload-preview .item .remove { position: absolute; top: 2px; right: 2px; width: 20px; height: 20px; border-radius: 9999px; background: rgba(0,0,0,.55); color: #fff; border: none; font-size: 12px; line-height: 1; cursor: pointer; }

  .dc-review-sort { display: inline-flex; gap: 4px; padding: 3px; background: #f3f4f6; border-radius: 9999px; }
  .dc-review-sort a { padding: 6px 14px; border-radius: 9999px; font-size: 13px; font-weight: 700; color: #6b7280; text-decoration: none; transition: background .15s, color .15s, box-shadow .15s; }
  .dc-review-sort a.active { background: #fff; color: #111827; box-shadow: 0 1px 3px rgba(0,0,0,.08); }
  .dc-review-sort a:hover:not(.active) { color: #374151; }

  .dc-review-show-more-btn { width: 100%; display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 12px 20px; background: #fff; border: 1px solid #e5e7eb; border-radius: 12px; font-size: 14px; font-weight: 700; color: #374151; cursor: pointer; transition: border-color .15s, background .15s; }
  .dc-review-show-more-btn:hover { border-color: #93c5fd; background: #eff6ff; color: #1d4ed8; }
  .dc-review-show-more-btn:disabled { opacity: .6; cursor: wait; }

  .dc-review-modal { position: fixed; inset: 0; z-index: 100; display: flex; overflow-y: auto; padding: 16px; }
  .dc-review-modal.hidden { display: none; }
  .dc-review-modal-backdrop { position: fixed; inset: 0; background: rgba(15,23,42,.5); backdrop-filter: blur(4px); }
  .dc-review-modal-card { position: relative; margin: auto; width: 100%; max-width: 640px; background: #fff; border-radius: 16px; box-shadow: 0 20px 60px rgba(0,0,0,.2); }
  .dc-review-modal-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 22px 28px 18px; background: #fff; border-bottom: 1px solid #f3f4f6; border-radius: 16px 16px 0 0; }
  .dc-review-modal-body { padding: 24px 28px 28px; }
  .dc-review-modal-body #form-review > div:not(.dc-review-alert) { display: flex; flex-direction: column; gap: 24px; }
  .dc-review-modal-body #form-review > div > * { margin-top: 0 !important; }
  .dc-review-modal-body #form-review > div > .grid { display: grid !important; }

  .pagination { display: inline-flex; flex-wrap: wrap; gap: 5px; list-style: none; margin: 0; padding: 0; }
  .pagination li a, .pagination li span { display: inline-flex; align-items: center; justify-content: center; min-width: 34px; height: 34px; padding: 0 9px; border: 1px solid #e5e7eb; border-radius: 10px; font-size: 12px; color: #4b5563; text-decoration: none; transition: border-color .15s, color .15s, background .15s; }
  .pagination li.active span { background: #2563eb; border-color: #2563eb; color: #fff; font-weight: 700; }
  .pagination li a:hover { border-color: #93c5fd; color: #2563eb; background: #eff6ff; }
  .pagination li { margin: 0; }

  @media (max-width: 639px) {
    .dc-review-thumbs { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .dc-review-thumb { max-width: none; }
    .dc-review-thumb-zoom { opacity: 1; width: 28px; height: 28px; }
    .dc-lightbox { padding: 52px 14px; }
    .dc-lightbox img { max-width: 96vw; max-height: 80vh; }
    .dc-lightbox-nav { width: 38px; height: 52px; margin-top: -26px; }
    .dc-lightbox-prev { left: 8px; }
    .dc-lightbox-next { right: 8px; }
    .dc-review-modal { padding: 12px; }
    .dc-review-modal-card { border-radius: 16px 16px 0 0; }
    .dc-review-sort { width: 100%; overflow-x: auto; }
  }
</style>
