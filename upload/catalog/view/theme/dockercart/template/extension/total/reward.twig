<div class="bg-white rounded-2xl border border-gray-100 shadow-sm p-5 mb-4">
  <h3 class="text-sm font-bold uppercase tracking-widest text-gray-500 mb-3">{{ heading_title }}</h3>
  <div class="flex gap-2 items-stretch">
    <input type="text" name="reward" value="{{ reward }}" placeholder="{{ entry_reward }}" id="input-reward"
      class="flex-1 h-11 px-4 bg-gray-50 border border-gray-300 rounded-lg text-sm text-text-primary placeholder-gray-400 focus:border-primary focus:bg-white transition">
    <button type="button" id="button-reward" class="px-5 h-11 bg-gray-100 hover:bg-gray-200 text-text-primary font-medium rounded-lg transition text-sm whitespace-nowrap flex-shrink-0">
      <i data-lucide="stars" class="w-4 h-4 inline-block -ml-1 mr-1"></i>{{ button_reward|default('Apply') }}
    </button>
  </div>
</div>
<script>
document.addEventListener('DOMContentLoaded', function() {
  var btn = document.getElementById('button-reward');
  if (!btn) return;
  btn.addEventListener('click', function() {
    var input = document.getElementById('input-reward');
    var code = input ? input.value.trim() : '';
    if (!code) return;
    fetch('index.php?route=extension/total/reward/reward', {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: 'reward=' + encodeURIComponent(code)
    })
    .then(function(r) { return r.json(); })
    .then(function(json) {
      if (json.error) alert(json.error);
      if (json.redirect) window.location.href = json.redirect;
    });
  });
});
</script>
