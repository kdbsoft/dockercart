{# Latest module for dockercart theme #}
{% if products|length %}
<section id="latest" class="py-16 bg-white px-4 sm:px-6 lg:px-8">
  <div class="max-w-7xl mx-auto">
    <div class="mb-8">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-teal-500 text-sm font-semibold uppercase tracking-widest mb-1">{{ section_small }}</p>
          <h2 class="text-2xl sm:text-3xl font-bold text-gray-900">{{ section_title }}</h2>
        </div>
        <div class="hidden sm:block">
          <a href="{{ section_href }}" class="text-sm font-semibold dc-view-all-link">{{ text_view_all }}</a>
        </div>
      </div>
    </div>

    {% if category_filter and categories|length > 0 %}
    <div class="dc-category-filter mb-6 flex flex-wrap gap-2" data-module="latest">
      {% set has_other = false %}
      {% for product in products %}
        {% if product.category_id == 0 %}
          {% set has_other = true %}
        {% endif %}
      {% endfor %}
      {% for cat in categories %}
      <button type="button" class="dc-cat-btn px-4 py-2 text-sm font-medium rounded-full border border-gray-300 text-gray-700 hover:bg-gray-50 hover:border-teal-500 transition-all duration-200" data-category-id="{{ cat.category_id }}">
        {{ cat.name }}
      </button>
      {% endfor %}
      {% if has_other %}
      <button type="button" class="dc-cat-btn px-4 py-2 text-sm font-medium rounded-full border border-gray-300 text-gray-700 hover:bg-gray-50 hover:border-teal-500 transition-all duration-200" data-category-id="0">
        {{ text_other }}
      </button>
      {% endif %}
    </div>
    {% endif %}

    <div id="latest-grid" class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4 sm:gap-6">
      {% for product in products %}
      <div class="dc-product-item" data-category-id="{{ product.category_id }}">
        {% include 'dockercart/template/product/product_card_widget.twig' with {
          product: product,
          accent_color: 'teal-600',
          btn_cart_classes: 'bg-teal-600 text-white hover:bg-teal-700',
          btn_quick_hover: 'hover:bg-teal-600',
          show_new_arrival: true
        } %}
      </div>
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}

{% if category_filter and categories|length > 0 %}
<style>
.dc-cat-btn.bg-teal-600 {
  border-color: rgb(13 148 136);
}
.dc-cat-btn.bg-teal-600:hover {
  background-color: rgb(15 118 110);
  border-color: rgb(15 118 110);
}
</style>
<script>
document.addEventListener('DOMContentLoaded', function() {
  const filterContainer = document.querySelector('.dc-category-filter[data-module="latest"]');
  if (!filterContainer) return;
  
  const buttons = filterContainer.querySelectorAll('.dc-cat-btn');
  const productItems = document.querySelectorAll('#latest .dc-product-item');
  let activeBtn = null;
  
  buttons.forEach(btn => {
    btn.addEventListener('click', function() {
      const catId = this.dataset.categoryId;
      
      if (activeBtn === this) {
        this.classList.remove('bg-teal-600', 'text-white', 'border-teal-600');
        this.classList.add('border-gray-300', 'text-gray-700', 'hover:bg-gray-50');
        activeBtn = null;
        productItems.forEach(item => item.style.display = '');
        return;
      }
      
      buttons.forEach(b => {
        b.classList.remove('bg-teal-600', 'text-white', 'border-teal-600');
        b.classList.add('border-gray-300', 'text-gray-700', 'hover:bg-gray-50');
      });
      
      this.classList.remove('border-gray-300', 'text-gray-700', 'hover:bg-gray-50');
      this.classList.add('bg-teal-600', 'text-white', 'border-teal-600');
      activeBtn = this;
      
      productItems.forEach(item => {
        if (item.dataset.categoryId === catId) {
          item.style.display = '';
        } else {
          item.style.display = 'none';
        }
      });
    });
  });
});
</script>
{% endif %}

{# Quick View modal is created dynamically by quick-view.js #}
